# 📋 การแปลงสถานะจากฐานข้อมูลไปยังหน้าเว็บ - Status Database to Frontend Mapping

**Last Updated**: 2026-02-03

เอกสารนี้อธิบายการแปลงสถานะจากฐานข้อมูลไปยังหน้าเว็บในส่วนของ **รายการงานที่รับผิดชอบ (Tax Status Page)**

---

## 🔄 กระบวนการแปลงสถานะ

### ขั้นตอนการแปลง

1. **ดึงข้อมูลจากฐานข้อมูล** → Field ในตาราง `monthly_tax_data`
2. **แปลง/Derive สถานะ** → ถ้าไม่มีสถานะในฐานข้อมูล จะ derive จาก timestamp fields
3. **แปลงเป็นข้อความภาษาไทย** → ใช้ `getStatusLabel()` เพื่อแปลงค่าเป็นข้อความ
4. **กำหนดสี** → ใช้ `getStatusColor()` เพื่อกำหนดสีของ Badge
5. **แสดงผลในหน้าเว็บ** → แสดงเป็น Badge พร้อมสีและข้อความ

---

## 📊 สถานะ ภ.ง.ด. (PND Status)

### ข้อมูลจากฐานข้อมูล

**Field ในฐานข้อมูล**: `pnd_status` (VARCHAR(100))

**Location**: `src/components/TaxStatus/TaxStatusTable.tsx` (line 586)
```typescript
pndStatus: item.pnd_status || null
```

**หมายเหตุ**: ใช้ค่าจากฐานข้อมูลโดยตรง ไม่มีการ derive

### การแปลงเป็นข้อความภาษาไทย

**Function**: `getStatusLabel()`  
**Location**: `src/components/TaxStatus/TaxStatusTable.tsx` (lines 195-229)

| ค่าในฐานข้อมูล (Database Value) | ข้อความที่แสดงในหน้าเว็บ (Frontend Display) | สี (Color) |
|--------------------------------|------------------------------------------|----------|
| `paid` | **ชำระแล้ว** | `#ffc107` (เหลือง) |
| `received_receipt` | **รับใบเสร็จ** | `#4facfe` (ฟ้า) |
| `sent_to_customer` | **ส่งลูกค้าแล้ว** | `#81d4fa` (ฟ้าอ่อน) |
| `draft_completed` | **ร่างแบบเสร็จแล้ว** | `#ffcdd2` (ชมพูอ่อน) |
| `passed` | **ผ่าน** | `#4caf50` (เขียว) |
| `pending_review` | **รอตรวจ** | `#ff6b35` (ส้ม) |
| `pending_recheck` | **รอตรวจอีกครั้ง** | `#f44336` (แดง) |
| `draft_ready` | **ร่างแบบได้** | `#ffcdd2` (ชมพูอ่อน) |
| `needs_correction` | **แก้ไข** | `#f44336` (แดง) |
| `edit` | **แก้ไข** | `#f44336` (แดง) |
| `inquire_customer` | **สอบถามลูกค้าเพิ่มเติม** | `#9c27b0` (ม่วง) |
| `additional_review` | **ตรวจสอบเพิ่มเติม** | `#81d4fa` (ฟ้าอ่อน) |
| `not_started` | **สถานะยังไม่ดำเนินการ** | `#808080` (เทา) |
| `null` หรือ `''` | **สถานะยังไม่ดำเนินการ** | `#808080` (เทา) |

---

## 📊 สถานะ ภ.พ.30 (PP30 Status)

### ข้อมูลจากฐานข้อมูล

**Fields ในฐานข้อมูล**:
- `pp30_form` (VARCHAR(100)) - สถานะ ภ.พ.30 (หลัง migration 028) - **ใช้สำหรับเก็บสถานะโดยตรง**
- `pp30_sent_to_customer_date` (DATETIME) - วันที่ส่งลูกค้า - **ใช้สำหรับ derive สถานะ `sent_to_customer`**
- `pp30_review_returned_date` (DATETIME) - วันที่ส่งตรวจคืน - **ใช้สำหรับ derive สถานะ `pending_recheck`**
- `pp30_sent_for_review_date` (DATETIME) - วันที่ส่งตรวจ - **ใช้สำหรับ derive สถานะ `pending_review`**
- `vat_draft_completed_date` (DATETIME) - วันที่ร่างแบบเสร็จแล้ว - **ใช้สำหรับ derive สถานะ `draft_completed`**

**หมายเหตุ**: `pp30_filing_response` (TEXT) เป็นข้อมูลที่ผู้ใช้กรอก (การตอบกลับการยื่นแบบ) ไม่ใช่สถานะ จึงไม่ใช้ในการ derive สถานะ

**Location**: `src/components/TaxStatus/TaxStatusTable.tsx` (line 533)
```typescript
const pp30Status = derivePp30Status(item)
```

### การ Derive สถานะ

**Function**: `derivePp30Status()`  
**Location**: `src/utils/pp30StatusUtils.ts`

**ลำดับการตรวจสอบ**:

1. **ถ้ามี `pp30_form` และไม่ใช่ boolean** → ใช้ค่าจาก `pp30_form` โดยตรง (หลัง migration 028)
2. **ถ้าไม่มี → Derive จาก timestamp fields** (เรียงตามวันที่ล่าสุด):
   - `pp30_sent_to_customer_date` → `'sent_to_customer'` (ส่งลูกค้าแล้ว)
   - `pp30_review_returned_date` → `'pending_recheck'` (รอตรวจอีกครั้ง)
   - `pp30_sent_for_review_date` → `'pending_review'` (รอตรวจ)
   - `vat_draft_completed_date` → `'draft_completed'` (ร่างแบบเสร็จแล้ว) ⚠️ สำคัญ: เช็คก่อน `pp30_review_returned_date`
3. **ถ้ามี `pp30_form = true/1`** → ใช้ค่า `'not_started'` (สถานะยังไม่ดำเนินการ) (backward compatibility)
4. **ถ้าไม่มีเลย** → `null` (สถานะยังไม่ดำเนินการ)

**หมายเหตุ**: `pp30_filing_response` เป็นข้อมูลที่ผู้ใช้กรอก (TEXT) ไม่ใช่สถานะ จึงไม่ใช้ในการ derive สถานะ

### การแปลงเป็นข้อความภาษาไทย

**Function**: `getStatusLabel()`  
**Location**: `src/components/TaxStatus/TaxStatusTable.tsx` (lines 195-229)

| ค่าในฐานข้อมูล (Database Value) | ข้อความที่แสดงในหน้าเว็บ (Frontend Display) | สี (Color) |
|--------------------------------|------------------------------------------|----------|
| `paid` | **ชำระแล้ว** | `#ffc107` (เหลือง) |
| `received_receipt` | **รับใบเสร็จ** | `#4facfe` (ฟ้า) |
| `sent_to_customer` | **ส่งลูกค้าแล้ว** | `#81d4fa` (ฟ้าอ่อน) |
| `draft_completed` | **ร่างแบบเสร็จแล้ว** | `#ffcdd2` (ชมพูอ่อน) |
| `passed` | **ผ่าน** | `#4caf50` (เขียว) |
| `pending_review` | **รอตรวจ** | `#ff6b35` (ส้ม) |
| `pending_recheck` | **รอตรวจอีกครั้ง** | `#f44336` (แดง) |
| `draft_ready` | **ร่างแบบได้** | `#ffcdd2` (ชมพูอ่อน) |
| `needs_correction` | **แก้ไข** | `#f44336` (แดง) |
| `inquire_customer` | **สอบถามลูกค้าเพิ่มเติม** | `#9c27b0` (ม่วง) |
| `additional_review` | **ตรวจสอบเพิ่มเติม** | `#81d4fa` (ฟ้าอ่อน) |
| `not_submitted` | **ไม่มียื่น** | `#808080` (เทา) |
| `not_started` | **สถานะยังไม่ดำเนินการ** | `#808080` (เทา) |
| `null` หรือ `''` | **สถานะยังไม่ดำเนินการ** | `#808080` (เทา) |

---

## 📋 สรุปทุกสถานะ

### สถานะ ภ.ง.ด. (PND Status)

| ค่าในฐานข้อมูล | Field ในฐานข้อมูล | ข้อความที่แสดง | สี | หมายเหตุ |
|--------------|-----------------|--------------|-----|---------|
| `paid` | `pnd_status` | **ชำระแล้ว** | เหลือง (#ffc107) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `received_receipt` | `pnd_status` | **รับใบเสร็จ** | ฟ้า (#4facfe) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `sent_to_customer` | `pnd_status` | **ส่งลูกค้าแล้ว** | ฟ้าอ่อน (#81d4fa) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `draft_completed` | `pnd_status` | **ร่างแบบเสร็จแล้ว** | ชมพูอ่อน (#ffcdd2) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `passed` | `pnd_status` | **ผ่าน** | เขียว (#4caf50) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `pending_review` | `pnd_status` | **รอตรวจ** | ส้ม (#ff6b35) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `pending_recheck` | `pnd_status` | **รอตรวจอีกครั้ง** | แดง (#f44336) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `draft_ready` | `pnd_status` | **ร่างแบบได้** | ชมพูอ่อน (#ffcdd2) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `needs_correction` | `pnd_status` | **แก้ไข** | แดง (#f44336) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `edit` | `pnd_status` | **แก้ไข** | แดง (#f44336) | ใช้ค่าจากฐานข้อมูลโดยตรง (backward compatibility) |
| `inquire_customer` | `pnd_status` | **สอบถามลูกค้าเพิ่มเติม** | ม่วง (#9c27b0) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `additional_review` | `pnd_status` | **ตรวจสอบเพิ่มเติม** | ฟ้าอ่อน (#81d4fa) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `not_started` | `pnd_status` | **สถานะยังไม่ดำเนินการ** | เทา (#808080) | ใช้ค่าจากฐานข้อมูลโดยตรง |
| `null` หรือ `''` | `pnd_status` | **สถานะยังไม่ดำเนินการ** | เทา (#808080) | ถ้าไม่มีค่าในฐานข้อมูล |

### สถานะ ภ.พ.30 (PP30 Status)

| ค่าในฐานข้อมูล | Fields ที่ใช้ | ข้อความที่แสดง | สี | หมายเหตุ |
|--------------|-------------|--------------|-----|---------|
| `paid` | `pp30_form` | **ชำระแล้ว** | เหลือง (#ffc107) | ใช้ค่าจาก `pp30_form` |
| `received_receipt` | `pp30_form` | **รับใบเสร็จ** | ฟ้า (#4facfe) | ใช้ค่าจาก `pp30_form` โดยตรง (ไม่ต้อง derive จาก timestamp) |
| `sent_to_customer` | `pp30_form` หรือ `pp30_sent_to_customer_date` | **ส่งลูกค้าแล้ว** | ฟ้าอ่อน (#81d4fa) | ถ้าไม่มี `pp30_form` จะ derive จาก `pp30_sent_to_customer_date` |
| `draft_completed` | `pp30_form` หรือ `vat_draft_completed_date` | **ร่างแบบเสร็จแล้ว** | ชมพูอ่อน (#ffcdd2) | ถ้าไม่มี `pp30_form` จะ derive จาก `vat_draft_completed_date` |
| `passed` | `pp30_form` | **ผ่าน** | เขียว (#4caf50) | ใช้ค่าจาก `pp30_form` |
| `pending_review` | `pp30_form` หรือ `pp30_sent_for_review_date` | **รอตรวจ** | ส้ม (#ff6b35) | ถ้าไม่มี `pp30_form` จะ derive จาก `pp30_sent_for_review_date` |
| `pending_recheck` | `pp30_form` หรือ `pp30_review_returned_date` | **รอตรวจอีกครั้ง** | แดง (#f44336) | ถ้าไม่มี `pp30_form` จะ derive จาก `pp30_review_returned_date` |
| `draft_ready` | `pp30_form` | **ร่างแบบได้** | ชมพูอ่อน (#ffcdd2) | ใช้ค่าจาก `pp30_form` |
| `needs_correction` | `pp30_form` | **แก้ไข** | แดง (#f44336) | ใช้ค่าจาก `pp30_form` |
| `inquire_customer` | `pp30_form` | **สอบถามลูกค้าเพิ่มเติม** | ม่วง (#9c27b0) | ใช้ค่าจาก `pp30_form` |
| `additional_review` | `pp30_form` | **ตรวจสอบเพิ่มเติม** | ฟ้าอ่อน (#81d4fa) | ใช้ค่าจาก `pp30_form` |
| `not_submitted` | `pp30_form` | **ไม่มียื่น** | เทา (#808080) | ใช้ค่าจาก `pp30_form` (เฉพาะ PP30) |
| `not_started` | `pp30_form` หรือ `pp30_form = true/1` | **สถานะยังไม่ดำเนินการ** | เทา (#808080) | ถ้า `pp30_form = true/1` จะ derive เป็น `not_started` (backward compatibility) |
| `null` หรือ `''` | ไม่มี | **สถานะยังไม่ดำเนินการ** | เทา (#808080) | ถ้าไม่มีค่าในฐานข้อมูล |

---

## 🔍 ตัวอย่างการทำงาน

### ตัวอย่างที่ 1: สถานะ "ชำระแล้ว" (paid)

**สำหรับ ภ.ง.ด. (PND)**:
1. **ฐานข้อมูล**: `pnd_status = 'paid'`
2. **Frontend**: ใช้ค่า `'paid'` โดยตรง
3. **แปลงเป็นข้อความ**: `getStatusLabel('paid')` → `'ชำระแล้ว'`
4. **กำหนดสี**: `getStatusColor('paid')` → `'#ffc107'` (เหลือง)
5. **แสดงผล**: Badge สีเหลืองพร้อมข้อความ "ชำระแล้ว"

**สำหรับ ภ.พ.30 (PP30)**:
1. **ฐานข้อมูล**: `pp30_form = 'paid'`
2. **Frontend**: ใช้ค่า `'paid'` จาก `pp30_form` โดยตรง
3. **แปลงเป็นข้อความ**: `getStatusLabel('paid')` → `'ชำระแล้ว'`
4. **กำหนดสี**: `getStatusColor('paid')` → `'#ffc107'` (เหลือง)
5. **แสดงผล**: Badge สีเหลืองพร้อมข้อความ "ชำระแล้ว"

### ตัวอย่างที่ 2: สถานะ "รอตรวจ" (pending_review)

**สำหรับ ภ.ง.ด. (PND)**:
1. **ฐานข้อมูล**: `pnd_status = 'pending_review'`
2. **Frontend**: ใช้ค่า `'pending_review'` โดยตรง
3. **แปลงเป็นข้อความ**: `getStatusLabel('pending_review')` → `'รอตรวจ'`
4. **กำหนดสี**: `getStatusColor('pending_review')` → `'#ff6b35'` (ส้ม)
5. **แสดงผล**: Badge สีส้มพร้อมข้อความ "รอตรวจ"

**สำหรับ ภ.พ.30 (PP30)**:
1. **ฐานข้อมูล**: `pp30_sent_for_review_date = '2026-02-03 10:00:00'` (และไม่มี `pp30_form`)
2. **Frontend**: `derivePp30Status()` จะ return `'pending_review'` (derive จาก timestamp)
3. **แปลงเป็นข้อความ**: `getStatusLabel('pending_review')` → `'รอตรวจ'`
4. **กำหนดสี**: `getStatusColor('pending_review')` → `'#ff6b35'` (ส้ม)
5. **แสดงผล**: Badge สีส้มพร้อมข้อความ "รอตรวจ"

---

## 📝 ไฟล์ที่เกี่ยวข้อง

### Frontend Components
- `src/components/TaxStatus/TaxStatusTable.tsx` - ตารางสถานะยื่นภาษี
  - `getStatusLabel()`: แปลงค่าเป็นข้อความภาษาไทย (lines 195-229)
  - `getStatusColor()`: กำหนดสีตามสถานะ (lines 159-193)
  - `derivePp30Status()`: Derive PP30 status จาก timestamp fields (import จาก `src/utils/pp30StatusUtils.ts`)

### Utilities
- `src/utils/pp30StatusUtils.ts` - Utility สำหรับ derive PP30 status
  - `derivePp30Status()`: Derive สถานะจาก timestamp fields

### Database Schema
- `Documentation/Database/schema.md` - Schema ของตาราง `monthly_tax_data`
  - `pnd_status` (VARCHAR(100)) - สถานะ ภ.ง.ด.
  - `pp30_form` (VARCHAR(100)) - สถานะ ภ.พ.30 (หลัง migration 028)
  - Timestamp fields สำหรับ derive สถานะ

---

## ⚠️ หมายเหตุสำคัญ

1. **PND Status**: ใช้ค่าจากฐานข้อมูลโดยตรง (`pnd_status`) ไม่มีการ derive
2. **PP30 Status**: มีการ derive จาก timestamp fields ถ้าไม่มี `pp30_form`
3. **ลำดับการ Derive PP30**: สำคัญ - ต้องเช็ค `vat_draft_completed_date` ก่อน `pp30_review_returned_date`
4. **Backward Compatibility**: รองรับสถานะเก่า (`edit`, `receipt`, `inquiry`, `review`) โดยแปลงเป็นสถานะใหม่อัตโนมัติ
5. **Null Handling**: ถ้าไม่มีสถานะ จะแสดง "สถานะยังไม่ดำเนินการ" (สีเทา)

---

**Last Updated**: 2026-02-03
