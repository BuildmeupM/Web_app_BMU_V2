# ⏰ Logic การตั้ง Timestamp สำหรับสถานะ ภ.พ.30

## 📋 สรุป

ระบบจะตั้ง timestamp ตามสถานะ `pp30_status` และหน้า (`sourcePage`) ที่ใช้งาน:

---

## 🎯 กฎการตั้ง Timestamp

### 1. `pp30_sent_to_customer_date`

**เงื่อนไข:**
- สถานะ: `'sent_to_customer'` (ส่งลูกค้าแล้ว)
- หน้า: `'taxFiling'` (หน้ายื่นภาษี)

**การทำงาน:**
- เมื่อผู้ใช้เลือกสถานะ "ส่งลูกค้าแล้ว" ในหน้ายื่นภาษี
- ระบบจะตั้ง `pp30_sent_to_customer_date = วันที่ปัจจุบัน` (ถ้ายังไม่มีค่า)

**ตัวอย่าง:**
```javascript
// Frontend ส่ง:
{
  pp30_status: 'sent_to_customer',
  sourcePage: 'taxFiling'
}

// Backend ตั้ง:
pp30_sent_to_customer_date = '2026-02-03 14:54:14'
pp30_form = 'sent_to_customer'
```

---

### 2. `pp30_review_returned_date`

**เงื่อนไข:**
- สถานะเดิม: `'pending_review'` (รอตรวจ) หรือ `'pending_recheck'` (รอตรวจอีกครั้ง)
- สถานะใหม่: สถานะอะไรก็ได้ที่ไม่ใช่ `'pending_review'` หรือ `'pending_recheck'`
- หน้า: `'taxInspection'` (หน้าตรวจภาษี)

**การทำงาน:**
- เมื่อผู้ใช้เปลี่ยนสถานะจาก "รอตรวจ" หรือ "รอตรวจอีกครั้ง" เป็นสถานะอื่นในหน้าตรวจภาษี
- ระบบจะตั้ง `pp30_review_returned_date = วันที่ปัจจุบัน` (ถ้ายังไม่มีค่า)
- **หมายเหตุ:** ไม่ใช่เมื่อเลือกสถานะ "รอตรวจ" หรือ "รอตรวจอีกครั้ง" แต่เป็นเมื่อ**เปลี่ยนจาก**สถานะเหล่านี้**เป็น**สถานะอื่น

**ตัวอย่าง:**
```javascript
// สถานะเดิมในฐานข้อมูล: 'pending_review' (รอตรวจ)

// Frontend ส่ง:
{
  pp30_status: 'sent_to_customer', // เปลี่ยนเป็น "ส่งลูกค้าแล้ว"
  sourcePage: 'taxInspection'
}

// Backend เช็ค:
// - สถานะเดิม: 'pending_review' ✅
// - สถานะใหม่: 'sent_to_customer' (ไม่ใช่ pending) ✅
// - sourcePage: 'taxInspection' ✅
// → ตั้ง pp30_review_returned_date

// Backend ตั้ง:
pp30_review_returned_date = '2026-02-03 15:42:13' ✅
pp30_form = 'sent_to_customer'
```

---

### 3. `vat_draft_completed_date`

**เงื่อนไข:**
- สถานะ: `'draft_completed'` (ร่างแบบเสร็จ)
- หน้า: `'taxFiling'` (หน้ายื่นภาษี)

**การทำงาน:**
- เมื่อผู้ใช้เลือกสถานะ "ร่างแบบเสร็จ" ในหน้ายื่นภาษี
- ระบบจะตั้ง `vat_draft_completed_date = วันที่ปัจจุบัน` (ถ้ายังไม่มีค่า)
- ระบบจะ clear `pp30_sent_to_customer_date` (เพราะสถานะ "ร่างแบบเสร็จ" มาก่อน "ส่งลูกค้า")

**ตัวอย่าง:**
```javascript
// Frontend ส่ง:
{
  pp30_status: 'draft_completed',
  sourcePage: 'taxFiling'
}

// Backend ตั้ง:
vat_draft_completed_date = '2026-02-03 13:30:00'
pp30_sent_to_customer_date = null // Clear เพราะสถานะ "ร่างแบบเสร็จ" มาก่อน
pp30_form = 'draft_completed'
```

---

### 4. `pp30_sent_for_review_date`

**เงื่อนไข:**
- สถานะ: `'pending_review'` (รอตรวจ), `'pending_recheck'` (รอตรวจอีกครั้ง)
- หน้า: `'taxStatus'` (หน้าสถานะยื่นภาษี)

**การทำงาน:**
- เมื่อผู้ใช้เลือกสถานะ "รอตรวจ" หรือ "รอตรวจอีกครั้ง" ในหน้าสถานะยื่นภาษี
- ระบบจะตั้ง `pp30_sent_for_review_date = วันที่ปัจจุบัน` (ถ้ายังไม่มีค่า)

**ตัวอย่าง:**
```javascript
// Frontend ส่ง:
{
  pp30_status: 'pending_review',
  sourcePage: 'taxStatus'
}

// Backend ตั้ง:
pp30_sent_for_review_date = '2026-02-03 10:20:00'
pp30_form = 'pending_review'
```

---

## 🔄 Flow การทำงาน

### Frontend → Backend

```
1. ผู้ใช้เลือกสถานะในฟอร์ม (เช่น "ส่งลูกค้าแล้ว")
2. Frontend ส่ง:
   {
     pp30_status: 'sent_to_customer',
     pp30_form: 'sent_to_customer',
     sourcePage: 'taxFiling'
   }
3. Backend รับข้อมูลและตรวจสอบ:
   - ถ้า pp30_status === 'sent_to_customer' && sourcePage === 'taxFiling'
     → ตั้ง pp30_sent_to_customer_date = now()
   - เก็บ pp30_form = 'sent_to_customer'
4. Backend UPDATE ฐานข้อมูล
5. Backend ส่ง response กลับพร้อม pp30_status
```

---

## 📊 ตารางสรุป

| สถานะ | หน้า | Timestamp ที่ตั้ง | คอลัมน์ |
|-------|------|-------------------|---------|
| `sent_to_customer` | `taxFiling` | `pp30_sent_to_customer_date` | ✅ |
| `draft_completed` | `taxFiling` | `vat_draft_completed_date` | ✅ |
| เปลี่ยนจาก `pending_review`/`pending_recheck` เป็นสถานะอื่น | `taxInspection` | `pp30_review_returned_date` | ✅ |
| `pending_review` | `taxStatus` | `pp30_sent_for_review_date` | ✅ |
| `pending_recheck` | `taxStatus` | `pp30_sent_for_review_date` | ✅ |

---

## ⚠️ หมายเหตุสำคัญ

1. **ทุกสถานะจะถูกเก็บใน `pp30_form`**: ไม่ว่าจะตั้ง timestamp หรือไม่
2. **Timestamp จะตั้งเฉพาะเมื่อตรงเงื่อนไข**: ถ้า sourcePage ไม่ตรงกับเงื่อนไข จะไม่ตั้ง timestamp
3. **Timestamp จะตั้งเฉพาะเมื่อยังไม่มีค่า**: ถ้ามีค่าแล้ว จะไม่ทับ (ยกเว้น `pp30_sent_to_customer_date` เมื่อเลือก `draft_completed`)
4. **`pp30_form` เป็น Single Source of Truth**: สถานะจะถูกเก็บใน `pp30_form` เสมอ

---

## 🔍 วิธีตรวจสอบ

### 1. เช็คในฐานข้อมูล
```sql
SELECT 
  build,
  pp30_form AS 'สถานะ',
  pp30_sent_to_customer_date,
  pp30_review_returned_date,
  pp30_sent_for_review_date,
  vat_draft_completed_date
FROM monthly_tax_data
WHERE build = '018' AND tax_year = 2026 AND tax_month = 1;
```

### 2. เช็คใน Backend Logs
```javascript
// Backend จะ log:
console.log('✅ [Backend] Set pp30_sent_to_customer_date for "sent_to_customer" (taxFiling):', {
  computedPp30Form: 'sent_to_customer',
  computedPp30SentToCustomerDate: '2026-02-03 14:54:14',
  sourcePage: 'taxFiling'
})
```

---

## 📚 เอกสารที่เกี่ยวข้อง

- `Documentation/PP30_FORM_STATUS_MIGRATION.md` - Migration 028
- `Documentation/PP30_STATUS_DATA_SOURCE.md` - แหล่งข้อมูลสถานะ
- `backend/routes/monthly-tax-data.js` - Backend logic
- `src/components/TaxInspection/TaxInspectionForm.tsx` - Frontend form
