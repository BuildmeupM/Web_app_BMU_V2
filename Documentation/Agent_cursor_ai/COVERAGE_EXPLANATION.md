# 📊 คำอธิบาย Coverage Report

## Coverage Report คืออะไร?

Coverage Report แสดงเปอร์เซ็นต์ของโค้ดที่ถูกทดสอบ (Test Coverage) ซึ่งช่วยให้รู้ว่าโค้ดส่วนไหนยังไม่มี test

---

## 📈 ตัวชี้วัดใน Coverage Report

### 1. **% Stmts (Statement Coverage)**
- **ความหมาย**: เปอร์เซ็นต์ของ statements (คำสั่ง) ที่ถูกทดสอบ
- **ตัวอย่าง**: ถ้ามี 100 statements แต่ test รันผ่านแค่ 38 statements = 38%
- **เป้าหมาย**: 80%+

### 2. **% Branch (Branch Coverage)**
- **ความหมาย**: เปอร์เซ็นต์ของ branches (เงื่อนไข if/else, switch cases) ที่ถูกทดสอบ
- **ตัวอย่าง**: ถ้ามี 10 branches แต่ test รันผ่านแค่ 1-2 branches = 10-20%
- **เป้าหมาย**: 70%+

### 3. **% Funcs (Function Coverage)**
- **ความหมาย**: เปอร์เซ็นต์ของ functions ที่ถูกทดสอบ
- **ตัวอย่าง**: ถ้ามี 100 functions แต่ test รันผ่านแค่ 7 functions = 7%
- **เป้าหมาย**: 80%+

### 4. **% Lines (Line Coverage)**
- **ความหมาย**: เปอร์เซ็นต์ของบรรทัดโค้ดที่ถูกทดสอบ
- **ตัวอย่าง**: ถ้ามี 1000 บรรทัด แต่ test รันผ่านแค่ 4 บรรทัด = 0.4%
- **เป้าหมาย**: 80%+

### 5. **Uncovered Line #s**
- **ความหมาย**: บรรทัดที่ยังไม่มี test
- **ตัวอย่าง**: `1-53` = บรรทัดที่ 1-53 ยังไม่มี test

---

## 📊 การอ่านผลลัพธ์ของคุณ

### สรุปภาพรวม
```
All files: 0.38% Stmts | 14.51% Branch | 7.4% Funcs | 0.38% Lines
```

**หมายความว่า**:
- มีโค้ดทั้งหมดมาก แต่มี test เพียง 0.38% ของ statements
- มี branches ที่ถูกทดสอบ 14.51%
- มี functions ที่ถูกทดสอบ 7.4%
- มีบรรทัดโค้ดที่ถูกทดสอบ 0.38%

### ไฟล์ที่มี Coverage สูง

#### ✅ `ProtectedRoute.tsx` - 100% Coverage
```
ProtectedRoute.tsx | 100% Stmts | 100% Branch | 100% Funcs | 100% Lines
```
**หมายความว่า**: ไฟล์นี้มี test ครบถ้วนแล้ว ✅

#### ✅ `authService.ts` - 84.44% Coverage
```
authService.ts | 84.44% Stmts | 62.5% Branch | 100% Funcs | 84.44% Lines
Uncovered Line #s: 19-20, 31-33, 40-41
```
**หมายความว่า**:
- มี test ครอบคลุม 84.44% ของโค้ด
- บรรทัดที่ 19-20, 31-33, 40-41 ยังไม่มี test (น่าจะเป็น error handling paths)

### ไฟล์ที่ยังไม่มี Coverage (0%)

ไฟล์เหล่านี้ยังไม่มี test:
- `App.tsx` - 0%
- `main.tsx` - 0%
- `EmployeeDashboard.tsx` - 0%
- `EmployeeList.tsx` - 0%
- `Login.tsx` - 0%
- `Dashboard.tsx` - 0%
- และอื่นๆ อีกมาก

---

## 🎯 Coverage Goals ตาม AGENT.md

ตาม `AGENT.md` กำหนดเป้าหมาย:
- **Unit Tests**: 80%+ coverage
- **Integration Tests**: 60%+ coverage
- **Critical Paths**: 100% coverage

### Priority Areas (ควรมี Coverage สูงสุด)
1. **Authentication & Authorization** - 100% coverage ✅ (ProtectedRoute: 100%)
2. **API Endpoints** - 80%+ coverage ⚠️ (authService: 84.44% - ใกล้เป้าหมาย)
3. **Form Validation** - 90%+ coverage ❌ (ยังไม่มี test)
4. **Business Logic** - 80%+ coverage ❌ (ยังไม่มี test)
5. **UI Components** - 70%+ coverage ❌ (ยังไม่มี test)

---

## 📝 สิ่งที่ควรทำต่อไป

### 1. เพิ่ม Tests สำหรับไฟล์สำคัญ

#### Priority 1: Authentication & Security
- [ ] `Login.tsx` - หน้า Login (Critical Path)
- [ ] `authStore.ts` - State management สำหรับ auth
- [ ] `api.ts` - API interceptor และ error handling

#### Priority 2: Core Features
- [ ] `EmployeeManagement.tsx` - จัดการพนักงาน
- [ ] `LeaveManagement.tsx` - จัดการลา/WFH
- [ ] `Dashboard.tsx` - Dashboard หลัก

#### Priority 3: Services
- [ ] `employeeService.ts` - Employee API calls
- [ ] `leaveService.ts` - Leave/WFH API calls

#### Priority 4: Components
- [ ] `EmployeeForm.tsx` - Form สำหรับเพิ่ม/แก้ไขพนักงาน
- [ ] `LeaveRequestForm.tsx` - Form สำหรับขอลา
- [ ] `WFHRequestForm.tsx` - Form สำหรับขอ WFH

### 2. เพิ่ม Tests สำหรับ Error Cases

ใน `authService.ts` มี uncovered lines:
- `19-20`: Error handling ใน login
- `31-33`: Error handling ใน logout
- `40-41`: Error handling ใน getCurrentUser

ควรเพิ่ม tests สำหรับ error cases เหล่านี้

### 3. เพิ่ม Tests สำหรับ Edge Cases

- Empty data
- Invalid input
- Network errors
- Timeout errors

---

## 🔍 วิธีดู Coverage Report แบบละเอียด

### ดู HTML Report (แนะนำ)
```bash
npm run test:coverage
# เปิดไฟล์ coverage/index.html ใน browser
```

HTML Report จะแสดง:
- ✅ บรรทัดที่ถูกทดสอบ (สีเขียว)
- ❌ บรรทัดที่ยังไม่มี test (สีแดง)
- ⚠️ บรรทัดที่ถูก skip (สีเหลือง)

### ดูใน Terminal
```bash
npm run test:coverage
# จะแสดงตารางเหมือนที่เห็น
```

---

## 📌 สรุป

### สถานะปัจจุบัน
- ✅ **Tests ผ่านทั้งหมด**: 7 tests passed
- ⚠️ **Coverage ต่ำ**: 0.38% (เพราะยังมี test น้อย)
- ✅ **ไฟล์ที่มี test**: ProtectedRoute (100%), authService (84.44%)

### สิ่งที่ต้องทำ
1. ✅ **ระบบ Testing พร้อมใช้งานแล้ว**
2. ⏳ **ต้องเพิ่ม Tests เพิ่มเติม** เพื่อเพิ่ม Coverage
3. 📝 **บันทึกผลใน TEST_LOG.md** ทุกครั้งที่รัน tests

### คำแนะนำ
- เริ่มจากไฟล์สำคัญก่อน (Authentication, Core Features)
- เพิ่ม tests ทีละส่วน
- ตั้งเป้า Coverage 80%+ สำหรับไฟล์สำคัญ
- ใช้ `npm run test:coverage` เพื่อดู progress

---

**อัปเดตล่าสุด**: 2026-01-30
