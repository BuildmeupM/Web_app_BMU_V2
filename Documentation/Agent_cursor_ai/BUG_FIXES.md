# 🐛 Bug Fixes Log - BMU Work Management System

> **📑 คู่มือการใช้งาน**: ไฟล์นี้มีขนาดใหญ่ ดูวิธีค้นหาและโครงสร้างได้ที่ [BUG_FIXES_INDEX.md](./BUG_FIXES_INDEX.md)

## 📋 วัตถุประสงค์

ไฟล์นี้ใช้สำหรับบันทึกการแก้ไขบัคทั้งหมดในระบบ เพื่อให้ Cursor AI และ Developer สามารถ:
- ✅ ติดตามประวัติการแก้ไขบัคทั้งหมด
- ✅ เข้าใจสาเหตุของปัญหาและวิธีแก้ไข
- ✅ หลีกเลี่ยงการแก้ไขบัคเดิมซ้ำ
- ✅ เรียนรู้จากปัญหาที่เกิดขึ้น
- ✅ ติดตามสถิติ Bug Fixes

---

## 📝 โครงสร้างการบันทึก

แต่ละ Bug Fix Entry จะมีข้อมูลดังนี้:
- **Bug ID**: รหัสบัค (BUG-XXX)
- **วันที่พบ**: วันที่ที่พบบัค
- **วันที่แก้ไข**: วันที่ที่แก้ไขบัคเสร็จ
- **ส่วน/หน้าที่เกี่ยวข้อง**: หน้า/Component ที่เกิดบัค
- **ระดับความรุนแรง**: Critical / High / Medium / Low
- **คำอธิบายปัญหา**: รายละเอียดของปัญหา
- **สาเหตุ**: สาเหตุที่แท้จริงของปัญหา
- **วิธีแก้ไข**: วิธีที่ใช้แก้ไขปัญหา
- **ไฟล์ที่แก้ไข**: ไฟล์ที่แก้ไข
- **ผู้แก้ไข**: ผู้ที่แก้ไขบัค (ถ้ามี)
- **หมายเหตุ**: ข้อมูลเพิ่มเติม

---

## 🔍 การค้นหา Bug Fixes

### ตามส่วน/หน้า
- [Login](#login)
- [Dashboard](#dashboard)
- [ข้อมูลพนักงาน](#ข้อมูลพนักงาน)
- [ลางาน/WFH](#ลางานwfh)
- [ขอเบิกเงินเดือน](#ขอเบิกเงินเดือน)
- [ข้อมูลเข้าออฟฟิศ](#ข้อมูลเข้าออฟฟิศ)
- [คัดแยกเอกสาร](#คัดแยกเอกสาร)
- [คีย์เอกสาร](#คีย์เอกสาร)
- [ตรวจภาษี](#ตรวจภาษี)
- [สถานะยื่นภาษี](#สถานะยื่นภาษี)
- [ยื่นภาษี](#ยื่นภาษี)
- [Layout/Components](#layoutcomponents)
- [Authentication](#authentication)
- [Services/API](#servicesapi)
- [Utils/Helpers](#utilshelpers)
- [Store/State Management](#storestate-management)

---

## BUG-168: ระบบไม่แสดงข้อมูลเมื่อ navigate ไปหน้าอื่นๆ (URL เปลี่ยนแต่หน้าเว็บไม่เปลี่ยน)

**วันที่พบ**: 2026-02-06  
**วันที่แก้ไข**: 2026-02-06  
**ส่วน/หน้าที่เกี่ยวข้อง**: Authentication (`src/store/authStore.ts`), App (`src/App.tsx`), ProtectedRoute (`src/components/Auth/ProtectedRoute.tsx`), WorkAssignment (`src/pages/WorkAssignment.tsx`), TaxInspection (`src/pages/TaxInspection.tsx`), TaxFiling (`src/pages/TaxFiling.tsx` และ components)  
**ระดับความรุนแรง**: Critical

### ปัญหา
- เมื่อผู้ใช้กดเข้ามายังหน้าตรวจภาษี (tax-inspection) และยื่นภาษี (tax-filing) แล้วทำการกดไปยังหน้าอื่นๆ
- URL ในเบราว์เซอร์เปลี่ยนปกติ แต่หน้าเว็บไม่แสดงข้อมูล
- ระบบแสดง loading spinner ตลอดเวลาหรือแสดง "ไม่พบข้อมูล"
- Component ไม่ render หรือ query ไม่ทำงาน ทำให้ผู้ใช้ไม่สามารถใช้งานได้

### สาเหตุ
1. **`_hasHydrated` ไม่ถูก set เป็น `true` เมื่อ navigate**:
   - `onRehydrateStorage` callback ใน zustand persist อาจไม่ทำงานหรือทำงานช้า
   - เมื่อ navigate ไปหน้าอื่น hydration อาจไม่เสร็จทันที
   - ทำให้ `_hasHydrated` ค้างที่ `false` ตลอดเวลา

2. **Race condition ระหว่าง hydration กับ navigation**:
   - เมื่อ navigate ไปหน้าอื่น React component re-render ก่อนที่ hydration จะเสร็จ
   - `App.tsx` และ `ProtectedRoute` ตรวจสอบ `_hasHydrated` และแสดง loading ตลอดเวลา
   - Component ไม่ render เพราะถูก block ที่ loading state

3. **`onRehydrateStorage` callback ไม่ handle error**:
   - ถ้า hydration error (เช่น sessionStorage ไม่สามารถ parse ได้) callback ไม่ทำงาน
   - ทำให้ `_hasHydrated` ไม่ถูก set เป็น `true`
   - Component ไม่ render

4. **`_hasHydrated` ถูก persist ใน storage**:
   - ถ้า `_hasHydrated` ถูก persist และมีค่าเป็น `false` ใน storage
   - เมื่อ mount ใหม่จะได้ค่า `false` จาก storage แทนที่จะถูก reset
   - ทำให้ component ไม่ render

5. **React Query `enabled` condition ใช้ค่าที่ยังไม่พร้อม**:
   - `enabled: !!employeeId` แต่ `employeeId` เป็น `null` เพราะ user ยังไม่ hydrated
   - Query ไม่ทำงานเพราะ `enabled` เป็น `false`
   - Component render แต่ไม่มีข้อมูลเพราะ query ไม่ทำงาน

6. **`refetchOnMount: false` ทำให้ไม่ refetch เมื่อ navigate**:
   - เมื่อ navigate ไปหน้าอื่น component ไม่ refetch
   - ถ้าไม่มี cache หรือ cache เก่า component จะแสดง "ไม่พบข้อมูล"
   - ผู้ใช้ต้อง refresh หน้าเว็บเองเพื่อให้ได้ข้อมูลใหม่

### วิธีแก้ไข
1. **เพิ่ม fallback hydration ใน App.tsx**:
   - เพิ่ม `useEffect` เพื่อตรวจสอบและ set `_hasHydrated` เป็น `true` หลังจาก 100ms
   - ตรวจสอบว่า sessionStorage มีข้อมูลหรือไม่
   - ถ้ามีข้อมูลแต่ `_hasHydrated` ยังเป็น `false` แสดงว่า callback ไม่ทำงาน ให้ set เป็น `true`
   - ถ้าไม่มีข้อมูลก็ set เป็น `true` เพื่อให้ component render ได้ (จะ redirect ไป login)

2. **แก้ไข `onRehydrateStorage` ใน authStore.ts**:
   - เพิ่ม error handling เพื่อ handle กรณีที่ hydration error
   - ถ้า error หรือ state เป็น null ให้ set `_hasHydrated` เป็น `true` เพื่อให้ component render ได้

3. **ไม่ persist `_hasHydrated`**:
   - ใช้ `partialize` เพื่อไม่ persist `_hasHydrated`
   - ทำให้ `_hasHydrated` จะถูก reset เป็น `false` ทุกครั้งที่ mount
   - และจะถูก set เป็น `true` เมื่อ hydration เสร็จ (หรือ fallback)

4. **แก้ไข React Query `enabled` condition**:
   - เพิ่ม `_hasHydrated` check ใน `enabled` condition: `enabled: !!employeeId && _hasHydrated`
   - รอ hydration เสร็จก่อน enable query เพื่อป้องกัน race condition
   - ใช้กับทุก useQuery ที่ใช้ `employeeId` หรือ `user` data

5. **แก้ไข `refetchOnMount` ใน React Query**:
   - เปลี่ยน `refetchOnMount: false` เป็น `refetchOnMount: true` ในหน้าที่ยังมีปัญหา
   - เพื่อให้ refetch เมื่อ navigate ไปหน้าอื่น
   - ใช้ `keepPreviousData: true` เพื่อแสดงข้อมูลเก่าก่อน refetch

### ไฟล์ที่แก้ไข
- `src/App.tsx`:
  - เพิ่ม `useEffect` เพื่อ fallback hydration
  - ตรวจสอบ sessionStorage และ set `_hasHydrated` เป็น `true` ถ้า callback ไม่ทำงาน
  - เพิ่ม debug logging เพื่อตรวจสอบ hydration state
- `src/store/authStore.ts`:
  - แก้ไข `onRehydrateStorage` เพื่อ handle error
  - เพิ่ม `partialize` เพื่อไม่ persist `_hasHydrated`
  - เพิ่ม debug logging เพื่อตรวจสอบ hydration
- `src/components/Auth/ProtectedRoute.tsx`:
  - เพิ่ม fallback hydration mechanism (150ms timeout)
  - เพิ่ม debug logging เพื่อตรวจสอบ hydration state
- `src/pages/WorkAssignment.tsx`:
  - เพิ่ม `_hasHydrated` check ใน `enabled` condition ของ useQuery
  - เปลี่ยน `refetchOnMount: false` เป็น `refetchOnMount: true`
  - เพิ่ม debug logging เพื่อตรวจสอบ component render และ query state
- `src/pages/TaxInspection.tsx`:
  - เพิ่ม `_hasHydrated` check ใน `enabled` condition ของ useQuery
  - เพิ่ม `refetchOnMount: true` เพื่อให้ refetch เมื่อ navigate
- `src/components/TaxInspection/SummaryCard.tsx`:
  - เพิ่ม `_hasHydrated` check ใน `enabled` condition ของ useQuery
  - เพิ่ม `refetchOnMount: true` เพื่อให้ refetch เมื่อ navigate
- `src/components/TaxFiling/TaxFilingTable.tsx`:
  - เพิ่ม `_hasHydrated` check ใน `enabled` condition ของ useQuery
  - เปลี่ยน `refetchOnMount: false` เป็น `refetchOnMount: true`
- `src/components/TaxFiling/SummaryCard.tsx`:
  - เพิ่ม `_hasHydrated` check ใน `enabled` condition ของ useQuery
  - เปลี่ยน `refetchOnMount: false` เป็น `refetchOnMount: true`

### ผลลัพธ์
- ✅ เมื่อ navigate ไปหน้าอื่นๆ ระบบจะแสดงข้อมูลได้ปกติ
- ✅ Component จะ render ได้แม้ว่า hydration callback จะไม่ทำงาน
- ✅ Fallback mechanism จะทำให้ component render ได้ภายใน 100ms
- ✅ ปัญหา "URL เปลี่ยนแต่หน้าเว็บไม่เปลี่ยน" จะหายไป
- ✅ หน้า TaxInspection และ TaxFiling แสดงข้อมูลได้ปกติเมื่อ navigate
- ✅ Query จะรอ hydration เสร็จก่อน enable เพื่อป้องกัน race condition

### ข้อผิดพลาดที่อาจทำให้เกิดปัญหานี้อีก

#### 1. **Race Condition ระหว่าง Async Operations**
- **ปัญหา**: ถ้ามี async operations อื่นๆ ที่ทำงานพร้อมกับ hydration
- **ตัวอย่าง**: API calls, WebSocket connections, หรือ state updates อื่นๆ
- **วิธีป้องกัน**: 
  - ใช้ `useEffect` เพื่อรอ async operations เสร็จก่อน
  - ใช้ loading states เพื่อแสดงผลระหว่างรอ
  - ใช้ `Promise.all()` หรือ `Promise.race()` เพื่อจัดการ async operations หลายตัว

#### 2. **State Management ที่ไม่ Synchronize**
- **ปัญหา**: ถ้ามี state management อื่นๆ ที่ไม่ sync กับ hydration
- **ตัวอย่าง**: React Query cache, Context API, หรือ global state อื่นๆ
- **วิธีป้องกัน**:
  - ตรวจสอบว่า state management ทั้งหมด sync กัน
  - ใช้ single source of truth สำหรับ auth state
  - ใช้ `useEffect` เพื่อ sync state เมื่อ hydration เสร็จ

#### 3. **Browser Storage Issues**
- **ปัญหา**: ถ้า sessionStorage หรือ localStorage มีปัญหา
- **ตัวอย่าง**: Storage quota exceeded, storage disabled, หรือ corrupted data
- **วิธีป้องกัน**:
  - เพิ่ม error handling สำหรับ storage operations
  - ใช้ try-catch เมื่ออ่าน/เขียน storage
  - ตรวจสอบว่า storage available ก่อนใช้งาน
  - มี fallback mechanism เมื่อ storage ไม่สามารถใช้งานได้

#### 4. **Component Re-render Issues**
- **ปัญหา**: ถ้า component re-render บ่อยเกินไปหรือไม่ re-render เมื่อควร
- **ตัวอย่าง**: `useEffect` dependencies ไม่ถูกต้อง, state updates ที่ไม่จำเป็น
- **วิธีป้องกัน**:
  - ตรวจสอบ `useEffect` dependencies ให้ถูกต้อง
  - ใช้ `useMemo` และ `useCallback` เพื่อลด re-renders
  - ใช้ React DevTools เพื่อตรวจสอบ re-renders

#### 5. **Router Navigation Issues**
- **ปัญหา**: ถ้า React Router มีปัญหาในการ navigate
- **ตัวอย่าง**: Route configuration ไม่ถูกต้อง, navigation guards ที่ block navigation
- **วิธีป้องกัน**:
  - ตรวจสอบ route configuration ให้ถูกต้อง
  - ตรวจสอบ navigation guards (เช่น ProtectedRoute) ให้ทำงานถูกต้อง
  - ใช้ `useLocation` และ `useNavigate` อย่างถูกต้อง

#### 6. **Zustand Persist Configuration Issues**
- **ปัญหา**: ถ้า zustand persist configuration ไม่ถูกต้อง
- **ตัวอย่าง**: `onRehydrateStorage` callback ไม่ทำงาน, `partialize` ไม่ถูกต้อง
- **วิธีป้องกัน**:
  - ตรวจสอบ persist configuration ให้ถูกต้อง
  - เพิ่ม error handling ใน `onRehydrateStorage`
  - ใช้ `partialize` เพื่อไม่ persist state ที่ไม่จำเป็น
  - เพิ่ม fallback mechanism เมื่อ callback ไม่ทำงาน

#### 7. **React Query `enabled` Condition Issues**
- **ปัญหา**: ถ้า `enabled` condition ใน useQuery ใช้ค่าที่ยังไม่พร้อม (เช่น `user?.employee_id` ก่อน hydration)
- **ตัวอย่าง**: `enabled: !!employeeId` แต่ `employeeId` เป็น `null` เพราะ user ยังไม่ hydrated
- **วิธีป้องกัน**:
  - เพิ่ม `_hasHydrated` check ใน `enabled` condition: `enabled: !!employeeId && _hasHydrated`
  - รอ hydration เสร็จก่อน enable query เพื่อป้องกัน race condition
  - ใช้ `refetchOnMount: true` เพื่อให้ refetch เมื่อ navigate ไปหน้าอื่น (ถ้าต้องการข้อมูลล่าสุด)

#### 8. **React Query `refetchOnMount: false` Issues**
- **ปัญหา**: ถ้า `refetchOnMount: false` และไม่มี cache หรือ cache เก่า
- **ตัวอย่าง**: เมื่อ navigate ไปหน้าอื่น component ไม่ refetch และใช้ cache เก่าหรือไม่มีข้อมูล
- **วิธีป้องกัน**:
  - ใช้ `refetchOnMount: true` เมื่อต้องการข้อมูลล่าสุดเมื่อ navigate
  - ใช้ `keepPreviousData: true` เพื่อแสดงข้อมูลเก่าก่อน refetch
  - ตรวจสอบว่า cache มีข้อมูลหรือไม่ก่อนใช้ `refetchOnMount: false`

### หมายเหตุ
- ⚠️ **สำคัญ**: การเพิ่ม fallback mechanism จะทำให้ component render ได้แม้ว่า hydration callback จะไม่ทำงาน
- ⚠️ **สำคัญ**: การไม่ persist `_hasHydrated` จะทำให้ flag ถูก reset ทุกครั้งที่ mount
- ⚠️ **สำคัญ**: Fallback timeout (100ms) ควรจะเพียงพอสำหรับ hydration แต่ไม่นานเกินไปที่จะทำให้ผู้ใช้รอนาน
- 💡 **Best Practice**: ควรมี fallback mechanism เสมอเมื่อใช้ async operations หรือ state persistence
- 💡 **Best Practice**: ควร handle errors ใน callbacks และ async operations เสมอ
- 💡 **Best Practice**: ควรไม่ persist flags หรือ temporary state ที่ไม่จำเป็น

---

## 📊 Bug Fixes Statistics

### สรุป Bug Fixes ตามส่วน/หน้า

| ส่วน/หน้า | Critical | High | Medium | Low | รวม |
|-----------|----------|------|--------|-----|-----|
| Authentication | 1 | 0 | 0 | 0 | 1 |
| Layout/Components | 1 | 0 | 0 | 0 | 1 |
| **รวม** | **2** | **0** | **0** | **0** | **2** |

---

## 📅 Bug Fixes Logs

### Authentication

#### BUG-168: ระบบไม่แสดงข้อมูลเมื่อ navigate ไปหน้าอื่นๆ (URL เปลี่ยนแต่หน้าเว็บไม่เปลี่ยน)

ดูรายละเอียดด้านบน

---

## 🔄 Recent Updates

### 2026-02-06

#### BUG-168: ระบบไม่แสดงข้อมูลเมื่อ navigate ไปหน้าอื่นๆ
- แก้ไข hydration race condition ใน authStore
- เพิ่ม fallback mechanism ใน App.tsx
- แก้ไข error handling ใน onRehydrateStorage
- ไม่ persist _hasHydrated เพื่อป้องกันปัญหาที่ state ถูก persist แล้วแต่ callback ไม่ทำงาน

---

## 📌 Best Practices

### เมื่อพบบัค
1. ✅ บันทึกบัคใน `BUG_FIXES.md` ทันที
2. ✅ อธิบายปัญหา สาเหตุ และวิธีแก้ไขให้ชัดเจน
3. ✅ ระบุไฟล์ที่แก้ไข
4. ✅ อัปเดตสถิติ Bug Fixes

### เมื่อแก้ไขบัค
1. ✅ แก้ไขบัคตามวิธีที่ระบุ
2. ✅ ทดสอบว่าแก้ไขสำเร็จ
3. ✅ อัปเดต `BUG_FIXES.md` พร้อมผลลัพธ์
4. ✅ อธิบายข้อผิดพลาดที่อาจทำให้เกิดปัญหานี้อีก

### การจัดระดับความรุนแรง
- **Critical**: ปัญหาที่ทำให้ระบบไม่สามารถใช้งานได้
- **High**: ปัญหาที่มีผลกระทบต่อการใช้งานอย่างมาก
- **Medium**: ปัญหาที่มีผลกระทบต่อการใช้งานปานกลาง
- **Low**: ปัญหาที่มีผลกระทบต่อการใช้งานน้อย

---

**Last Updated**: 2026-02-06  
**Version**: 1.3.0
