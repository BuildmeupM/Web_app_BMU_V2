 # 🤖 Agent Cursor AI - Guidelines

## 📋 วัตถุประสงค์

โฟลเดอร์นี้เก็บข้อมูล **AGENT.md** สำหรับกำหนดทิศทางของระบบ ให้ Cursor AI Agent ทำงานได้ถูกต้องและสอดคล้องกัน

## 📄 ไฟล์หลัก

### AGENT.md
ไฟล์หลักที่กำหนด:
- ✅ Tech Stack และ Dependencies
- ✅ Design Guidelines (Colors, Typography, UI Components)
- ✅ Roles และ Permissions
- ✅ Features และ Pages (11 หน้า)
- ✅ Security Guidelines
- ✅ Data Guidelines
- ✅ Testing Guidelines
- ✅ Deployment Guidelines
- ✅ Best Practices
- ✅ Workflow

### BUG_FIXES.md
ไฟล์บันทึกการแก้ไขข้อผิดพลาด (Bug Fixes):
- ✅ บันทึกประวัติการแก้ไขบัคทั้งหมด
- ✅ อธิบายสาเหตุของปัญหา
- ✅ วิธีแก้ไขที่ใช้
- ✅ ไฟล์ที่แก้ไข
- ✅ แยกตามส่วน/หน้าที่เกี่ยวข้อง
- ✅ สถิติ Bug Fixes

### TEST_LOG.md
ไฟล์บันทึกการทดสอบ Unit Test:
- ✅ บันทึกการทดสอบฟังก์ชันแต่ละส่วน
- ✅ ติดตามว่าทดสอบอะไรไปแล้วบ้าง
- ✅ บันทึกบัคที่พบระหว่างการทดสอบ
- ✅ ติดตาม Test Coverage ของแต่ละหน้า/Component
- ✅ แยกตามส่วน/หน้าที่เกี่ยวข้อง
- ✅ สถิติการทดสอบและ Coverage

## 🚨 สำคัญ: สำหรับ Cursor AI Agent

**ต้องอ่าน AGENT.md ก่อนเริ่มทำงานทุกครั้ง!**

AGENT.md เป็นไฟล์ที่สำคัญที่สุด เพราะ:
- กำหนดทิศทางของโปรเจกต์
- กำหนดมาตรฐานการพัฒนา
- กำหนด Design System
- กำหนด Security Requirements
- กำหนด Best Practices

## 📖 วิธีใช้งาน

1. **ก่อนเริ่มทำงาน**: 
   - อ่าน `AGENT.md` ทั้งหมด
   - **อ่าน `Documentation/API/API_INDEX.md` ก่อนสร้าง Feature ใหม่** เพื่อตรวจสอบว่า API ที่ต้องการมีอยู่แล้วหรือไม่
   - ตรวจสอบ `BUG_FIXES.md` เพื่อดูประวัติการแก้ไขบัคที่เกี่ยวข้อง
   - ตรวจสอบ `TEST_LOG.md` เพื่อดูว่ามีการทดสอบส่วนที่เกี่ยวข้องหรือไม่
2. **ขณะทำงาน**: อ้างอิง `AGENT.md` สำหรับ:
   - Design Guidelines
   - Code Standards
   - Security Requirements
   - Best Practices
3. **เมื่อพบปัญหา**: 
   - ตรวจสอบ `BUG_FIXES.md` ว่ามีปัญหาเดิมหรือไม่
   - ตรวจสอบ `TEST_LOG.md` ว่ามีบัคที่พบจากการทดสอบหรือไม่
   - ถ้าแก้ไขบัคใหม่ ให้บันทึกใน `BUG_FIXES.md` ทันที
4. **เมื่อเขียนหรือรัน Test**: 
   - บันทึกผลการทดสอบใน `TEST_LOG.md` ทันที
   - ถ้าพบบัคระหว่างการทดสอบ ให้บันทึกใน `BUG_FIXES.md` ด้วย
   - อัปเดต Test Coverage Summary
5. **เมื่อแก้ไข UI หรือ Features**: 
   - อัปเดต `Documentation/Guidebook_for_page/[PageName].md` ทันทีหลังจากแก้ไขเสร็จ
   - อัปเดต Layout Structure, Color Scheme, Components Used (ถ้ามีการเปลี่ยนแปลง)
   - อัปเดต Data Structure, API Endpoints (ถ้ามีการเปลี่ยนแปลง)
   - เพิ่มส่วน "Recent Updates" พร้อมวันที่และรายละเอียดการเปลี่ยนแปลง
   - อัปเดต User Flow (ถ้ามีการเปลี่ยนแปลง)
   - ตรวจสอบให้แน่ใจว่า Documentation สอดคล้องกับโค้ดจริง
6. **ก่อนสร้าง Feature ใหม่**: 
   - **บังคับ**: อ่าน `Documentation/API/API_INDEX.md` ก่อนทุกครั้ง
   - ตรวจสอบว่า API ที่ต้องการมีอยู่แล้วหรือไม่
   - ถ้ามี API อยู่แล้ว ให้ใช้ API ที่มีอยู่แทนการสร้างใหม่
   - ถ้ายังไม่มี ให้สร้างใหม่และอัพเดท `API_INDEX.md` ทันที
7. **เมื่อมีคำถาม**: ตรวจสอบ `AGENT.md`, `BUG_FIXES.md`, `TEST_LOG.md`, และ `Documentation/API/API_INDEX.md` ก่อน

## 🔄 การอัปเดต

เมื่อมีการเปลี่ยนแปลง Guidelines หรือ Standards:
- ✅ อัปเดต `AGENT.md` ทันที
- ✅ บันทึกวันที่อัปเดต
- ✅ อธิบายการเปลี่ยนแปลง

เมื่อแก้ไขบัคหรือปัญหา:
- ✅ บันทึกใน `BUG_FIXES.md` ทันทีหลังจากแก้ไขเสร็จ
- ✅ อธิบายปัญหา สาเหตุ และวิธีแก้ไขให้ชัดเจน
- ✅ อัปเดตสถิติ Bug Fixes

เมื่อเขียนหรือรัน Unit Test:
- ✅ บันทึกผลการทดสอบใน `TEST_LOG.md` ทันทีหลังจากรัน Test เสร็จ
- ✅ อธิบาย Test Case และผลลัพธ์ให้ชัดเจน
- ✅ ถ้าพบบัคระหว่างการทดสอบ ให้บันทึกใน `BUG_FIXES.md` ด้วย
- ✅ อัปเดต Test Coverage Summary ใน `TEST_LOG.md`

เมื่อแก้ไข UI หรือ Features:
- ✅ อัปเดต `Documentation/Guidebook_for_page/[PageName].md` ทันทีหลังจากแก้ไขเสร็จ
- ✅ อัปเดต Layout Structure, Color Scheme, Components Used (ถ้ามีการเปลี่ยนแปลง)
- ✅ อัปเดต Data Structure, API Endpoints (ถ้ามีการเปลี่ยนแปลง)
- ✅ เพิ่มส่วน "Recent Updates" พร้อมวันที่และรายละเอียดการเปลี่ยนแปลง
- ✅ อัปเดต User Flow (ถ้ามีการเปลี่ยนแปลง)
- ✅ ตรวจสอบให้แน่ใจว่า Documentation สอดคล้องกับโค้ดจริง

เมื่อเพิ่ม API ใหม่:
- ✅ อัพเดท `Documentation/API/API_INDEX.md` ทันทีหลังจากสร้าง API เสร็จ
- ✅ เพิ่ม API endpoint ในตารางที่เกี่ยวข้อง
- ✅ อัพเดท "Total Endpoints" count
- ✅ เพิ่มใน "Quick Search Guide" (ถ้าจำเป็น)
- ✅ สร้าง Documentation file ใหม่ (`Documentation/API/[API_NAME]_API_DESIGN.md` และ `Documentation/API/[API_NAME]_API_IMPLEMENTATION.md`)

---

**ไฟล์นี้เป็นส่วนหนึ่งของ Documentation System**
