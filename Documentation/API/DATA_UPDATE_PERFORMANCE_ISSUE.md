# 🐛 Data Update Performance Issue - การอัพเดทข้อมูลช้า

## 📋 Overview

เอกสารนี้อธิบายปัญหาและวิธีแก้ไขสำหรับการอัพเดทข้อมูลจาก backend ไปยัง frontend ที่ทำงานช้าและไม่ทันที

**วันที่พบ**: 2026-02-03  
**สถานะ**: 🔴 Critical - กำลังแก้ไข

---

## 🔍 ปัญหาที่พบ

### 1. การอัพเดทข้อมูลช้า

**อาการ**:
- หลังจากบันทึกข้อมูลสำเร็จ หน้าเว็บไม่ถูกอัพเดททันที
- ต้องรอหลายวินาทีหรือต้องรีเฟรชหน้าเว็บเอง
- ข้อมูลในตารางไม่ตรงกับฐานข้อมูล

**สาเหตุ**:
1. **`staleTime` ยาวเกินไป**: `TaxInspectionForm` มี `staleTime: 30 * 1000` (30 วินาที) ทำให้ข้อมูลไม่ถูก refetch ทันที
2. **`refetchOnMount: false`**: ทำให้ข้อมูลไม่ถูก refetch เมื่อ component mount
3. **`refetchQueries` ช้า**: การใช้ `refetchQueries` ต้องรอ network request ซึ่งช้า
4. **Cache update ไม่ครบถ้วน**: `setQueryData` อาจไม่ match query keys ที่ซับซ้อน

---

## ✅ วิธีแก้ไข

### 1. ปรับ `staleTime` และ `refetchOnMount` ใน TaxInspectionForm

**ปัญหา**: `staleTime: 30 * 1000` และ `refetchOnMount: false` ทำให้ข้อมูลไม่ถูก refetch ทันที

**วิธีแก้**:
```typescript
// ❌ เดิม
staleTime: 30 * 1000, // 30 วินาที
refetchOnMount: false, // ไม่ refetch เมื่อ mount

// ✅ แก้ไข
staleTime: 0, // ไม่ cache เพื่อให้ refetch ทันทีเมื่อ invalidate
refetchOnMount: true, // Refetch เมื่อ mount เพื่อให้ได้ข้อมูลล่าสุด
```

**ไฟล์**: `src/components/TaxInspection/TaxInspectionForm.tsx` (บรรทัด 57-76)

---

### 2. ปรับปรุง Cache Update Strategy

**ปัญหา**: การใช้ `refetchQueries` ต้องรอ network request ซึ่งช้า

**วิธีแก้**: ใช้ `setQueryData` เพื่ออัพเดท cache ทันที (ไม่ต้องรอ network)

**ไฟล์**: `src/components/TaxInspection/TaxInspectionForm.tsx` (บรรทัด 744-812)

**Logic**:
1. อัพเดท cache ทันทีด้วย `setQueryData` (ไม่ต้องรอ network)
2. Invalidate queries เพื่อบังคับให้ refetch ใน background
3. Refetch queries ใน background (ไม่บล็อก UI)

---

### 3. ปรับปรุง `invalidateQueries` และ `refetchQueries`

**ปัญหา**: การใช้ `invalidateQueries` และ `refetchQueries` อาจช้าเพราะต้องรอ network request

**วิธีแก้**: ใช้ `refetchType: 'active'` เพื่อ refetch เฉพาะ queries ที่กำลัง active

**ไฟล์**: `src/components/TaxInspection/TaxInspectionForm.tsx` (บรรทัด 815-843)

**Logic**:
```typescript
// ⚠️ สำคัญ: Invalidate cache เพื่อบังคับให้ refetch ทันที (ไม่ต้องรอ staleTime)
queryClient.invalidateQueries({ queryKey: listKey, exact: false }, { refetchType: 'active' })

// Refetch ทันที (ไม่ต้องรอ) - จะ refetch เฉพาะ queries ที่ match และ active
queryClient.refetchQueries({ queryKey: listKey, exact: false, type: 'active' })
```

---

### 4. ปรับปรุง Table Components

**ปัญหา**: Tables อาจไม่ถูกอัพเดททันทีเพราะ `staleTime` หรือ `refetchOnMount` ไม่เหมาะสม

**วิธีแก้**: ตั้งค่า `staleTime: 0` และ `refetchOnMount: true` สำหรับ tables

**ไฟล์**:
- `src/components/TaxStatus/TaxStatusTable.tsx` (บรรทัด 256-273) ✅ **ทำแล้ว**
- `src/components/TaxFiling/TaxFilingTable.tsx` (บรรทัด 226-250) ✅ **ทำแล้ว**

---

## 📊 Performance Improvements

### Before Fix
- **Cache Update**: ต้องรอ `refetchQueries` (ช้า - ต้องรอ network)
- **Data Refresh**: ต้องรอ `staleTime` (30 วินาที)
- **UI Update**: ไม่ทันที (ต้องรอ refetch)

### After Fix
- **Cache Update**: ทันทีด้วย `setQueryData` (ไม่ต้องรอ network)
- **Data Refresh**: ทันทีด้วย `staleTime: 0` และ `refetchOnMount: true`
- **UI Update**: ทันที (อัพเดท cache ก่อน แล้ว refetch ใน background)

---

## ⚠️ Important Notes

1. **`staleTime: 0`**: ไม่ cache เพื่อให้ refetch ทันทีเมื่อ invalidate
   - เหมาะสำหรับข้อมูลที่ต้องการ real-time
   - อาจเพิ่ม API calls แต่ทำให้ข้อมูลทันที

2. **`refetchOnMount: true`**: Refetch เมื่อ component mount
   - ทำให้ได้ข้อมูลล่าสุดเมื่อเปิด component
   - อาจเพิ่ม API calls แต่ทำให้ข้อมูลทันที

3. **`setQueryData` + `refetchQueries`**: 
   - `setQueryData`: อัพเดท cache ทันที (ไม่ต้องรอ network)
   - `refetchQueries`: Refetch ใน background เพื่อ sync กับ server

4. **`refetchType: 'active'`**: Refetch เฉพาะ queries ที่กำลัง active
   - ลด unnecessary refetches
   - ทำให้ refetch เร็วขึ้น

---

## 🎯 Implementation Checklist

- [x] ปรับ `staleTime` และ `refetchOnMount` ใน TaxInspectionForm
- [x] ปรับปรุง Cache Update Strategy
- [x] ปรับปรุง `invalidateQueries` และ `refetchQueries`
- [x] ปรับปรุง Table Components (TaxStatusTable, TaxFilingTable)
- [ ] ทดสอบการอัพเดทข้อมูลทันทีหลังบันทึก
- [ ] ทดสอบ performance และ API calls

---

## 📝 Testing Guide

### Test Case 1: บันทึกข้อมูลและตรวจสอบว่าตารางอัพเดททันที

1. เปิดหน้าตาราง (Tax Status, Tax Filing, หรือ Tax Inspection)
2. คลิก "เลือกบริษัทนี้" เพื่อเปิดฟอร์ม
3. แก้ไขข้อมูล (เช่น สถานะ ภ.พ.30)
4. กดบันทึก
5. **ตรวจสอบ**: ตารางควรอัพเดททันที (ไม่ต้องรอหรือรีเฟรช)

### Test Case 2: ตรวจสอบ Console Log

1. เปิด Browser DevTools → Console
2. บันทึกข้อมูล
3. **ตรวจสอบ**: ควรเห็น log:
   - `[TaxInspectionForm] Updating cache with status`
   - `[TaxInspectionForm] Invalidate and refetch list for sourcePage`
   - `[TaxInspectionForm] ผลการรีเซ็ตข้อมูลหน้าเว็บ`

### Test Case 3: ตรวจสอบ Network Requests

1. เปิด Browser DevTools → Network
2. บันทึกข้อมูล
3. **ตรวจสอบ**: ควรเห็น:
   - `PUT /api/monthly-tax-data/:id` (บันทึกข้อมูล)
   - `GET /api/monthly-tax-data` (refetch list) - ควรเกิดขึ้นใน background

---

**Last Updated**: 2026-02-03  
**Status**: 🔴 Critical - กำลังแก้ไข  
**Maintainer**: Cursor AI
