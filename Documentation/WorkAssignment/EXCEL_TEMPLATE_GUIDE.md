# 📋 คู่มือการกรอกข้อมูล Excel สำหรับนำเข้าข้อมูลการจัดงานรายเดือน

## 📖 Overview

คู่มือนี้ใช้สำหรับการกรอกข้อมูลในไฟล์ Excel เพื่อนำเข้าข้อมูลการจัดงานรายเดือนเข้าสู่ระบบ BMU Work Management System

## 📥 ดาวน์โหลด Template

1. เปิดหน้า "จัดงานรายเดือน" ในระบบ
2. คลิกปุ่ม "นำเข้าจาก Excel"
3. คลิกปุ่ม "ดาวน์โหลด Template" เพื่อดาวน์โหลดไฟล์ Excel template

## 📊 โครงสร้าง Template

### Columns (คอลัมน์) ใน Excel

| ลำดับ | ชื่อคอลัมน์ (ภาษาไทย) | ชื่อคอลัมน์ (ภาษาอังกฤษ) | Required | ข้อมูลเพิ่มเติม |
|------|---------------------|----------------------|----------|----------------|
| 1 | Build Code | build | ✅ **บังคับ** | รหัสลูกค้า (เช่น 001, 122, 122.1) |
| 2 | ปีภาษี | assignment_year | ✅ **บังคับ** | ปีภาษี (เช่น 2026) |
| 3 | เดือนภาษี | assignment_month | ✅ **บังคับ** | เดือนภาษี (1-12) |
| 4 | ผู้ทำบัญชี (รหัสพนักงาน) | accounting_responsible | ❌ | รหัสพนักงานผู้รับผิดชอบทำบัญชี (เช่น AC00034) |
| 5 | ผู้ตรวจภาษี (รหัสพนักงาน) | tax_inspection_responsible | ❌ | รหัสพนักงานผู้รับผิดชอบตรวจภาษี (เช่น AC00035) |
| 6 | ผู้ยื่น WHT (รหัสพนักงาน) | wht_filer_responsible | ❌ | รหัสพนักงานผู้รับผิดชอบยื่น WHT (เช่น AC00036) |
| 7 | ผู้ยื่น VAT (รหัสพนักงาน) | vat_filer_responsible | ❌ | รหัสพนักงานผู้รับผิดชอบยื่น VAT (เช่น AC00037) |
| 8 | ผู้คีย์เอกสาร (รหัสพนักงาน) | document_entry_responsible | ❌ | รหัสพนักงานผู้รับผิดชอบคีย์เอกสาร (เช่น AC00038) |
| 9 | หมายเหตุ | assignment_note | ❌ | หมายเหตุเพิ่มเติม (ถ้ามี) |

## ✅ Required Fields (ฟิลด์บังคับ)

**ฟิลด์ที่ต้องกรอกเท่านั้น:**
- ✅ **Build Code** - รหัสลูกค้า (เช่น 001, 122, 122.1)
- ✅ **ปีภาษี** - ปีภาษี (เช่น 2026)
- ✅ **เดือนภาษี** - เดือนภาษี (1-12)

**ฟิลด์อื่นๆ ทั้งหมดเป็น Optional (ไม่บังคับกรอก)**

## 📝 รายละเอียดการกรอกข้อมูล

### 1. Build Code
- **รูปแบบ**: ตัวเลขอย่างน้อย 3 หลัก (รองรับจุดทศนิยม)
- **ตัวอย่าง**: 
  - ตัวเลขธรรมดา: `001`, `122`, `375`
  - ตัวเลขที่มีจุดทศนิยม: `122.1`, `122.2`, `214.1`
- **ข้อกำหนด**: 
  - ต้องเป็นตัวเลขอย่างน้อย 3 หลัก (เช่น `001`, `122`, `375`)
  - รองรับจุดทศนิยมได้ (เช่น `122.1`, `214.2`)
  - ความยาวสูงสุด 10 ตัวอักษร (ตาม database schema)
  - **ต้องมีอยู่ในระบบแล้ว** (Build Code ต้องมีในตาราง clients)
- **⚠️ ข้อควรระวัง - Leading Zeros**:
  - Excel จะแปลง "001" เป็น number 1 โดยอัตโนมัติ
  - **วิธีแก้ไข**: Format cell เป็น **Text** ก่อนกรอกข้อมูล หรือกรอกเป็น `'001` (ใส่ apostrophe ด้านหน้า)
  - **หรือ**: กรอกเป็นตัวเลขธรรมดา (เช่น `1`, `8`, `18`) ระบบจะ pad leading zeros อัตโนมัติเป็น `001`, `008`, `018` (เฉพาะตัวเลขธรรมดาเท่านั้น)
  - **หมายเหตุ**: ถ้า Build Code มีจุดทศนิยม (เช่น `122.1`) ระบบจะไม่ pad leading zeros

### 2. ปีภาษี
- **รูปแบบ**: ตัวเลข 4 หลัก
- **ตัวอย่าง**: `2026`, `2025`, `2024`
- **ข้อกำหนด**: 
  - ต้องเป็นตัวเลขระหว่าง 2000-2100
  - ต้องกรอกข้อมูล

### 3. เดือนภาษี
- **รูปแบบ**: ตัวเลข 1-2 หลัก
- **ตัวอย่าง**: `1` (มกราคม), `6` (มิถุนายน), `12` (ธันวาคม)
- **ข้อกำหนด**: 
  - ต้องเป็นตัวเลขระหว่าง 1-12
  - ต้องกรอกข้อมูล
- **หมายเหตุ**: เดือนภาษี = เดือนปฏิทิน - 1 (เช่น เดือนมกราคม 2026 = เดือนภาษี ธันวาคม 2025)

### 4. ผู้ทำบัญชี (รหัสพนักงาน)
- **รูปแบบ**: รหัสพนักงาน (เช่น AC00034)
- **ตัวอย่าง**: `AC00034`, `AC00035`
- **ข้อกำหนด**: 
  - รหัสพนักงานต้องมีอยู่ในระบบ (ตาราง employees)
  - พนักงานต้องมี role: `service` หรือ `data_entry_and_service`
  - ถ้าไม่กรอก จะเป็น `null`

### 5. ผู้ตรวจภาษี (รหัสพนักงาน)
- **รูปแบบ**: รหัสพนักงาน (เช่น AC00035)
- **ตัวอย่าง**: `AC00035`, `AC00036`
- **ข้อกำหนด**: 
  - รหัสพนักงานต้องมีอยู่ในระบบ (ตาราง employees)
  - พนักงานต้องมี role: `audit`
  - ถ้าไม่กรอก จะเป็น `null`

### 6. ผู้ยื่น WHT (รหัสพนักงาน)
- **รูปแบบ**: รหัสพนักงาน (เช่น AC00036)
- **ตัวอย่าง**: `AC00036`, `AC00037`
- **ข้อกำหนด**: 
  - รหัสพนักงานต้องมีอยู่ในระบบ (ตาราง employees)
  - พนักงานต้องมี role: `data_entry_and_service`
  - ถ้าไม่กรอก จะเป็น `null`

### 7. ผู้ยื่น VAT (รหัสพนักงาน)
- **รูปแบบ**: รหัสพนักงาน (เช่น AC00037)
- **ตัวอย่าง**: `AC00037`, `AC00038`
- **ข้อกำหนด**: 
  - รหัสพนักงานต้องมีอยู่ในระบบ (ตาราง employees)
  - พนักงานต้องมี role: `data_entry_and_service`
  - ถ้าไม่กรอก จะเป็น `null`

### 8. ผู้คีย์เอกสาร (รหัสพนักงาน)
- **รูปแบบ**: รหัสพนักงาน (เช่น AC00038)
- **ตัวอย่าง**: `AC00038`, `AC00039`
- **ข้อกำหนด**: 
  - รหัสพนักงานต้องมีอยู่ในระบบ (ตาราง employees)
  - พนักงานต้องมี role: `data_entry_and_service` หรือ `data_entry`
  - ถ้าไม่กรอก จะเป็น `null`

### 9. หมายเหตุ
- **รูปแบบ**: ข้อความ
- **ตัวอย่าง**: `หมายเหตุการจัดงาน`, `ข้อมูลเพิ่มเติม`
- **ข้อกำหนด**: ถ้าไม่กรอก จะเป็น `null`

## 🔍 ตัวอย่างการกรอกข้อมูล

### ตัวอย่างที่ 1: ข้อมูลพื้นฐาน
```
Build Code | ปีภาษี | เดือนภาษี | ผู้ทำบัญชี (รหัสพนักงาน) | ผู้ตรวจภาษี (รหัสพนักงาน)
001 | 2026 | 1 | AC00034 | AC00035
```

### ตัวอย่างที่ 2: ข้อมูลครบถ้วน
```
Build Code | ปีภาษี | เดือนภาษี | ผู้ทำบัญชี | ผู้ตรวจภาษี | ผู้ยื่น WHT | ผู้ยื่น VAT | ผู้คีย์เอกสาร | หมายเหตุ
001 | 2026 | 1 | AC00034 | AC00035 | AC00036 | AC00037 | AC00038 | หมายเหตุการจัดงาน
122 | 2026 | 1 | AC00034 | AC00035 | AC00036 | AC00037 | AC00038 | 
122.1 | 2026 | 1 | AC00034 | AC00035 | AC00036 | AC00037 | AC00038 | สาขา
```

### ตัวอย่างที่ 3: ข้อมูลบางส่วน (ไม่กรอกทุกฟิลด์)
```
Build Code | ปีภาษี | เดือนภาษี | ผู้ทำบัญชี | ผู้ตรวจภาษี
001 | 2026 | 1 | AC00034 | 
122 | 2026 | 1 | | AC00035
```

## ⚠️ ข้อควรระวัง

### 1. Build Code ต้องมีอยู่ในระบบ
- **Build Code ที่กรอกต้องมีอยู่ในตาราง clients แล้ว**
- ถ้า Build Code ไม่มีอยู่ในระบบ ระบบจะแสดง error และไม่สามารถนำเข้าข้อมูลได้
- **วิธีแก้ไข**: ต้องสร้างข้อมูลลูกค้าในระบบก่อน (ผ่านหน้า "ข้อมูลลูกค้า")

### 2. การจัดงานซ้ำกัน
- ถ้ามีการจัดงานซ้ำกันในไฟล์ Excel (Build Code + ปีภาษี + เดือนภาษี ซ้ำกัน) ระบบจะแสดงคำเตือน
- ถ้ามีการจัดงานซ้ำกันในระบบ (Build Code + ปีภาษี + เดือนภาษี ซ้ำกัน) ระบบจะ**อัพเดทข้อมูลเดิม** (ไม่สร้างใหม่)
- **หมายเหตุ**: เมื่ออัพเดทข้อมูลเดิม ระบบจะรีเซ็ตข้อมูล `monthly_tax_data` และ `document_entry_work` อัตโนมัติ

### 3. รหัสพนักงาน
- รหัสพนักงานที่กรอกต้องมีอยู่ในระบบ (ตาราง employees)
- ถ้ารหัสพนักงานไม่มีอยู่ในระบบ ระบบจะแสดงคำเตือน แต่ยังสามารถนำเข้าข้อมูลได้ (ฟิลด์จะเป็น `null`)
- **หมายเหตุ**: ควรตรวจสอบรหัสพนักงานให้ถูกต้องก่อนนำเข้าข้อมูล

### 4. Leading Zeros (ศูนย์นำหน้า) - ⚠️ สำคัญมาก
- **Build Code**: Excel จะแปลง "001" เป็น number 1 โดยอัตโนมัติ
- **วิธีแก้ไข**:
  1. **วิธีที่ 1 (แนะนำ)**: Format cell เป็น **Text** ก่อนกรอกข้อมูล
     - เลือกคอลัมน์ Build Code
     - คลิกขวา → Format Cells → เลือก **Text**
     - จากนั้นกรอกข้อมูล (เช่น `001`, `122.1`)
  2. **วิธีที่ 2**: กรอกเป็นตัวเลขธรรมดา (เช่น `1`, `8`, `18`)
     - ระบบจะ pad leading zeros อัตโนมัติให้เป็น 3 หลัก (เฉพาะตัวเลขธรรมดาเท่านั้น)
  3. **วิธีที่ 3**: ใส่ apostrophe (`'`) ด้านหน้า (เช่น `'001`)
     - Excel จะ treat เป็น text และไม่แปลงเป็น number

### 5. เดือนภาษี
- เดือนภาษี = เดือนปฏิทิน - 1
- **ตัวอย่าง**:
  - เดือนมกราคม 2026 (ปฏิทิน) = เดือนภาษี ธันวาคม 2025
  - เดือนกุมภาพันธ์ 2026 (ปฏิทิน) = เดือนภาษี มกราคม 2026
- **หมายเหตุ**: ควรตรวจสอบเดือนภาษีให้ถูกต้องก่อนนำเข้าข้อมูล

## 🚀 ขั้นตอนการนำเข้าข้อมูล

1. **ดาวน์โหลด Template**
   - คลิกปุ่ม "ดาวน์โหลด Template" ในหน้า "นำเข้าจาก Excel"

2. **กรอกข้อมูล**
   - เปิดไฟล์ Excel template
   - กรอกข้อมูลตามรูปแบบที่กำหนด
   - **บังคับกรอก**: Build Code, ปีภาษี, เดือนภาษี
   - **ไม่บังคับ**: ฟิลด์อื่นๆ ทั้งหมด

3. **ตรวจสอบข้อมูล**
   - ตรวจสอบข้อมูลให้ถูกต้อง
   - ตรวจสอบ Build Code มีอยู่ในระบบแล้ว
   - ตรวจสอบรหัสพนักงานถูกต้อง

4. **บันทึกไฟล์**
   - บันทึกไฟล์ Excel (.xlsx หรือ .xls)

5. **อัพโหลดไฟล์**
   - คลิกปุ่ม "เลือกไฟล์" ในหน้า "นำเข้าจาก Excel"
   - เลือกไฟล์ Excel ที่กรอกข้อมูลแล้ว
   - ระบบจะตรวจสอบข้อมูลอัตโนมัติ

6. **ตรวจสอบผลการตรวจสอบ**
   - ดูผลการตรวจสอบข้อมูล
   - แก้ไขข้อมูลที่ไม่ถูกต้อง (ถ้ามี)
   - อัพโหลดไฟล์ใหม่ (ถ้าจำเป็น)

7. **นำเข้าข้อมูล**
   - คลิกปุ่ม "นำเข้าข้อมูล"
   - รอให้ระบบนำเข้าข้อมูลเสร็จ
   - ดูผลการนำเข้า

## 📋 Checklist ก่อนนำเข้าข้อมูล

- [ ] ดาวน์โหลด Template แล้ว
- [ ] กรอก Build Code, ปีภาษี, เดือนภาษี ครบทุกแถว
- [ ] ตรวจสอบ Build Code มีอยู่ในระบบแล้ว
- [ ] ตรวจสอบรหัสพนักงานถูกต้อง (ถ้ามี)
- [ ] ตรวจสอบเดือนภาษีถูกต้อง (เดือนภาษี = เดือนปฏิทิน - 1)
- [ ] บันทึกไฟล์ Excel แล้ว
- [ ] อัพโหลดไฟล์และตรวจสอบผลการตรวจสอบแล้ว

## 🆘 การแก้ไขปัญหา

### ปัญหา: Build Code ไม่มีอยู่ในระบบ
**อาการ**: Error "Build Code 'XXX' ไม่มีอยู่ในระบบ"
**สาเหตุ**: Build Code ที่กรอกไม่มีอยู่ในตาราง clients
**วิธีแก้**: 
1. ตรวจสอบ Build Code ให้ถูกต้อง
2. สร้างข้อมูลลูกค้าในระบบก่อน (ผ่านหน้า "ข้อมูลลูกค้า")
3. จากนั้นนำเข้าข้อมูลการจัดงานอีกครั้ง

### ปัญหา: Build Code หรือเลขทะเบียนนิติบุคคลขาด leading zeros
**อาการ**: Error "Build Code ต้องเป็นตัวเลขอย่างน้อย 3 หลัก"
**สาเหตุ**: Excel แปลงตัวเลขที่มี leading zeros เป็น number type ซึ่งจะตัด leading zeros ออก
**วิธีแก้**: 
1. Format cell เป็น **Text** ก่อนกรอกข้อมูล (แนะนำ)
2. หรือกรอกเป็นตัวเลขธรรมดา (เช่น `1` แทน `001`) ระบบจะ pad อัตโนมัติ (เฉพาะตัวเลขธรรมดา)
3. หรือใส่ apostrophe (`'`) ด้านหน้า (เช่น `'001`)

### ปัญหา: การจัดงานซ้ำกัน
**อาการ**: Warning "การจัดงานซ้ำกันในไฟล์" หรือ "การจัดงานนี้มีอยู่ในระบบแล้ว"
**สาเหตุ**: มีการจัดงานซ้ำกันในไฟล์ Excel หรือในระบบ (Build Code + ปีภาษี + เดือนภาษี ซ้ำกัน)
**วิธีแก้**: 
- ถ้าต้องการอัพเดทข้อมูลเดิม: ไม่ต้องแก้ไข ระบบจะอัพเดทข้อมูลเดิมอัตโนมัติ
- ถ้าไม่ต้องการอัพเดท: ลบแถวที่ซ้ำกันออกจากไฟล์ Excel

### ปัญหา: รหัสพนักงานไม่มีอยู่ในระบบ
**อาการ**: Warning "รหัสพนักงาน 'XXX' สำหรับ... ไม่มีอยู่ในระบบ"
**สาเหตุ**: รหัสพนักงานที่กรอกไม่มีอยู่ในตาราง employees
**วิธีแก้**: 
1. ตรวจสอบรหัสพนักงานให้ถูกต้อง
2. หรือลบรหัสพนักงานออกจากไฟล์ Excel (ถ้าไม่ต้องการกำหนดผู้รับผิดชอบ)
3. ระบบจะยังสามารถนำเข้าข้อมูลได้ (ฟิลด์จะเป็น `null`)

### ปัญหา: ปีภาษีหรือเดือนภาษีไม่ถูกต้อง
**อาการ**: Error "ปีภาษีต้องเป็นตัวเลขระหว่าง 2000-2100" หรือ "เดือนภาษีต้องเป็นตัวเลขระหว่าง 1-12"
**สาเหตุ**: ปีภาษีหรือเดือนภาษีที่กรอกไม่ถูกต้อง
**วิธีแก้**: 
1. ตรวจสอบปีภาษีให้เป็นตัวเลขระหว่าง 2000-2100
2. ตรวจสอบเดือนภาษีให้เป็นตัวเลขระหว่าง 1-12
3. ตรวจสอบเดือนภาษีให้ถูกต้อง (เดือนภาษี = เดือนปฏิทิน - 1)

### ปัญหา: ข้อมูลไม่ครบถ้วน
**วิธีแก้**: ตรวจสอบว่ามี Build Code, ปีภาษี, เดือนภาษี ครบทุกแถว

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อทีมพัฒนา

---

**Last Updated**: 2026-01-31
**Version**: 1.0.0
