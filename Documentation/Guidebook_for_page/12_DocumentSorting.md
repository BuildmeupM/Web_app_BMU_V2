# 📝 12. Document Sorting Page (คัดแยกเอกสาร)

## 📋 Overview

หน้าคัดแยกเอกสาร - สำหรับคัดแยกข้อมูลเอกสารและส่งข้อมูลเข้าไปยัง `document_entry_work` พร้อมระบบแจ้งเตือนไปยัง `document_entry_responsible`

**Route**: `/document-sorting`  
**Component**: `src/pages/DocumentSorting.tsx`

## 🔐 Access Control

- ✅ **admin**
- ✅ **data_entry**
- ✅ **data_entry_and_service**

## ✨ Features

### 1. Summary Statistics (สรุปข้อมูลการส่งงานคีย์)
- ✅ แสดงสรุปข้อมูล 4 ตัวเลข:
  - จำนวนบริษัทที่ส่งงานคีย์ (สีส้ม)
  - จำนวนครั้งที่ส่งข้อมูล (สีส้ม)
  - ดำเนินการเสร็จแล้ว (สีเขียว)
  - ยังไม่ดำเนินการเสร็จ (สีแดง)
- ✅ แสดงรายชื่อบริษัทที่เสร็จแล้วและยังไม่เสร็จใน Accordion
- ✅ แต่ละรายการแสดง Build และชื่อบริษัท
- ✅ เรียงลำดับตามชื่อบริษัท
- ✅ **Component**: `SummaryStats` จาก `src/components/DocumentSorting/SummaryStats.tsx`

### 2. Company Selection (เลือกบริษัท)
- ✅ ตารางแสดงรายการบริษัทพร้อมข้อมูลสรุป:
  - Build (sticky column)
  - ชื่อบริษัท (sticky column)
  - เอกสารมีหัก ณ ที่จ่าย (WHT) - รวมทุกครั้งที่ส่ง (centered)
  - เอกสารมีภาษีมูลค่าเพิ่ม (VAT) - รวมทุกครั้งที่ส่ง (centered)
  - เอกสารไม่มีภาษีมูลค่าเพิ่ม (Non-VAT) - รวมทุกครั้งที่ส่ง (centered)
  - ส่งข้อมูลบอทอัตโนมัติ - รวมทุกครั้งที่ส่ง (centered)
  - ข้อมูลผู้รับผิดชอบ (document_entry_responsible)
  - ปุ่ม "เลือกบริษัทนี้"
- ✅ ดึงข้อมูลจาก `monthly_tax_data` โดย filter `accounting_responsible` = current user's employee_id
- ✅ ดึงข้อมูล `document_entry_work` เพื่อรวม document counts และ bot counts
- ✅ Sticky columns สำหรับ Build และชื่อบริษัทเมื่อ scroll แนวนอน
- ✅ เมื่อเลือกบริษัท ระบบจะดึงข้อมูล `document_entry_work` จาก database อัตโนมัติ
- ✅ แสดง submission_count (ครั้งที่ 1, 2, 3, 4...) เมื่อเลือกบริษัท
- ✅ **Component**: `CompanyTable` จาก `src/components/DocumentSorting/CompanyTable.tsx`

### 3. Submission Count Badge (แสดงครั้งที่ส่งงาน)
- ✅ แสดง "ครั้งที่ X" (1, 2, 3, 4...)
- ✅ ดึงข้อมูลจาก `document_entry_work` โดยเช็ค `submission_count` (MAX)
- ✅ แสดงเป็น Badge สีส้ม (outline variant, white background, orange border/font)
- ✅ เริ่มนับจากครั้งที่ 1 เมื่อยังไม่มีการส่ง (submission_count = 0)
- ✅ **Component**: `SubmissionCountBadge` จาก `src/components/DocumentSorting/SubmissionCountBadge.tsx`

### 4. ลำดับการแสดงเมื่อเลือกบริษัท (Layout Order)
- ✅ **ส่วนที่จะต้องกรอกข้อมูล** อยู่**ด้านบน**ของหัวข้อ "ประวัติการส่งข้อมูลก่อนหน้านี้"
- ✅ ลำดับ: ปุ่มปิด/ล้างการเลือก → ข้อความการส่งงานคีย์ + ครั้งที่ X → ส่วนการคีย์เอกสาร → บอท → ความคิดเห็น → ปุ่มส่ง → (เส้นแบ่ง) → **ประวัติการส่งข้อมูลก่อนหน้านี้**
- ✅ ไฟล์: `src/pages/DocumentSorting.tsx`

### 5. Submission History (ประวัติการส่งข้อมูลก่อนหน้านี้)
- ✅ แสดงตารางประวัติการส่งข้อมูลทั้งหมดของบริษัทที่เลือก
- ✅ **หัวตารางตรึง (sticky)**: ห่อตารางด้วย `maxHeight: 60vh`, `overflow: auto` เพื่อให้การเลื่อนเกิดภายในบล็อก; thead ใช้ `position: sticky`, `top: 0` — เวลาเลื่อนลงหัวคอลัมน์จะคงอยู่ด้านบน
- ✅ คอลัมน์:
  - ครั้งที่ (Badge สีส้ม outline)
  - วันที่ส่ง
  - WHT, สถานะ WHT, ผู้อนุมัติ WHT
  - VAT, สถานะ VAT, ผู้อนุมัติ VAT
  - Non-VAT, สถานะ Non-VAT, ผู้อนุมัติ Non-VAT
  - บอท
  - จัดการ (ปุ่มแก้ไข - แสดงเฉพาะเมื่อสถานะทั้งหมดเป็น "ยังไม่ดำเนินการ")
- ✅ **การแสดงจำนวน**: ค่าเป็น **0** แสดงเป็น **"-"** (ไม่แสดงตัวเลข 0)
- ✅ **การแสดงสถานะ**:
  - **มีข้อมูล (count > 0)** → แสดงสถานะ (รวม "ยังไม่ดำเนินการ" ถ้าสถานะเป็น null/ยังไม่ดำเนินการ)
  - **ไม่มีข้อมูล (count = 0)** → ไม่แสดงสถานะ (แสดง "-")
- ✅ สถานะการคีย์: "ยังไม่ดำเนินการ" (แดง), "กำลังดำเนินการ" (เหลือง), "ดำเนินการเสร็จแล้ว" (เขียว)
- ✅ แสดงความคิดเห็นส่งมอบงานคีย์และความคิดเห็นส่งคืนงานคีย์ในรูปแบบ grid 2 คอลัมน์
- ✅ แสดงความคิดเห็นทุกครั้ง (ถ้าไม่มีจะแสดง "-")
- ✅ ปุ่มแก้ไข: แสดงเฉพาะเมื่อสถานะทั้งหมดเป็น "ยังไม่ดำเนินการ"
- ✅ **Component**: `SubmissionHistory` จาก `src/components/DocumentSorting/SubmissionHistory.tsx`

### 6. Document Keying Section (ส่วนการคีย์เอกสาร)
- ✅ แสดงจำนวนเอกสารแต่ละประเภท:
  - เอกสารมีหัก ณ ที่จ่าย (WHT) - NumberInput
  - เอกสารมีภาษีมูลค่าเพิ่ม (VAT) - NumberInput (disabled เมื่อ tax_registration_status !== 'จดภาษีมูลค่าเพิ่ม')
  - เอกสารไม่มีภาษีมูลค่าเพิ่ม (Non-VAT) - NumberInput
- ✅ Layout: SimpleGrid (3 columns on desktop, 1 column on mobile)
- ✅ Validation: ต้องกรอกจำนวนเอกสาร (min: 0, ไม่สามารถกรอกค่าติดลบได้)
- ✅ Placeholder: "กรอกจำนวนเอกสาร" (แทน "0")
- ✅ ค่าเริ่มต้น: ว่าง (แทน 0) แต่ส่งค่า 0 ไป backend
- ✅ VAT Input: Disabled เมื่อบริษัทไม่ได้จดภาษีมูลค่าเพิ่ม
- ✅ **Component**: `DocumentKeyingSection` จาก `src/components/DocumentSorting/DocumentKeyingSection.tsx`

### 7. Bot Submission Section (ส่วนการส่งข้อมูลบอทอัตโนมัติ)
- ✅ แสดงรายการ bots ที่เพิ่มแล้ว
- ✅ แต่ละ bot มี:
  - Select dropdown (5 ตัวเลือก):
    - Shopee (Thailand)
    - SPX Express (Thailand)
    - Lazada Limited (Head Office)
    - Lazada Express Limited
    - ระบบ OCR
  - NumberInput สำหรับจำนวนเอกสาร
  - Textarea สำหรับ OCR additional info (แสดงเมื่อเลือก "ระบบ OCR")
  - ปุ่มลบ (trash icon)
- ✅ ปุ่ม "เพิ่มบอท" สำหรับเพิ่ม bot entry ใหม่
- ✅ รองรับการเพิ่ม/ลบ bots หลายตัว
- ✅ Placeholder: "กรอกจำนวนเอกสาร" (แทน "0")
- ✅ ค่าเริ่มต้น: ว่าง (แทน 0) แต่ส่งค่า 0 ไป backend
- ✅ Validation: ไม่สามารถกรอกค่าติดลบได้
- ✅ **Component**: `BotSubmissionSection` จาก `src/components/DocumentSorting/BotSubmissionSection.tsx`

### 8. Comments Section (ส่วนการกรอกความคิดเห็น)
- ✅ ความคิดเห็นส่งมอบงานคีย์ - Textarea
- ✅ ความคิดเห็นส่งคืนงานคีย์ - Textarea (disabled เสมอ, พื้นหลังสีเทา)
- ✅ **Component**: `CommentsSection` จาก `src/components/DocumentSorting/CommentsSection.tsx`

### 9. Form Submission (การส่งข้อมูล)
- ✅ ปุ่ม "เริ่มต้นการคัดแยกเอกสาร" (เมื่อสร้างใหม่)
- ✅ ปุ่ม "อัพเดทข้อมูล" (เมื่อมีข้อมูลเก่าหรือกำลังแก้ไข)
- ✅ Loading state เมื่อกำลังส่งข้อมูล
- ✅ Success notification เมื่อส่งข้อมูลสำเร็จ
- ✅ Error notification เมื่อเกิดข้อผิดพลาด (รวมถึง Network Error)
- ✅ Auto-reset form หลังจากสร้างสำเร็จ
- ✅ Auto-refetch data หลังจากอัพเดทสำเร็จ
- ✅ Logic การตัดสินใจว่าจะ update หรือ create:
  - ถ้ามี `existingDocumentEntryWorkId` → Update
  - ถ้าไม่มี `existingDocumentEntryWorkId` → Create ใหม่
- ✅ การส่งครั้งที่ 2, 3, ... จะสร้าง record ใหม่เสมอ ไม่ได้อัพเดทครั้งที่ 1

### 10. Refresh Data Button (ปุ่มรีเฟรชข้อมูล)
- ✅ ปุ่มรีเฟรชข้อมูลใน header ด้านขวา
- ✅ Invalidate และ refetch queries ทั้งหมดที่เกี่ยวข้อง (`document-entry-work`, `monthly-tax-data`)
- ✅ แสดง loading state เมื่อกำลัง refresh
- ✅ แสดง notification เมื่อสำเร็จหรือเกิดข้อผิดพลาด

### 11. Clear Selection Button (ปุ่มปิด/ล้างการเลือกบริษัท)
- ✅ ปุ่มปิด/ล้างการเลือกบริษัทด้านบนของ Submission History
- ✅ ล้างการเลือกบริษัทและ reset form ทั้งหมด
- ✅ Disable เมื่อกำลัง submit

### 12. Edit Entry Feature (ฟีเจอร์แก้ไขข้อมูล)
- ✅ ปุ่มแก้ไขในตารางประวัติการส่งข้อมูล
- ✅ แสดงปุ่มแก้ไขเฉพาะเมื่อสถานะทั้งหมด (WHT, VAT, Non-VAT) เป็น "ยังไม่ดำเนินการ"
- ✅ เมื่อคลิกปุ่มแก้ไข:
  - ตรวจสอบว่าสามารถแก้ไขได้หรือไม่
  - ถ้าไม่สามารถแก้ไขได้ แสดง notification
  - ถ้าสามารถแก้ไขได้ โหลดข้อมูล entry มาแสดงในฟอร์ม
  - Scroll ไปยังฟอร์มเพื่อให้ผู้ใช้เห็นข้อมูลที่โหลดมา
- ✅ เมื่อ submit จะใช้ update mutation เพื่ออัพเดทข้อมูล

## 📊 Data Flow

### 1. Initial Load
1. User เข้าหน้า Document Sorting
2. `SummaryStats` ดึงข้อมูลสรุปจาก `GET /api/document-entry-work?accounting_responsible={employeeId}&year={year}&month={month}`
3. `CompanyTable` ดึงข้อมูลบริษัทจาก `GET /api/monthly-tax-data?accounting_responsible={employeeId}&year={year}&month={month}`
4. `CompanyTable` ดึงข้อมูล `document_entry_work` เพื่อรวม document counts และ bot counts
5. แสดงตารางบริษัทพร้อมข้อมูลสรุป

### 2. Company Selection
1. User คลิกปุ่ม "เลือกบริษัทนี้" ในตาราง
2. ระบบดึงข้อมูล `document_entry_work` จาก `GET /api/document-entry-work/{build}/{year}/{month}`
3. แสดง submission_count (MAX จาก database)
4. แสดงประวัติการส่งข้อมูลก่อนหน้านี้ (SubmissionHistory)
5. Logic การโหลดข้อมูล:
   - ถ้ามีการส่งไปแล้ว (`submissionCount > 0`) → Reset form เป็นค่าว่าง (เพื่อกรอกครั้งใหม่)
   - ถ้ายังไม่มีการส่ง (`submissionCount === 0`) และมี record อยู่ → โหลดข้อมูลมาแสดง (เพื่อให้แก้ไขได้)

### 3. Edit Entry from History
1. User คลิกปุ่ม "แก้ไข" ในตารางประวัติการส่งข้อมูล
2. ระบบตรวจสอบว่าสามารถแก้ไขได้หรือไม่ (สถานะทั้งหมดต้องเป็น "ยังไม่ดำเนินการ")
3. ถ้าไม่สามารถแก้ไขได้ แสดง notification
4. ถ้าสามารถแก้ไขได้:
   - ดึงข้อมูล entry จาก `GET /api/document-entry-work/{id}`
   - โหลดข้อมูลเข้า form (document counts, bots, comments)
   - ตั้งค่า `existingDocumentEntryWorkId` เพื่อให้ระบบรู้ว่าต้อง update
   - Scroll ไปยังฟอร์ม

### 4. Form Submission
1. User กรอกข้อมูล 3 ส่วน:
   - ส่วนการคีย์เอกสาร (WHT, VAT, Non-VAT)
   - ส่วนการส่งข้อมูลบอทอัตโนมัติ (สามารถเพิ่มหลายบรรทัด)
   - ส่วนการกรอกความคิดเห็น
2. กดปุ่ม "เริ่มต้นการคัดแยกเอกสาร" หรือ "อัพเดทข้อมูล"
3. Logic การตัดสินใจ:
   - ถ้ามี `existingDocumentEntryWorkId` → ส่งไปยัง `PUT /api/document-entry-work/:id`
   - ถ้าไม่มี `existingDocumentEntryWorkId` → ส่งไปยัง `POST /api/document-entry-work`
4. Backend:
   - สร้าง/อัพเดท `document_entry_work`
   - สร้าง/อัพเดท `document_entry_work_bots`
   - ส่ง notification ไปยัง `document_entry_responsible` (พร้อม `related_user_id`)
   - อัพเดท `submission_count` (เฉพาะเมื่อสร้างใหม่)

## 🔔 Notification System

### Notification Creation
เมื่อสร้าง `document_entry_work` สำเร็จ ระบบจะสร้าง notification อัตโนมัติสำหรับ `document_entry_responsible`:

**Notification Type**: `document_entry_pending`

**Category**: `document`

**Priority**: `medium`

**Title**: "มีการส่งข้อมูลการคัดแยกเอกสาร"

**Message**: "บริษัท {companyName} ({build}) - เดือน {month}/{year}\nครั้งที่ {submissionCount}"

**Icon**: `TbFileText`

**Color**: `blue`

**Action URL**: `/document-entry?build={build}&year={year}&month={month}`

**Action Label**: "ดูรายละเอียด"

**Related User ID**: `user_id` ของพนักงานที่รับผิดชอบในการคีย์ (document_entry_responsible)

**Related Entity Type**: `document_entry_work`

**Related Entity ID**: `document_entry_work.id`

**Expires At**: 7 days from now

**หมายเหตุ**: 
- ⚠️ **สำคัญ**: `related_user_id` จะถูกตั้งค่าเป็น `user_id` ของพนักงานที่รับผิดชอบในการคีย์ (document_entry_responsible)
- ⚠️ **สำคัญ**: Notification จะถูกสร้างเฉพาะเมื่อสร้าง entry ใหม่เท่านั้น (ไม่สร้างเมื่อ update)

## 📡 API Endpoints

### GET /api/monthly-tax-data
ดึงรายการบริษัทที่รับผิดชอบ (filter by `accounting_responsible`)

**Used By**: `CompanySelector` component

### GET /api/document-entry-work/:build/:year/:month
ดึงข้อมูลงานคีย์เอกสารตาม Build, Year, Month (รวม bots)

**Used By**: Main page (when company is selected)

### POST /api/document-entry-work
สร้างงานคีย์เอกสารใหม่ (พร้อม bots และ notification)

**Used By**: Form submission (when creating new entry)

### PUT /api/document-entry-work/:id
แก้ไขข้อมูลงานคีย์เอกสาร (พร้อม bots)

**Used By**: Form submission (when updating existing entry)

**Documentation**: `Documentation/API/DOCUMENT_ENTRY_WORK_API.md`

## 🧩 Components

### Main Page Component
- **File**: `src/pages/DocumentSorting.tsx`
- **Features**:
  - State management สำหรับ form data
  - Form submission logic
  - Integration with React Query
  - Error handling และ notifications

### Sub-Components
1. **SummaryStats** (`src/components/DocumentSorting/SummaryStats.tsx`)
   - แสดงสรุปข้อมูลการส่งงานคีย์ (จำนวนบริษัท, จำนวนครั้ง, สถานะ)
   - แสดงรายชื่อบริษัทที่เสร็จแล้วและยังไม่เสร็จใน Accordion
   - ดึงข้อมูลจาก `document_entry_work` และ `monthly_tax_data`

2. **CompanyTable** (`src/components/DocumentSorting/CompanyTable.tsx`)
   - ตารางแสดงรายการบริษัทพร้อมข้อมูลสรุป
   - Sticky columns สำหรับ Build และชื่อบริษัท
   - รวม document counts และ bot counts จากทุกครั้งที่ส่ง
   - ดึงข้อมูลจาก `monthly_tax_data` และ `document_entry_work`

3. **SubmissionHistory** (`src/components/DocumentSorting/SubmissionHistory.tsx`)
   - แสดงตารางประวัติการส่งข้อมูลทั้งหมดของบริษัทที่เลือก
   - แสดงสถานะการคีย์และผู้อนุมัติ
   - แสดงความคิดเห็นในรูปแบบ grid 2 คอลัมน์
   - ปุ่มแก้ไข (แสดงเฉพาะเมื่อสถานะทั้งหมดเป็น "ยังไม่ดำเนินการ")
   - ดึงข้อมูลจาก `document_entry_work` และ `employee` (สำหรับแสดงชื่อผู้อนุมัติ)

4. **SubmissionCountBadge** (`src/components/DocumentSorting/SubmissionCountBadge.tsx`)
   - แสดงครั้งที่ส่งงาน
   - Badge สีส้ม (outline variant)

5. **DocumentKeyingSection** (`src/components/DocumentSorting/DocumentKeyingSection.tsx`)
   - Form สำหรับกรอกจำนวนเอกสารแต่ละประเภท (WHT, VAT, Non-VAT)
   - VAT Input: Disabled เมื่อ `vatDisabled` prop เป็น true
   - Placeholder: "กรอกจำนวนเอกสาร"
   - ค่าเริ่มต้น: ว่าง (แทน 0)

6. **BotSubmissionSection** (`src/components/DocumentSorting/BotSubmissionSection.tsx`)
   - Form สำหรับกรอกข้อมูลบอทอัตโนมัติ (รองรับหลายบอท, OCR field)
   - Placeholder: "กรอกจำนวนเอกสาร"
   - ค่าเริ่มต้น: ว่าง (แทน 0)

7. **CommentsSection** (`src/components/DocumentSorting/CommentsSection.tsx`)
   - Form สำหรับกรอกความคิดเห็น (submission_comment, return_comment)
   - Return Comment: Disabled เสมอ, พื้นหลังสีเทา

## 📦 Service Layer

### documentEntryWorkService
**File**: `src/services/documentEntryWorkService.ts`

**Functions**:
- `getList(params)` - ดึงรายการงานคีย์เอกสาร
- `getByBuildYearMonth(build, year, month)` - ดึงข้อมูลตาม Build, Year, Month
- `getById(id)` - ดึงข้อมูลตาม ID
- `create(data)` - สร้างงานคีย์เอกสารใหม่
- `update(id, data)` - แก้ไขข้อมูลงานคีย์เอกสาร

## 🗄️ Database Schema

### document_entry_work
ตารางหลักสำหรับเก็บข้อมูลงานคีย์เอกสาร

**Migration**: `migrations/015_create_document_entry_work_table.sql`

**Key Fields**:
- `id` - Primary Key (UUID)
- `build` - Build code (Foreign Key to clients)
- `work_year` - Work year
- `work_month` - Work month (1-12)
- `entry_timestamp` - Timestamp when entry was created
- `submission_count` - Number of times submitted (incremented automatically)
- `responsible_employee_id` - Responsible employee ID
- `wht_document_count` - Number of WHT documents
- `vat_document_count` - Number of VAT documents
- `non_vat_document_count` - Number of Non-VAT documents
- `submission_comment` - Submission comment
- `return_comment` - Return comment

### document_entry_work_bots
ตารางสำหรับเก็บข้อมูลบอทอัตโนมัติสำหรับแต่ละงานคีย์เอกสาร

**Migration**: `migrations/031_create_document_entry_work_bots_table.sql`

**Key Fields**:
- `id` - Primary Key (UUID)
- `document_entry_work_id` - Foreign Key to document_entry_work
- `bot_type` - Bot type (ENUM: 5 options)
- `document_count` - Number of documents
- `ocr_additional_info` - Additional info for OCR (only when bot_type = 'ระบบ OCR')

## 🎨 UI/UX Guidelines

### Layout
- **Container**: Fluid container with padding
- **Header**: Orange card with title and description
- **Form**: Paper component with border, padding, and radius
- **Sections**: Separated by Dividers

### Colors
- **Primary**: Orange (#ff6b35, #ff8c42)
- **Success**: Green
- **Error**: Red
- **Info**: Blue

### Typography
- **Font**: Kanit (Thai), Arial (English)
- **Title**: Size lg, Weight 600
- **Label**: Size sm, Weight 500
- **Description**: Size sm, Color dimmed

### Form Elements
- **NumberInput**: Min 0, required
- **Select**: Searchable, clearable, required
- **Textarea**: Min rows 3
- **Button**: Size md, with loading state

## 🔄 State Management

### Form State
- `selectedBuild` - Selected company build code
- `selectedCompanyName` - Selected company name
- `taxRegistrationStatus` - Tax registration status (สำหรับตรวจสอบว่า VAT allowed หรือไม่)
- `whtDocumentCount` - WHT document count
- `vatDocumentCount` - VAT document count
- `nonVatDocumentCount` - Non-VAT document count
- `bots` - Array of bot entries
- `submissionComment` - Submission comment
- `returnComment` - Return comment (disabled เสมอ)
- `submissionCount` - Submission count (MAX from database)
- `existingDocumentEntryWorkId` - Existing document entry work ID (for update)
- `existingRecordSubmissionCount` - Submission count ของ record ที่โหลดมา (สำหรับแสดงใน badge)
- `isRefreshing` - Loading state สำหรับปุ่มรีเฟรช

### Derived State
- `isVatAllowed` - ตรวจสอบว่า VAT allowed หรือไม่ (`taxRegistrationStatus === 'จดภาษีมูลค่าเพิ่ม'`)

### React Query
- **Query Key**: `['document-entry-work', build, year, month]`
- **Stale Time**: 1 minute
- **Cache Time**: 5 minutes
- **Auto-refetch**: When company is selected

## ✅ Validation

### Required Fields
- `build` - Build code (from company selection)
- `work_year` - Work year (auto-filled from current tax month)
- `work_month` - Work month (auto-filled from current tax month)
- `responsible_employee_id` - Responsible employee ID (from current user)

### Optional Fields
- `wht_document_count` (default: 0)
- `vat_document_count` (default: 0)
- `non_vat_document_count` (default: 0)
- `submission_comment` (default: null)
- `return_comment` (default: null)
- `bots` (default: [])

### Bot Validation
- `bot_type` - Required, must be one of 5 options
- `document_count` - Required, min: 0
- `ocr_additional_info` - Required when `bot_type` = 'ระบบ OCR'

## 🐛 Error Handling

### API Errors
- **400 Bad Request**: Missing required fields
- **404 Not Found**: Document entry work not found
- **500 Internal Server Error**: Server error

### User Feedback
- Success notification when create/update succeeds
- Error notification when create/update fails
- Loading state during API calls

## 🎯 Key Features & Logic

### 1. Submission Count Logic
- **การนับครั้งที่ส่ง**: เริ่มจากครั้งที่ 1 เมื่อยังไม่มีการส่ง (`submissionCount = 0`)
- **การแสดงผล**: แสดง `(submissionCount || 0) + 1` ใน badge
- **Backend**: ส่ง `MAX(submission_count)` จาก database เสมอ

### 2. Create vs Update Logic
- **Create**: เมื่อไม่มี `existingDocumentEntryWorkId` → สร้าง record ใหม่เสมอ
- **Update**: เมื่อมี `existingDocumentEntryWorkId` → อัพเดท record ที่มีอยู่
- **การส่งครั้งที่ 2, 3, ...**: สร้าง record ใหม่เสมอ ไม่ได้อัพเดทครั้งที่ 1
- **การแก้ไข**: เมื่อแก้ไข entry จาก submission history จะตั้งค่า `existingDocumentEntryWorkId` → ใช้ update

### 3. Form Reset Logic
- **เมื่อมีการส่งไปแล้ว** (`submissionCount > 0`): Reset form เป็นค่าว่าง (เพื่อกรอกครั้งใหม่)
- **เมื่อยังไม่มีการส่ง** (`submissionCount === 0`): โหลดข้อมูลมาแสดง (เพื่อให้แก้ไขได้)

### 4. VAT Input Disable Logic
- **ตรวจสอบ**: `tax_registration_status` จาก `clients` table
- **Disable เมื่อ**: `tax_registration_status !== 'จดภาษีมูลค่าเพิ่ม'`
- **Enable เมื่อ**: `tax_registration_status === 'จดภาษีมูลค่าเพิ่ม'`

### 5. Edit Entry Logic
- **เงื่อนไขการแก้ไข**: สถานะทั้งหมด (WHT, VAT, Non-VAT) ต้องเป็น "ยังไม่ดำเนินการ"
- **เมื่อคลิกแก้ไข**:
  1. ตรวจสอบสถานะทั้งหมด
  2. ถ้าไม่สามารถแก้ไขได้ → แสดง notification
  3. ถ้าสามารถแก้ไขได้ → โหลดข้อมูลมาแสดงในฟอร์ม
  4. ตั้งค่า `existingDocumentEntryWorkId` → ใช้ update เมื่อ submit

### 6. Sticky Columns
- **Build Column**: `position: sticky`, `left: 0`, `zIndex: 10` (body) / `15` (header)
- **Company Name Column**: `position: sticky`, `left: 120px`, `zIndex: 10` (body) / `15` (header)
- **Background**: `#fff` (ปกติ) หรือ `#fff3e0` (เมื่อ selected)
- **Border**: `borderRight: '1px solid #dee2e6'`

## 📝 Recent Updates

### 2026-02-04
- ✅ เพิ่ม Summary Stats Component (แสดงสรุปข้อมูลและรายชื่อบริษัท)
- ✅ เปลี่ยนจาก CompanySelector เป็น CompanyTable (แสดงตารางพร้อมข้อมูลสรุป)
- ✅ เพิ่ม Submission History Component (แสดงประวัติการส่งข้อมูลพร้อมความคิดเห็น)
- ✅ เพิ่มปุ่มรีเฟรชข้อมูลใน header
- ✅ เพิ่มปุ่มปิด/ล้างการเลือกบริษัท
- ✅ เพิ่มฟีเจอร์แก้ไขข้อมูลจาก submission history (เฉพาะเมื่อสถานะทั้งหมดเป็น "ยังไม่ดำเนินการ")
- ✅ ปรับปรุง logic การสร้าง/อัพเดทข้อมูล (การส่งครั้งที่ 2, 3, ... สร้าง record ใหม่เสมอ)
- ✅ ปรับปรุง form reset logic (reset เมื่อมีการส่งไปแล้ว)
- ✅ เพิ่มการตรวจสอบ tax_registration_status เพื่อ disable VAT input
- ✅ ปรับปรุง NumberInput (placeholder, ค่าเริ่มต้น, ไม่สามารถกรอกค่าติดลบ)
- ✅ ปรับปรุง Comments Section (return comment disabled)
- ✅ ปรับปรุงการแสดงความคิดเห็นในรูปแบบ grid 2 คอลัมน์
- ✅ เพิ่ม related_user_id ใน notification (เป็น user_id ของ document_entry_responsible)
- ✅ ปรับปรุง error handling สำหรับ Network Error
- ✅ ย้ายส่วนกรอกข้อมูลไว้ด้านบนของประวัติการส่งข้อมูลก่อนหน้านี้
- ✅ ประวัติการส่งข้อมูล: จำนวน 0 แสดงเป็น "-"; แสดงสถานะ "ยังไม่ดำเนินการ" เฉพาะเมื่อมีข้อมูล (count > 0)
- ✅ หัวตารางประวัติตรึง (sticky) เมื่อเลื่อน — ใช้ wrapper maxHeight 60vh + overflow auto

### 2026-02-03
- ✅ Created Document Sorting page with all components
- ✅ Integrated with backend API
- ✅ Added notification system for `document_entry_responsible`
- ✅ Added submission count tracking
- ✅ Added bot submission section with OCR support
- ✅ Added form validation and error handling

---

**Last Updated**: 2026-02-04
