# 📄 07. Document Sorting Page

## 📋 Overview

หน้าจัดการการคัดแยกเอกสาร - สำหรับคัดแยกข้อมูลเอกสารและส่งข้อมูลเข้าไปยัง document_entry_work

**Route**: `/document-sorting`  
**Component**: `src/pages/DocumentSorting.tsx`

## 🔐 Access Control

- ✅ **admin**
- ✅ **service**

## ✨ Features

### 1. Document Sorting
- ✅ คัดแยกเอกสาร
- ✅ จัดหมวดหมู่
- ✅ Search และ Filter
- ✅ **Acknowledgment Modal**: แสดงหน้าต่างยืนยันก่อนเปิดฟอร์มสำหรับทุกบริษัทเมื่อกดปุ่ม "เลือกบริษัทนี้"

### 2. Acknowledgment Modal
- ✅ แสดงหน้าต่างยืนยันก่อนเปิดฟอร์มสำหรับทุกบริษัท
- ✅ ดึงข้อมูล acknowledgment จาก `monthly_tax_data` เมื่อเลือกบริษัท
- ✅ แสดงข้อมูล acknowledgment (ถ้ามี) หรือ modal ว่าง (ถ้าไม่มี)
- ✅ ผู้ใช้ต้องพิมพ์ "Yes" เพื่อยืนยันก่อนเปิดฟอร์มเสมอ

## 📊 Data Structure

```typescript
interface Document {
  id: string
  documentType: string
  category: string
  status: 'pending' | 'sorted' | 'processed'
}
```

## 📝 Recent Updates (2026-02-05)

### ✅ Feature Update: เพิ่ม Acknowledgment Modal สำหรับทุกบริษัท
- **วันที่แก้ไข**: 2026-02-05
- **ส่วน/หน้าที่เกี่ยวข้อง**: Document Sorting Page (`src/pages/DocumentSorting.tsx`, `src/components/TaxInspection/AcknowledgmentModal.tsx`)
- **ระดับความรุนแรง**: Medium (Feature Enhancement)
- **คำอธิบายปัญหา**: 
  - หน้า Document Sorting ไม่มี acknowledgment modal เมื่อเลือกบริษัท
  - ผู้ใช้ต้องการให้แสดง acknowledgment modal สำหรับทุกบริษัทเมื่อกดปุ่ม "เลือกบริษัทนี้"
- **วิธีแก้ไข**: 
  1. **เพิ่ม Acknowledgment Modal**: เพิ่ม `AcknowledgmentModal` component ในหน้า Document Sorting
  2. **เพิ่ม State Management**: เพิ่ม state สำหรับ acknowledgment modal (opened, pendingBuildId, sections, record)
  3. **แก้ไข handleCompanyChange**: แทนที่จะตั้งค่า selectedBuild ทันที ให้ตั้งค่า pendingBuildId เพื่อ trigger query สำหรับดึงข้อมูล acknowledgment
  4. **เพิ่ม Query**: เพิ่ม useQuery เพื่อดึงข้อมูล acknowledgment จาก `monthly_tax_data` เมื่อมี pendingBuildId
  5. **เพิ่ม handleAcknowledgmentConfirm**: หลังจากยืนยันแล้วค่อยตั้งค่า selectedBuild และเปิดฟอร์ม
- **ไฟล์ที่แก้ไข**: 
  - `src/pages/DocumentSorting.tsx` - เพิ่ม acknowledgment modal และ logic
- **หมายเหตุ**: 
  - ตอนนี้ทุกบริษัทจะแสดง acknowledgment modal เมื่อกดปุ่ม "เลือกบริษัทนี้"
  - ผู้ใช้ต้องพิมพ์ "Yes" เพื่อยืนยันก่อนเปิดฟอร์มเสมอ
  - ระบบจะดึงข้อมูล acknowledgment จาก `monthly_tax_data` โดยใช้ `getByBuildYearMonth` API

---

**Last Updated**: 2026-02-05
