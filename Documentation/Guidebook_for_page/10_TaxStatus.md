# 📊 10. Tax Status Page (สถานะยื่นภาษี)

## 📋 Overview

หน้าติดตามสถานะการยื่นภาษี - แสดงรายการงานที่ผู้รับผิดชอบทำบัญชี (`accounting_responsible`) ต้องรับผิดชอบ

**Route**: `/tax-status`  
**Component**: `src/pages/TaxStatus.tsx`  
**Table Component**: `src/components/TaxStatus/TaxStatusTable.tsx`  
**Form Component**: `src/components/TaxInspection/TaxInspectionForm.tsx` (ใช้ร่วมกับหน้า Tax Inspection)

## 🔐 Access Control

- ✅ **admin** - สามารถดูและบันทึกข้อมูลได้ทุกอย่าง
- ✅ **data_entry_and_service** - สามารถดูและบันทึกข้อมูลที่ตนเองรับผิดชอบ (`accounting_responsible`)
- ✅ **service** - สามารถดูและบันทึกข้อมูลที่ตนเองรับผิดชอบ (`accounting_responsible`)

## ✨ Features

### 1. Summary Card (สรุปงานที่รับผิดชอบ)
- ✅ แสดงสรุปงานที่รับผิดชอบใน 4 cards:
  - **งานที่รับผิดชอบ - WHT**: นับจากสถานะ ภงด. (received_receipt, paid, sent_to_customer, not_submitted)
  - **งานที่รับผิดชอบ - VAT**: นับจากสถานะ ภพ.30 (received_receipt, paid, sent_to_customer) **เฉพาะบริษัทที่จดภาษีมูลค่าเพิ่ม** (`tax_registration_status = 'จดภาษีมูลค่าเพิ่ม'`)
  - **เปอร์เซ็นสถานะกระทบภาษี**: นับจาก monthly_tax_impact ที่มีค่า
  - **เปอร์เซ็นสถานะกระทบแบงค์**: นับจาก bank_impact ที่มีค่า
- ✅ Filter โดย `accounting_responsible` และ `tax_month` (ย้อนหลัง 1 เดือน)
- ✅ ดึงข้อมูลจาก API แทน hardcoded values
- ✅ VAT summary filter เฉพาะบริษัทที่จดภาษีมูลค่าเพิ่มเท่านั้น
- ✅ **Fixed Grid Layout**: ใช้ `cols={4}` แทน responsive breakpoints เพื่อให้แสดงผลคงที่ 4 คอลัมน์

### 2. Filter Section (ส่วนกรองข้อมูล) (FEATURE-007: ใช้ FilterSection แบบรวมร่วมกับหน้าตรวจภาษีและยื่นภาษี)
- ✅ **โหมดการแสดงผล**: มี 3 โหมด:
  - **ทั้งหมด**: แสดงข้อมูลทั้งหมด
  - **ภาษีหัก ณ ที่จ่าย**: แสดงข้อมูลทั้งหมด (เหมือนทั้งหมด)
  - **ภาษีมูลค่าเพิ่ม**: แสดงเฉพาะบริษัทที่จดทะเบียนภาษีมูลค่าเพิ่ม (`tax_registration_status = 'จดภาษีมูลค่าเพิ่ม'`)
- ✅ **เลือกประเภทการกรอง**: Build/ชื่อบริษัท หรือ วันที่และเวลา
- ✅ **Search Input**: ค้นหาด้วยหมายเลข Build หรือชื่อบริษัท (เมื่อเลือกกรองตาม Build)
- ✅ **Date Range Picker**: เมื่อเลือกกรองตามวันที่
  - DatePickerInput สำหรับวันที่เริ่มต้น
  - DatePickerInput สำหรับวันที่สิ้นสุด (มี minDate เป็นวันที่เริ่มต้น)
  - รองรับการล้างค่า (clearable)
- ✅ **Status Filters (MultiSelect - สามารถเลือกได้หลายสถานะ)**:
  - **สถานะ WHT** รองรับตัวเลือกสถานะทั้งหมด 12 รายการ
  - **สถานะ ภ.พ.30** รองรับตัวเลือกสถานะทั้งหมด 12 รายการ
  - **สถานะยอดชำระ ภ.พ.30**: มียอดชำระ, ไม่มียอดชำระ
  - **คุณสมบัติ MultiSelect**:
    - สามารถเลือกได้หลายสถานะพร้อมกัน
    - มี searchable เพื่อค้นหาสถานะที่ต้องการ
    - มี clearable เพื่อล้างการเลือกทั้งหมด
- ✅ **Active Filters Count Badge**: แสดงจำนวนตัวกรองที่ใช้งานอยู่
- ✅ **Filter Summary**: แสดงตัวกรองที่เลือกอยู่พร้อมปุ่มลบแต่ละตัว
  - แสดง Badge สำหรับแต่ละ filter ที่เลือก (รวมถึงโหมดการแสดงผล)
  - สามารถลบแต่ละ filter ได้โดยคลิกที่ปุ่ม X
  - แสดงวันที่ในรูปแบบ DD/MM/YYYY
- ✅ **Action Buttons**:
  - รีเฟรชข้อมูล
  - รีเซ็ตฟิลเตอร์ (ล้างการเลือกทั้งหมด, ปุ่มจะ disabled เมื่อไม่มี filter)
- ✅ **Filter Change Callback**: ส่ง filter values ไปยัง parent component เมื่อมีการเปลี่ยนแปลง
- ✅ **Component**: ใช้ `FilterSection` จาก `src/components/shared/FilterSection.tsx` (ใช้ร่วมกับหน้าตรวจภาษีและยื่นภาษี)

### 3. Tax Status Table (ตารางสถานะยื่นภาษี)
- ✅ แสดงรายการงานที่ผู้รับผิดชอบทำบัญชี (`accounting_responsible`) ต้องรับผิดชอบ
- ✅ Filter โดย `accounting_responsible` (employee_id ของผู้ล็อกอิน)
- ✅ Filter โดย `tax_registration_status` เมื่อเลือกโหมด "ภาษีมูลค่าเพิ่ม"
- ✅ แสดงข้อมูล:
  - Build Number (Sticky column - ตรึงเมื่อ scroll แนวนอน)
  - ชื่อบริษัท (Sticky column - ตรึงเมื่อ scroll แนวนอน)
  - วันที่สถานะ ภ.ง.ด. (PND Status Dates) - แสดง:
    - วันที่ส่งตรวจ ภ.ง.ด. (pnd_sent_for_review_date)
    - วันที่ส่งตรวจคืน ภ.ง.ด. (pnd_review_returned_date)
    - วันที่ส่งลูกค้า ภ.ง.ด. (pnd_sent_to_customer_date)
  - สถานะ ภงด. (PND Status) - รองรับสถานะทั้งหมด 12 รายการ รวมถึง "แก้ไข" (edit)
  - วันที่สถานะ ภ.พ.30 (PP30 Status Dates) - แสดง:
    - วันที่ส่งตรวจ ภ.พ.30 (pp30_sent_for_review_date)
    - วันที่ส่งตรวจคืน ภ.พ.30 (pp30_review_returned_date)
    - วันที่ส่งลูกค้า ภ.พ.30 (pp30_sent_to_customer_date)
  - สถานะ ภ.พ.30 (PP30 Status) - รองรับสถานะทั้งหมด 12 รายการ
  - สถานะยอดชำระ ภ.พ.30 (PP30 Payment Status) - แสดง Badge: "มียอดชำระ" (เขียว) หรือ "ไม่มียอดชำระ" (เทา) หรือ "-"
  - จำนวนยอดชำระ (PP30 Payment Amount) - แสดงตัวเลขพร้อม format ทศนิยม 2 ตำแหน่ง หรือ "-"
  - ข้อมูลผู้รับผิดชอบ (Responsible Person Information) - แสดง:
    - ผู้ตรวจภาษี (tax_inspection_responsible_first_name + nick_name)
    - พนักงานที่รับผิดชอบในการคีย์ (document_entry_responsible_first_name + nick_name)
    - พนักงานที่ยื่น WHT (wht_filer_current_employee_first_name/nick_name หรือ wht_filer_employee_first_name/nick_name)
    - พนักงานที่ยื่น VAT (vat_filer_current_employee_first_name/nick_name หรือ vat_filer_employee_first_name/nick_name)
  - จัดการ (ปุ่มเลือกบริษัทนี้)
- ✅ **Status Badges**: Color-coded badges สำหรับแต่ละสถานะ
- ✅ **Label Highlighting**: Label ทั้งหมดจะถูกไฮไลท์ด้วยสีส้ม (#ff6b35) และตัวหนา (bold)
- ✅ **Date Display**: แสดงวันที่ 3 ตัวในแต่ละคอลลัม (ส่งตรวจ, ส่งตรวจคืน, ส่งลูกค้า)
- ✅ **Name Format**: แสดงชื่อในรูปแบบ "ชื่อ (ชื่อเล่น)" โดยไม่แสดงนามสกุล
- ✅ **Error Handling**: แสดง error message ที่ชัดเจนเมื่อ Backend Server ไม่ได้รัน
- ✅ **Sticky Columns**: Build และ ชื่อบริษัท จะตรึงเมื่อ scroll แนวนอน
- ✅ **Full Width Layout**: Layout เต็มหน้าจอ (ไม่รองรับ mobile) เพื่อให้เห็นข้อมูลชัดเจนขึ้น
- ✅ **Scrollable Table**: มี horizontal scrollbar เมื่อข้อมูลกว้างเกินหน้าจอ
- ✅ **Data Refresh**: อัพเดทข้อมูลทันทีหลังจากบันทึกสำเร็จ

### 4. Tax Inspection Form Modal (ฟอร์มสถานะภาษีประจำเดือน)
- ✅ Full Screen Modal
- ✅ แสดงข้อมูลบริษัท (Company Information)
- ✅ แสดงข้อมูลภาษีรายเดือน (Monthly Tax Data)
- ✅ **Modal Header**:
  - แสดง Icon, Title "ฟอร์มสถานะภาษีประจำเดือน" และปุ่มรีเฟรชข้อมูล
  - **ปุ่มรีเฟรชข้อมูล** (FEATURE-006):
    - อยู่ด้านขวาของ title ใน header
    - เมื่อกดปุ่มรีเฟรช ระบบจะดึงข้อมูลใหม่จากฐานข้อมูลและรีเซ็ต form state ทั้งหมด
    - สถานะที่ผู้ใช้เปลี่ยนแต่ยังไม่ได้บันทึกจะถูก reset กลับไปเป็นสถานะจากฐานข้อมูล
    - แสดง loading state และ notification เมื่อรีเฟรชสำเร็จหรือเกิดข้อผิดพลาด
- ✅ **สถานะที่เลือกได้**: **ทุกสถานะ (13 สถานะ)** - หน้าสถานะยื่นภาษีสามารถเลือกได้ทุกสถานะ
- ✅ **การเรียงลำดับสถานะ**: สถานะที่เลือกได้จะแสดงอยู่ด้านบน สถานะที่เลือกไม่ได้จะแสดงอยู่ด้านล่าง
- ✅ **Backward Compatibility**: ระบบจะแปลงสถานะเก่า (`receipt`, `inquiry`, `review`, `edit`) เป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล
- ✅ **Auto-timestamp Logic**:
  - `pp30_sent_for_review_date`: อัพเดท timestamp ทุกครั้งที่เลือกสถานะ "รอตรวจ" หรือ "รอตรวจอีกครั้ง"
  - `pp30_review_returned_date`: อัพเดท timestamp เมื่อเปลี่ยนสถานะจาก "รอตรวจ" เป็นสถานะอื่น
- ✅ **VAT Tab Restriction**: ป้องกันการเข้าถึงแถบ VAT เมื่อบริษัทไม่ได้จดภาษีมูลค่าเพิ่ม
- ✅ **Date Format**: แสดงวันที่ในรูปแบบ `DD/MM/YYYY`
- ✅ **Status Translation**: แปลงสถานะ "EDIT" เป็น "แก้ไข" (ภาษาไทย)
- ✅ **Data Refresh**: Refetch ข้อมูลทันทีหลังจากบันทึกสำเร็จ

### 5. Error Handling
- ✅ Network Error Detection: ตรวจสอบ `ERR_CONNECTION_REFUSED`, `ERR_SOCKET_NOT_CONNECTED`
- ✅ แสดง error message ที่ชัดเจน: "ไม่สามารถเชื่อมต่อกับ Backend Server ได้ กรุณาตรวจสอบว่า Backend Server รันอยู่ที่ http://localhost:3001"

## 📊 Data Structure

```typescript
interface TaxStatusRecord {
  id: string
  build: string
  companyName: string
  // PND Dates
  pndSentForReviewDate: string | null // วันที่ส่งตรวจ ภ.ง.ด.
  pndReviewReturnedDate: string | null // วันที่ส่งตรวจคืน ภ.ง.ด.
  pndSentToCustomerDate: string | null // วันที่ส่งลูกค้า ภ.ง.ด.
  pndStatus: 'received_receipt' | 'paid' | 'sent_to_customer' | 'draft_completed' | 'passed' | 'pending_review' | 'pending_recheck' | 'draft_ready' | 'needs_correction' | 'edit' | 'inquire_customer' | 'additional_review' | 'not_started' | null
  // PP30 Dates
  pp30SentForReviewDate: string | null // วันที่ส่งตรวจ ภ.พ.30
  pp30ReviewReturnedDate: string | null // วันที่ส่งตรวจคืน ภ.พ.30
  pp30SentToCustomerDate: string | null // วันที่ส่งลูกค้า ภ.พ.30
  pp30Status: 'received_receipt' | 'paid' | 'sent_to_customer' | 'draft_completed' | 'passed' | 'pending_review' | 'pending_recheck' | 'draft_ready' | 'needs_correction' | 'inquire_customer' | 'additional_review' | 'not_submitted' | 'not_started' | null
  pp30PaymentStatus?: string | null // 'has_payment' | 'no_payment'
  pp30PaymentAmount?: number | null // จำนวนยอดชำระ ภ.พ.30
  performer: string // ข้อความรวมของผู้ทำทั้งหมด (แสดงเป็น bullet points)
  performers: {
    taxInspector?: string | null // ผู้ตรวจภาษี
    documentEntry?: string | null // พนักงานที่รับผิดชอบในการคีย์
    whtFiler?: string | null // พนักงานที่ยื่น WHT
    vatFiler?: string | null // พนักงานที่ยื่น VAT
  }
}
```

## 🔄 Data Flow

### การดึงข้อมูล
- **API**: `GET /api/monthly-tax-data`
- **Filter**: `accounting_responsible` = `employee_id` ของผู้ล็อกอิน
- **Query Key**: `['monthly-tax-data', 'tax-status', page, limit, employeeId, currentTaxMonth.year, currentTaxMonth.month]`

### การบันทึกข้อมูล
- **API**: `PUT /api/monthly-tax-data/:id`
- **Permission**: Admin หรือ Responsible User (`accounting_responsible`)
- **Auto-refetch**: หลังจากบันทึกสำเร็จ ระบบจะ refetch ข้อมูลทันที

## 📝 Recent Updates (2026-02-02)

### ✅ BUG-120: แปลงสถานะ "EDIT" เป็นภาษาไทย
- เพิ่ม `case 'edit'` ใน `getStatusColor` และ `getStatusLabel` functions
- แสดงเป็น "แก้ไข" (สีแดง) แทน "EDIT"

### ✅ BUG-123: Error Message ไม่ชัดเจนเมื่อ Backend Server ไม่ได้รัน
- เพิ่มการตรวจสอบ network error
- แสดง error message ที่ชัดเจน

### ✅ BUG-124: pp30_sent_for_review_date ไม่ถูกอัพเดทเมื่อสถานะเป็น "รอตรวจ" หรือ "รอตรวจอีกครั้ง"
- เปลี่ยนลำดับการตรวจสอบ: ตรวจสอบสถานะก่อน `formValues.pp30_sent_date`
- อัพเดท timestamp ทุกครั้งที่เลือกสถานะ "รอตรวจ" หรือ "รอตรวจอีกครั้ง"

### ✅ BUG-126: หน้าเว็บไม่อัพเดทข้อมูล pp30_sent_for_review_date หลังจากบันทึกสำเร็จ
- เพิ่ม `refetchTaxData()` หลังจากบันทึกสำเร็จ
- เพิ่ม `refetchOnMount` และ `refetchOnWindowFocus`

### ✅ BUG-127: Pagination แสดงจำนวนรายการไม่ถูกต้อง
- แก้ไขการใช้ค่า hardcoded `totalItems = 121` เป็นค่าจาก API response
- ส่ง `page` และ `limit` props จาก `TaxStatus.tsx` ไปยัง `TaxStatusTable`
- เพิ่ม `onPaginationChange` callback เพื่อส่ง pagination data กลับไปยัง parent

### ✅ Feature Update: แสดงข้อมูลผู้ทำหลายคนในคอลลัม "ข้อมูลผู้รับผิดชอบ"
- เปลี่ยนหัวคอลลัมจาก "ผู้ทำ" เป็น "ข้อมูลผู้รับผิดชอบ"
- แก้ไขการแสดงผลในคอลลัม "ข้อมูลผู้รับผิดชอบ" ให้แสดงข้อมูลหลายคน:
  - ผู้ตรวจภาษี (tax_inspection_responsible_first_name + nick_name)
  - พนักงานที่รับผิดชอบในการคีย์ (document_entry_responsible_first_name + nick_name)
  - พนักงานที่ยื่น WHT (wht_filer_current_employee_first_name/nick_name หรือ wht_filer_employee_first_name/nick_name)
  - พนักงานที่ยื่น VAT (vat_filer_current_employee_first_name/nick_name หรือ vat_filer_employee_first_name/nick_name)
- แสดงชื่อในรูปแบบ "ชื่อ (ชื่อเล่น)" โดยไม่แสดงนามสกุล
- แสดงเป็น bullet points เพื่อให้อ่านง่าย
- แสดงเฉพาะข้อมูลที่มีค่า (ไม่แสดง null หรือ undefined)
- Backend API ส่ง first_name และ nick_name มาเพิ่มเติมเพื่อรองรับการแสดงผล
- **Label Highlighting**: Label ทั้งหมด (วันที่ส่งตรวจ ภ.ง.ด., วันที่ส่งตรวจคืน ภ.ง.ด., วันที่ส่งลูกค้า ภ.ง.ด., วันที่ส่งตรวจ ภ.พ.30, วันที่ส่งตรวจคืน ภ.พ.30, วันที่ส่งลูกค้า ภ.พ.30, ผู้ตรวจภาษี, พนักงานที่รับผิดชอบในการคีย์, พนักงานที่ยื่น WHT, พนักงานที่ยื่น VAT) จะถูกไฮไลท์ด้วยสีส้ม (#ff6b35) และตัวหนา (bold) เพื่อให้อ่านง่าย
- Pagination จะแสดงจำนวนรายการที่ถูกต้องตามข้อมูลจริงจาก API

### ✅ Feature Update: แสดงวันที่สถานะ ภ.ง.ด. และ ภ.พ.30 แบบละเอียด
- เปลี่ยนหัวคอลลัม "วันที่ยื่น ภงด." เป็น "วันที่สถานะ ภ.ง.ด."
- เปลี่ยนหัวคอลลัม "วันที่ยื่น ภ.พ. 30" เป็น "วันที่สถานะ ภ.พ.30"
- แสดงวันที่ 3 ตัวในแต่ละคอลลัม:
  - **วันที่สถานะ ภ.ง.ด.**: แสดงวันที่ส่งตรวจ ภ.ง.ด., วันที่ส่งตรวจคืน ภ.ง.ด., วันที่ส่งลูกค้า ภ.ง.ด.
  - **วันที่สถานะ ภ.พ.30**: แสดงวันที่ส่งตรวจ ภ.พ.30, วันที่ส่งตรวจคืน ภ.พ.30, วันที่ส่งลูกค้า ภ.พ.30
- แสดงเฉพาะวันที่ที่มีค่า (ไม่แสดง "-" ถ้ามีข้อมูล)
- แสดงเป็นหลายบรรทัด (ใช้ `whiteSpace: 'pre-line'`)
- Label ทั้งหมดจะถูกไฮไลท์ด้วยสีส้ม (#ff6b35) และตัวหนา (bold)

### ✅ BUG-128: Notification สำหรับ PP30 ส่งไปที่ผู้ทำบัญชีแทนผู้รับผิดชอบตรวจ
- แก้ไข PP30 notification ให้ส่งไปที่ `tax_inspection_responsible` แทน `accounting_responsible`
- เมื่อผู้ทำบัญชีส่งสถานะ "รอตรวจ" หรือ "รอตรวจอีกครั้ง" → notification จะถูกส่งไปที่ผู้ตรวจ (`tax_inspection_responsible`)
- ทั้ง PND และ PP30 จะส่ง notification ไปที่ `tax_inspection_responsible` (สอดคล้องกัน)

### ✅ BUG-129: ระบบไม่ส่งสถานะทั้งสองและไม่อัพเดท timestamp ทั้งสองตัวเมื่อสถานะเป็น "รอตรวจ" หรือ "รอตรวจอีกครั้ง"
- แก้ไขให้ส่งสถานะทั้งสอง (`pnd_status` และ `pp30_status`) ไปยัง backend เสมอเมื่อกดบันทึก ไม่ว่าจะอยู่แถบไหน
- แก้ไข logic การอัพเดท timestamp: เมื่อสถานะเป็น "รอตรวจ" หรือ "รอตรวจอีกครั้ง" จะอัพเดท timestamp ทั้งสองตัว (`pnd_sent_for_review_date` และ `pp30_sent_for_review_date`) ทุกครั้งที่กดบันทึก
- Backend จะใช้ `pp30_status` เพื่อตรวจสอบสถานะและอัพเดท timestamp และสร้าง notification

### ✅ Feature Update: อัพเดทการนับข้อมูลใน Summary Card
- **งานที่รับผิดชอบ - WHT**: นับจากสถานะ ภงด. (pnd_status) โดยนับสถานะ:
  - รับใบเสร็จ (received_receipt)
  - ชำระแล้ว (paid)
  - ส่งลูกค้าแล้ว (sent_to_customer)
  - ไม่มียื่น (not_submitted)
- **งานที่รับผิดชอบ - VAT**: นับจากสถานะ ภพ.30 (pp30_status) โดยนับสถานะ:
  - รับใบเสร็จ (received_receipt): pp30_filing_response IS NOT NULL AND pp30_sent_to_customer_date IS NULL
  - ชำระแล้ว (paid): pp30_filing_response IS NOT NULL
  - ส่งลูกค้าแล้ว (sent_to_customer): pp30_sent_to_customer_date IS NOT NULL
  - **⚠️ สำคัญ**: นับเฉพาะบริษัทที่จดภาษีมูลค่าเพิ่ม (`tax_registration_status = 'จดภาษีมูลค่าเพิ่ม'`)
- **เปอร์เซ็นสถานะกระทบภาษี**: นับจาก `monthly_tax_impact` ที่มีค่า (ไม่ใช่ null หรือ empty string)
- **เปอร์เซ็นสถานะกระทบแบงค์**: นับจาก `bank_impact` ที่มีค่า (ไม่ใช่ null หรือ empty string)
- Summary Card ดึงข้อมูลจาก API แทน hardcoded values
- Filter โดย `accounting_responsible` และ `tax_month` เหมือนกับ TaxStatusTable

### ✅ Feature Update: เพิ่มโหมดการกรองข้อมูล
- เพิ่มโหมดการแสดงผล 3 โหมด:
  - **ทั้งหมด**: แสดงข้อมูลทั้งหมด (ไม่ filter)
  - **ภาษีหัก ณ ที่จ่าย**: แสดงข้อมูลทั้งหมด (ไม่ filter)
  - **ภาษีมูลค่าเพิ่ม**: แสดงเฉพาะบริษัทที่จดทะเบียนภาษีมูลค่าเพิ่ม (`tax_registration_status = 'จดภาษีมูลค่าเพิ่ม'`)
- Backend API รองรับการ filter โดย `tax_registration_status`
- Filter mode จะถูกส่งไปยัง API และใช้ในการ filter ข้อมูล

**ดูรายละเอียดเพิ่มเติม**: `Documentation/Agent_cursor_ai/MONTHLY_TAX_STATUS_FORM_FLOW.md`

### ✅ Feature Update: ปรับ Layout ให้เต็มหน้าจอและยกเลิกการรองรับ Mobile (2026-02-02)
- **วันที่แก้ไข**: 2026-02-02
- **ส่วน/หน้าที่เกี่ยวข้อง**: Tax Status Page (`src/pages/TaxStatus.tsx`, `src/components/TaxStatus/TaxStatusTable.tsx`, `src/components/TaxStatus/SummaryCard.tsx`)
- **ระดับความรุนแรง**: Low (UI/UX Enhancement)
- **คำอธิบายปัญหา**: 
  - Layout ถูกจำกัดด้วย Container size="xl" ทำให้มี whitespace ด้านข้างมาก
  - Summary Card ใช้ responsive breakpoints ทำให้แสดงผลไม่สม่ำเสมอบนหน้าจอขนาดใหญ่
  - ไม่รองรับ mobile อยู่แล้ว จึงควรปรับให้เต็มหน้าจอเพื่อให้เห็นข้อมูลชัดเจนขึ้น
- **วิธีแก้ไข**: 
  1. **ปรับ Container**: เปลี่ยนจาก `<Container size="xl">` เป็น `<Container fluid px="xl" py="md">` เพื่อให้ layout เต็มหน้าจอ
  2. **ปรับ Summary Card**: เปลี่ยนจาก `cols={{ base: 2, sm: 2, lg: 4 }}` เป็น `cols={4}` เพื่อให้แสดงผลคงที่ 4 คอลัมน์
  3. **Sticky Columns**: Build และ ชื่อบริษัท จะตรึงเมื่อ scroll แนวนอน (z-index: 15 สำหรับ header, 10 สำหรับ body)
- **ไฟล์ที่แก้ไข**: 
  - `src/pages/TaxStatus.tsx`
  - `src/components/TaxStatus/SummaryCard.tsx`
- **หมายเหตุ**: 
  - Layout ตอนนี้เต็มหน้าจอแล้ว (full width)
  - ไม่มี whitespace ด้านข้าง
  - เหมาะสำหรับการใช้งานบน desktop เท่านั้น (ไม่รองรับ mobile)
  - ข้อมูลแสดงชัดเจนขึ้น

---

## 🎨 UI/UX Features

### Label Highlighting
- Label ทั้งหมดในคอลลัม "วันที่สถานะ ภ.ง.ด.", "วันที่สถานะ ภ.พ.30", และ "ข้อมูลผู้รับผิดชอบ" จะถูกไฮไลท์ด้วย:
  - สีส้ม (#ff6b35) - สีหลักของโปรเจกต์
  - ตัวหนา (bold) - `fw={700}`
- Label ที่ถูกไฮไลท์:
  - วันที่ส่งตรวจ ภ.ง.ด.:
  - วันที่ส่งตรวจคืน ภ.ง.ด.:
  - วันที่ส่งลูกค้า ภ.ง.ด.:
  - วันที่ส่งตรวจ ภ.พ.30:
  - วันที่ส่งตรวจคืน ภ.พ.30:
  - วันที่ส่งลูกค้า ภ.พ.30:
  - ผู้ตรวจภาษี:
  - พนักงานที่รับผิดชอบในการคีย์:
  - พนักงานที่ยื่น WHT:
  - พนักงานที่ยื่น VAT:

### Date Format
- วันที่แสดงในรูปแบบ `DD/MM/YYYY HH:mm` (เช่น 02/02/2026 13:55)
- ใช้ `dayjs` สำหรับการ format วันที่

### Name Format
- แสดงชื่อในรูปแบบ "ชื่อ (ชื่อเล่น)" โดยไม่แสดงนามสกุล
- ใช้ `first_name` และ `nick_name` จาก backend API
- ถ้าไม่มี `nick_name` จะแสดงแค่ `first_name`

---

## 📝 Recent Updates (2026-02-02)

### ✅ Feature Update: หน้าสถานะยื่นภาษีสามารถเลือกได้ทุกสถานะ
- **ปัญหา**: หน้าสถานะยื่นภาษีมีข้อจำกัดในการเลือกสถานะบางสถานะ
- **แก้ไข**: 
  - เพิ่มสถานะทั้งหมด (13 สถานะ) เข้าไปใน `allowedStatusesByPage.taxStatus`
  - หน้าสถานะยื่นภาษีสามารถเลือกได้ทุกสถานะ: `received_receipt`, `paid`, `sent_to_customer`, `draft_completed`, `passed`, `pending_review`, `pending_recheck`, `draft_ready`, `needs_correction`, `inquire_customer`, `additional_review`, `not_started`, `not_submitted`
- **ผลลัพธ์**:
  - ✅ ผู้ทำบัญชีสามารถเลือกสถานะได้ครบทุกสถานะ
  - ✅ มีความยืดหยุ่นในการจัดการสถานะมากขึ้น

### ✅ Feature Update: เรียงลำดับสถานะให้สถานะที่เลือกได้อยู่ด้านบน
- **ปัญหา**: สถานะที่เลือกได้และเลือกไม่ได้ปนกันใน dropdown ทำให้ใช้งานยาก
- **แก้ไข**: 
  - แยกสถานะที่เลือกได้และเลือกไม่ได้
  - เรียงให้สถานะที่เลือกได้อยู่ด้านบน สถานะที่เลือกไม่ได้อยู่ด้านล่าง
- **ผลลัพธ์**:
  - ✅ ผู้ใช้เห็นสถานะที่เลือกได้ก่อน ทำให้ใช้งานง่ายขึ้น
  - ✅ UX ดีขึ้น

### ✅ Feature Update: ลบสถานะเก่า (ผู้ตรวจภาษี) ออกจาก dropdown
- **ปัญหา**: สถานะเก่า (`receipt`, `inquiry`, `review`, `edit`) แสดงใน dropdown ทำให้สับสน
- **แก้ไข**: 
  - ลบสถานะเก่าออกจาก `allStatusOptions`
  - เพิ่มการแปลงสถานะเก่าเป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล
- **ผลลัพธ์**:
  - ✅ Dropdown แสดงเฉพาะสถานะใหม่ (13 สถานะ)
  - ✅ ระบบยังรองรับข้อมูลเก่าในฐานข้อมูล (backward compatibility)

### 2026-02-03 (FEATURE-007: ปรับการกรองข้อมูลให้ตรงกันทั้ง 3 หน้า)
- ✅ สร้าง FilterSection แบบรวมที่ใช้ร่วมกันทั้ง 3 หน้า (ตรวจภาษี, สถานะยื่นภาษี, ยื่นภาษี)
- ✅ การกรองข้อมูลตรงกันทั้ง 3 หน้า: โหมดการแสดงผล, ประเภทการกรอง, สถานะ WHT, สถานะ ภ.พ.30, สถานะยอดชำระ ภ.พ.30
- ✅ ใช้ FilterSection จาก `src/components/shared/FilterSection.tsx` แทน FilterSection แยกของแต่ละหน้า

### 2026-02-03 (FEATURE-006: เพิ่มปุ่มรีเฟรชข้อมูล)
- ✅ เพิ่มปุ่ม "รีเฟรชข้อมูล" ใน header ของฟอร์มสถานะภาษีประจำเดือน
- ✅ เมื่อกดปุ่มรีเฟรช ระบบจะดึงข้อมูลใหม่จากฐานข้อมูลและรีเซ็ต form state ทั้งหมด
- ✅ สถานะที่ผู้ใช้เปลี่ยนแต่ยังไม่ได้บันทึกจะถูก reset กลับไปเป็นสถานะจากฐานข้อมูล
- ✅ แสดง loading state และ notification เมื่อรีเฟรชสำเร็จหรือเกิดข้อผิดพลาด

## 📝 Recent Updates (2026-02-05)

### ✅ Feature Update: Acknowledgment Modal สำหรับทุกบริษัท
- **วันที่แก้ไข**: 2026-02-05
- **ส่วน/หน้าที่เกี่ยวข้อง**: Tax Status Page (`src/pages/TaxStatus.tsx`, `src/components/TaxInspection/AcknowledgmentModal.tsx`)
- **ระดับความรุนแรง**: Medium (Feature Enhancement)
- **คำอธิบายปัญหา**: 
  - ระบบเดิมจะแสดง acknowledgment modal เฉพาะเมื่อบริษัทมีข้อมูล acknowledgment เท่านั้น
  - ผู้ใช้ต้องการให้แสดง acknowledgment modal สำหรับทุกบริษัทเมื่อกดปุ่ม "เลือกบริษัทนี้"
- **วิธีแก้ไข**: 
  1. **แก้ไข handleSelectCompany**: ลบการตรวจสอบ `hasAcknowledgmentData(record)` ออก และแสดง modal เสมอ
  2. **แก้ไข AcknowledgmentModal**: เปลี่ยนข้อความจาก "บริษัทนี้มีข้อมูลในส่วนต่อไปนี้..." เป็น "กรุณาอ่านข้อมูลในส่วนต่อไปนี้และยืนยันว่าทราบก่อนเปิดฟอร์มสถานะภาษีประจำเดือน"
  3. **แสดงข้อมูล acknowledgment**: ถ้ามีข้อมูล acknowledgment จะแสดงข้อมูล ถ้าไม่มีจะแสดง modal ว่าง (แต่ยังต้องพิมพ์ "Yes" เพื่อยืนยัน)
- **ไฟล์ที่แก้ไข**: 
  - `src/pages/TaxStatus.tsx` - แก้ไข `handleSelectCompany` function
  - `src/components/TaxInspection/AcknowledgmentModal.tsx` - แก้ไขข้อความ
- **หมายเหตุ**: 
  - ตอนนี้ทุกบริษัทจะแสดง acknowledgment modal เมื่อกดปุ่ม "เลือกบริษัทนี้"
  - ผู้ใช้ต้องพิมพ์ "Yes" เพื่อยืนยันก่อนเปิดฟอร์มเสมอ
  - ถ้ามีข้อมูล acknowledgment จะแสดงข้อมูล ถ้าไม่มีจะแสดง modal ว่าง

**Last Updated**: 2026-02-05
