# 🛒 13. Accounting Marketplace Page

## 📋 Overview

หน้าตลาดกลางผู้ทำบัญชี - ระบบขายงานผู้ทำบัญชีที่รับผิดชอบให้กับพนักงานคนอื่น

**Route**: `/accounting-marketplace`  
**Component**: `src/pages/AccountingMarketplace.tsx`

## 🔐 Access Control

- ✅ **admin**
- ✅ **data_entry_and_service**
- ✅ **audit**
- ✅ **service**

## ✨ Features

### 1. งานที่ขายได้ (Available Jobs)
- ✅ แสดงรายการงานที่ขายได้ในเดือนภาษีปัจจุบัน
- ✅ Filter: ค้นหาด้วย Build หรือชื่อบริษัท
- ✅ แสดงข้อมูล: Build, ชื่อบริษัท, เดือนภาษี, ผู้ขาย, ราคา
- ✅ ปุ่ม "ซื้อ" สำหรับซื้องาน
- ✅ Exclude งานที่ผู้ใช้เป็น `accounting_responsible` อยู่แล้ว

### 2. งานที่ฉันขาย (My Listings)
- ✅ แสดงรายการงานที่ฉันขาย (ทุกสถานะ)
- ✅ Filter: ค้นหาด้วย Build หรือชื่อบริษัท, กรองตามสถานะ
- ✅ แสดงข้อมูล: Build, ชื่อบริษัท, เดือนภาษี, ราคา, สถานะ, ผู้ซื้อ, วันที่ขาย
- ✅ ปุ่ม "ยกเลิก" สำหรับรายการที่ status=available
- ✅ ปุ่ม "ขายงาน" สำหรับสร้างรายการขายงานใหม่
- ✅ Modal สำหรับสร้างรายการขาย:
  - เลือก Build (dropdown)
  - แสดงเดือนภาษีปัจจุบัน (read-only)
  - ระบุราคา (NumberInput, min=150 บาท)

### 3. งานที่ฉันซื้อ (Purchased Jobs)
- ✅ แสดงรายการงานที่ฉันซื้อ
- ✅ Filter: ค้นหาด้วย Build หรือชื่อบริษัท, กรองตามเดือนภาษี
- ✅ แสดงข้อมูล: Build, ชื่อบริษัท, เดือนภาษี, ผู้ขาย, ราคา, วันที่ซื้อ

### 4. ประวัติการซื้อขาย (Transaction History)
- ✅ แสดงประวัติการซื้อขายทั้งหมด (ทั้งขายและซื้อ)
- ✅ Filter: ค้นหาด้วย Build หรือชื่อบริษัท, กรองตามประเภท (ขาย/ซื้อ), กรองตามเดือนภาษี
- ✅ แสดงข้อมูล: Build, ชื่อบริษัท, เดือนภาษี, ประเภท (ขาย/ซื้อ), ราคา, สถานะ, วันที่ทำรายการ

### 5. รายได้รายเดือน (Monthly Income)
- ✅ แสดงรายได้รายเดือนของคนที่ซื้องาน
- ✅ การ์ดสรุป: รายได้รวมทั้งหมด, จำนวนงานรวม, สรุปรายได้แต่ละเดือน (จาก API buyer-income)
- ✅ ตารางรายละเอียด: ใช้ข้อมูลจาก **งานที่ฉันซื้อ** (getPurchasedJobs) แสดงทีละงาน
- ✅ คอลัมน์ตาราง: **Build**, **ชื่อบริษัท**, เดือนภาษี, **ชื่อผู้ขาย**, รายได้
- ✅ การแสดงผลเงิน: จำนวน 0 แสดงเป็น "-"; ใช้ NumberFormatter พร้อม decimalScale=2 เพื่อป้องกันการแสดงผิด (เช่น 0500.00)

## 🎨 UI/UX Guidelines

### Layout Structure

```
┌─────────────────────────────────────┐
│  Header (Orange)                    │
│  - Title: ตลาดกลางผู้ทำบัญชี       │
│  - Tax Month Info                   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  Tabs                               │
│  - งานที่ขายได้                    │
│  - งานที่ฉันขาย                     │
│  - งานที่ฉันซื้อ                    │
│  - ประวัติการซื้อขาย                │
│  - รายได้รายเดือน                   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│  Content (Table/Cards)               │
└─────────────────────────────────────┘
```

### Design Elements

- **Primary Color**: Orange (#ff6b35, #ff8c42)
- **Status Badges**:
  - ขายได้: Blue
  - ขายแล้ว: Green
  - ยกเลิก: Gray
- **Price Display**: Orange, formatted with thousand separator
- **Tables**: Clean design, hover effects, responsive

## 📊 Data Flow

### Creating a Listing (สร้างรายการขายงาน)

1. User clicks "ขายงาน" button
2. Modal opens with form:
   - Build selector (dropdown)
   - Tax month (read-only, current tax month)
   - Price input (min: 150 บาท)
3. User submits form
4. Backend validates:
   - User is `accounting_responsible` for this build/month
   - Price >= 150 (ฝั่ง Frontend; ฝั่ง Backend ควรอัพเดทให้สอดคล้อง)
   - Tax month = current tax month
   - No existing available listing for this build/month
5. Backend creates listing
6. Frontend refreshes "งานที่ฉันขาย" tab

### Purchasing a Listing (ซื้องาน)

1. User clicks "ซื้อ" button on available job
2. Backend validates:
   - Listing status = 'available'
   - User has correct role (admin, data_entry_and_service, audit, service)
   - User is not seller
3. Backend updates:
   - Listing status = 'sold'
   - `sold_to_employee_id` = buyer employee_id
   - `sold_at` = current timestamp
   - `accounting_responsible` in `monthly_tax_data`
   - `accounting_responsible` in `work_assignments`
4. Backend sends notification to seller
5. Frontend refreshes all tabs

### Cancelling a Listing (ยกเลิกรายการขาย)

1. User clicks "ยกเลิก" button on their listing
2. Confirmation dialog appears
3. Backend validates:
   - Listing status = 'available'
   - User is seller
4. Backend updates:
   - Listing status = 'cancelled'
   - `cancelled_at` = current timestamp
5. Frontend refreshes "งานที่ฉันขาย" tab

## ⏱️ Timestamp (วันที่/เวลา)

- **Backend**: API ส่งค่าวันที่เป็น **ISO 8601 UTC** (ลงท้ายด้วย `Z`) เช่น `2026-02-04T13:52:17.000Z` เพื่อให้ Frontend แปลงเป็นเวลาท้องถิ่นได้ถูกต้อง
- **Frontend**: ใช้ `dayjs.utc(dateStr).local()` สำหรับแสดงผล (DD/MM/YYYY HH:mm)
- **ไฟล์ที่เกี่ยวข้อง**: `backend/routes/accounting-marketplace.js` (formatDateForResponse), components ที่ใช้ formatDate

## 🔄 API Endpoints

### GET /api/accounting-marketplace
- ดึงรายการงานที่ขายได้
- Filter: `status=available`, `tax_year`, `tax_month`, `build`, `search`
- Exclude jobs where user is already `accounting_responsible`

### GET /api/accounting-marketplace/my-listings
- ดึงรายการงานที่ฉันขาย
- Filter: `seller_employee_id` = logged-in user's employee_id
- Shows all statuses (available, sold, cancelled)

### GET /api/accounting-marketplace/purchased
- ดึงรายการงานที่ฉันซื้อ
- Filter: `sold_to_employee_id` = logged-in user's employee_id, `status=sold`

### GET /api/accounting-marketplace/history
- ประวัติการซื้อขาย
- Filter: `seller_employee_id` หรือ `sold_to_employee_id` = logged-in user's employee_id
- Shows all statuses

### GET /api/accounting-marketplace/buyer-income
- รายได้รายเดือนของคนที่ซื้องาน
- Group by: `sold_to_employee_id`, `tax_year`, `tax_month`
- Sum: `price` as `total_income`

### POST /api/accounting-marketplace
- สร้างรายการขายงาน
- Validation:
  - `seller_employee_id` = `accounting_responsible` in `monthly_tax_data` or `work_assignments`
  - `price >= 150` (แนะนำให้ Backend อัพเดทให้ตรงกับ Frontend)
  - Tax month = current tax month
  - No existing available listing for this build/month

### POST /api/accounting-marketplace/:id/purchase
- ซื้องาน
- Validation:
  - `status=available`
  - Buyer has correct role
  - Buyer is not seller
- Updates `accounting_responsible` in `monthly_tax_data` and `work_assignments`
- Sends notification to seller

### POST /api/accounting-marketplace/:id/cancel
- ยกเลิกรายการขาย
- Validation:
  - `status=available`
  - User is seller

## 🔔 Notification System

### Notification Type: `accounting_marketplace_sold`

**Trigger**: เมื่อมีการซื้องาน

**Recipient**: Seller (ผู้ขาย)

**Message**: "บริษัท {company_name} ({build}) - เดือน {month}/{year} ถูกซื้อโดย {buyer_name}"

**Link**: `/accounting-marketplace/my-listings`

**Metadata**:
- `build`: Build code
- `company_name`: ชื่อบริษัท
- `tax_year`: ปีภาษี
- `tax_month`: เดือนภาษี
- `buyer_employee_id`: Employee ID ของผู้ซื้อ
- `buyer_name`: ชื่อผู้ซื้อ

## 📝 Business Rules

### การขายงาน
- ✅ ผู้ขายต้องเป็น `accounting_responsible` ในเดือนภาษีปัจจุบัน
- ✅ ราคาขั้นต่ำ (Frontend): 150 บาท
- ✅ สามารถขายได้เฉพาะเดือนภาษีปัจจุบัน (ย้อนหลัง 1 เดือนจากเดือนปฏิทินปัจจุบัน)
- ✅ ไม่สามารถขายงานซ้ำในเดือนเดียวกัน (UNIQUE constraint)

### การซื้องาน
- ✅ ผู้ซื้อต้องมี role: `admin`, `data_entry_and_service`, `audit`, `service`
- ✅ ไม่สามารถซื้องานที่ตัวเองขายได้
- ✅ เมื่อซื้อสำเร็จ ระบบจะอัพเดท `accounting_responsible` อัตโนมัติ

### การยกเลิกรายการ
- ✅ สามารถยกเลิกได้เฉพาะรายการที่ `status=available`
- ✅ ผู้ยกเลิกต้องเป็น seller

## 🗂️ Components

### AvailableJobsTable
- แสดงรายการงานที่ขายได้
- Filter: search
- Actions: ปุ่ม "ซื้อ"

### MyListingsTable
- แสดงรายการงานที่ฉันขาย
- การ์ดสรุปเดือนภาษี: รวมเงินที่ขายได้ในเดือนนี้ (ใช้ Number(listing.price) ตอนรวม เพื่อป้องกันการแสดงผิดเป็น "0500.00")
- Filter: status, search
- Actions: ปุ่ม "ยกเลิก" (ถ้า status=available)

### PurchasedJobsTable
- แสดงรายการงานที่ฉันซื้อ
- Filter: search

### TransactionHistoryTable
- แสดงประวัติการซื้อขาย
- Filter: type (sell/buy), search

### MonthlyIncomeTable
- แสดงรายได้รายเดือน
- สรุปจาก getBuyerIncome; ตารางรายละเอียดจาก getPurchasedJobs (มี Build, ชื่อบริษัท, ชื่อผู้ขาย)
- คำนวณยอดด้วย Number(...) เพื่อป้องกัน string concatenation; แสดงเงินด้วย decimalScale=2

### CreateListingModal
- Modal สำหรับสร้างรายการขายงาน
- Fields: Build (Select), Tax Month (read-only), Price (NumberInput, min=150 บาท)

## 🔗 Related Pages

- **สถานะยื่นภาษี** (`/tax-status`) - ใช้ `accounting_responsible` filter
- **จัดงานรายเดือน** (`/work-assignment`) - กำหนด `accounting_responsible` เดิม

## 📚 Related Documentation

- **Database Schema**: `Documentation/Database/schema.md` - `accounting_marketplace_listings` table
- **API Documentation**: `Documentation/API/API_INDEX.md` - Accounting Marketplace endpoints
- **Migration**: `Documentation/Database/migrations/032_create_accounting_marketplace_listings_table.sql`

---

**Last Updated**: 2026-02-04
