# 📅 04. Leave Management Page (ลางาน/WFH)

## 📋 Overview

หน้าจัดการการลาและ Work from Home (WFH) - ระบบจัดการการลางานและการทำงานจากบ้านของพนักงาน

**Route**: `/leave`  
**Component**: `src/pages/LeaveManagement.tsx`

**Reference**: 
- `Documentation/Database/MyDatabase/Employy_data_WFH _&_ Leave_work.md`
- `Documentation/Database/MyDatabase/LEAVE_WFH_DATABASE_DESIGN.md`

---

## 🔐 Access Control

### Role Permissions

| Role | Permissions |
|------|-------------|
| **HR** | ✅ Full access (View all, Approve/Reject, Self-approve) |
| **Admin** | ✅ Full access (View all, Approve/Reject, Self-approve) |
| **All Employees** | ✅ View own data, Request leave/WFH, Submit work report |

### Access Rules

- **HR/Admin**: มองเห็นข้อมูลการลา/WFH ของพนักงานทุกคน สามารถอนุมัติ/ปฏิเสธได้
- **Employees**: มองเห็นเฉพาะข้อมูลการลา/WFH ของตัวเองเท่านั้น
- **Self-Approval**: HR/Admin สามารถอนุมัติตัวเองได้

---

## ✨ Features

### 1. Main Tabs Structure

หน้า ลางาน/WFH แบ่งออกเป็น **2 แถบหลัก**:

#### Tab 1: ข้อมูลพนักงานลางาน
- แสดงข้อมูลการลางานทั้งหมด
- แบ่งเป็น 3 แถบย่อย:
  1. **ข้อมูลการลางาน** - แสดงประวัติการลาทั้งหมด
  2. **ข้อมูลการขอลางาน** - แสดงการลาที่รออนุมัติ (สำหรับ HR/Admin)
  3. **Dashboard - ลางาน** - สรุปข้อมูลการลา

#### Tab 2: ข้อมูลพนักงาน WFH
- แสดงข้อมูลการ WFH ทั้งหมด
- แบ่งเป็น 4 แถบย่อย:
  1. **Dashboard - WFH** - สรุปข้อมูลการ WFH (สำหรับ HR/Admin - เป็นหน้าแรกเมื่อเข้าแถบนี้)
  2. **รายงานการทำงาน** - แสดงรายการที่ต้องรายงานการทำงาน (แยกออกมาให้เห็นชัดเจน)
  3. **ข้อมูลการ WFH** - แสดงประวัติการ WFH ทั้งหมด (เฉพาะข้อมูลของตัวเอง)
  4. **ข้อมูลการขอ WFH** - แสดงการขอ WFH ที่รออนุมัติ (สำหรับ HR/Admin)

---

### 2. Leave Management (ข้อมูลพนักงานลางาน)

#### 2.1 ข้อมูลการลางาน (Leave History)

**Purpose**: แสดงประวัติการลางานทั้งหมด

**Features**:
- ✅ แสดงรายการการลางานทั้งหมด (สำหรับ HR/Admin) หรือเฉพาะของตัวเอง (สำหรับ Employee)
- ✅ Filter ตาม:
  - สถานะ (รออนุมัติ, อนุมัติแล้ว, ไม่อนุมัติ)
  - ประเภทการลา (ลาป่วย, ลากิจ, ลาพักร้อน, ลาไม่รับค่าจ้าง, ลาอื่นๆ)
  - ช่วงวันที่
- ✅ Search: ค้นหาตามชื่อพนักงาน, รหัสพนักงาน
- ✅ Sort: เรียงตามวันที่ขอลา, วันที่ลา, สถานะ
- ✅ Pagination: แสดง 20 รายการต่อหน้า

**Data Display**:
- รหัสพนักงาน
- ชื่อ-นามสกุล
- วันที่ขอลา (request_date)
- วันที่ลา (leave_start_date - leave_end_date)
- ประเภทการลา
- จำนวนวันลา
- หมายเหตุ (reason)
- สถานะการลา
- ผู้ที่อนุมัติ/ปฏิเสธ
- หมายเหตุเพิ่มเติมสำหรับผู้อนุมัติ (approver_note)

**API Endpoint**: `GET /api/leave-requests`

---

#### 2.2 ข้อมูลการขอลางาน (Leave Requests - Pending Approval)

**Purpose**: แสดงการลาที่รออนุมัติ (สำหรับ HR/Admin)

**Features**:
- ✅ แสดงเฉพาะการลาที่ `status = 'รออนุมัติ'`
- ✅ Actions:
  - **อนุมัติ**: เปลี่ยนสถานะเป็น "อนุมัติแล้ว"
  - **ปฏิเสธ**: เปลี่ยนสถานะเป็น "ไม่อนุมัติ" + **บังคับกรอกหมายเหตุเพิ่มเติม**
- ✅ Self-approval: HR/Admin สามารถอนุมัติตัวเองได้
- ✅ แสดงข้อมูลพนักงานที่จะลาภายใน 3 วันข้างหน้า (Dashboard preview)

**UI Components**:
- Table แสดงรายการรออนุมัติ
- Modal สำหรับอนุมัติ/ปฏิเสธ
- Form สำหรับกรอกหมายเหตุเพิ่มเติม (บังคับเมื่อปฏิเสธ)

**API Endpoints**:
- `GET /api/leave-requests/pending` - ดึงการลาที่รออนุมัติ
- `PUT /api/leave-requests/:id/approve` - อนุมัติการลา
- `PUT /api/leave-requests/:id/reject` - ปฏิเสธการลา

---

#### 2.3 Dashboard - ลางาน

**Purpose**: สรุปข้อมูลการลางาน

**Features**:
- ✅ **สรุปการลาของพนักงาน**:
  - จำนวนวันลาที่ใช้ไปแล้ว (used_leave_days)
  - สิทธิ์ลาที่เหลือ (remaining_leave_days)
  - แยกตามประเภทการลา
- ✅ **กราฟแสดงการลา**:
  - กราฟแท่งแสดงจำนวนวันลาตามประเภท
  - กราฟเส้นแสดงแนวโน้มการลา
- ✅ **พนักงานที่จะลาภายใน 3 วันข้างหน้า**:
  - แสดงรายชื่อพนักงานที่ได้รับการอนุมัติแล้วและจะลาภายใน 3 วัน
  - ช่วยในการวางแผนงาน

**API Endpoint**: `GET /api/leave-requests/dashboard`

---

### 3. WFH Management (ข้อมูลพนักงาน WFH)

#### 3.1 ข้อมูลการ WFH (WFH History)

**Purpose**: แสดงประวัติการ WFH ทั้งหมด

**Features**:
- ✅ แสดงรายการการ WFH เฉพาะของตัวเอง (แม้เป็น Admin/HR)
- ✅ Filter ตาม:
  - สถานะ (รออนุมัติ, อนุมัติแล้ว, ไม่อนุมัติ)
  - ช่วงวันที่
- ✅ Search: ค้นหาตามชื่อพนักงาน, รหัสพนักงาน
- ✅ Sort: เรียงตามวันที่ขอ WFH, วันที่ WFH, สถานะ
- ✅ Pagination: แสดง 20 รายการต่อหน้า
- ✅ แสดงชื่อพนักงานที่เลือกวันเดียวกัน (เมื่อคลิกวันที่ในปฏิทิน)

**Data Display**:
- รหัสพนักงาน
- ชื่อ-นามสกุล
- วันที่ขอ WFH (request_date)
- วันที่ WFH (wfh_date)
- สถานะ WFH
- ผู้ที่อนุมัติ/ปฏิเสธ
- หมายเหตุเพิ่มเติมสำหรับผู้อนุมัติ (approver_note)
- รายงานการทำงาน (work_report) - แสดงถ้ามี

**API Endpoint**: `GET /api/wfh-requests`

---

#### 3.2 ข้อมูลการขอ WFH (WFH Requests - Pending Approval)

**Purpose**: แสดงการขอ WFH ที่รออนุมัติ (สำหรับ HR/Admin)

**Features**:
- ✅ แสดงเฉพาะการขอ WFH ที่ `status = 'รออนุมัติ'`
- ✅ Actions:
  - **อนุมัติ**: เปลี่ยนสถานะเป็น "อนุมัติแล้ว"
  - **ปฏิเสธ**: เปลี่ยนสถานะเป็น "ไม่อนุมัติ" + **บังคับกรอกหมายเหตุเพิ่มเติม**
- ✅ Self-approval: HR/Admin สามารถอนุมัติตัวเองได้

**UI Components**:
- Table แสดงรายการรออนุมัติ
- Modal สำหรับอนุมัติ/ปฏิเสธ
- Form สำหรับกรอกหมายเหตุเพิ่มเติม (บังคับเมื่อปฏิเสธ)

**API Endpoints**:
- `GET /api/wfh-requests/pending` - ดึงการขอ WFH ที่รออนุมัติ
- `PUT /api/wfh-requests/:id/approve` - อนุมัติการขอ WFH
- `PUT /api/wfh-requests/:id/reject` - ปฏิเสธการขอ WFH

---

#### 3.3 Dashboard - WFH

**Purpose**: Dashboard สำหรับแสดงข้อมูลการ WFH ครบถ้วน (สำหรับ HR/Admin)

**Features**:

##### 3.3.1 การขอ WFH ที่รออนุมัติ
- ✅ **Dashboard Card Layout**:
  - แสดงเป็น Card พื้นหลังสีอ่อน (#fff5f5) คล้ายกับ "การลาที่รออนุมัติ"
  - Header พร้อมไอคอน, Title "การขอ WFH ที่รออนุมัติ" และ Badge แสดงจำนวนรายการทั้งหมด
  - แสดงรายการเป็น Card แต่ละรายการ (แสดง 5 รายการต่อหน้า)
  
- ✅ **รายการการขอ WFH ที่รออนุมัติ**:
  - แสดงเฉพาะการขอ WFH ที่ `status = 'รออนุมัติ'`
  - แสดงข้อมูลในแต่ละ Card:
    - ชื่อพนักงาน (ชื่อเล่น) พร้อม Badge สถานะ "รออนุมัติ" (สีเหลือง)
    - ตำแหน่ง • รหัสพนักงาน
    - วันที่ WFH (รูปแบบภาษาไทย: "วันพุธ ที่ 11 กุมภาพันธ์ 2569")
  - **ดูข้อมูลอย่างเดียว**: ไม่มีปุ่มอนุมัติ/ปฏิเสธ (สำหรับดูข้อมูลเท่านั้น)
  
- ✅ **Pagination**:
  - แสดง 5 รายการต่อหน้า
  - มี Pagination component สำหรับเปลี่ยนหน้า
  - แสดงจำนวนหน้าทั้งหมดและหน้าปัจจุบัน

- ✅ **Date Filter with Calendar Color Coding**:
  - มี DateInput สำหรับกรองตามวันที่ WFH
  - **ปฏิทินแสดงสีตามจำนวนคนที่ขอ WFH ที่รออนุมัติ**:
    - **สีเหลือง** = มี 1-2 คนขอ WFH ที่รออนุมัติ
    - **สีแดง** = มี 3 คนขึ้นไปขอ WFH ที่รออนุมัติ
    - **สีขาว** = ไม่มีคนขอ WFH ที่รออนุมัติในวันนี้
  - สามารถเลือกวันที่เพื่อดูเฉพาะการขอ WFH ในวันนั้น
  - มีปุ่ม "ล้างตัวกรอง" เพื่อลบตัวกรองและแสดงทั้งหมด
  - แสดง Alert เมื่อมีการกรองข้อมูล พร้อมจำนวนรายการที่พบ
  - มี Color Legend แสดงความหมายของสีแต่ละสี
  - **วันเสาร์และอาทิตย์ไม่สามารถเลือกได้** (disabled)
  - **แสดงเดือนเป็นภาษาไทย** (กุมภาพันธ์ 2569)

##### 3.3.2 กราฟข้อมูลการ WFH ของเดือนปัจจุบัน
- ✅ **Bar Chart**:
  - แสดงกราฟแท่งแสดงจำนวนคนที่ WFH ในแต่ละวันของเดือน
  - แยกเป็น 2 bars: อนุมัติแล้ว (สีส้ม) และ รออนุมัติ (สีเหลือง)
  - มี Select สำหรับเลือกเดือน
  - แสดงข้อมูลรายวัน (1-31)
  - Tooltip แสดงจำนวนคนและสถานะ

##### 3.3.3 รายงานการทำงาน (รวมระบบกรองข้อมูลและ Grid)
**หมายเหตุสำคัญ**: ส่วนนี้รวม 3 ส่วนที่เกี่ยวข้องกันไว้ใน Card เดียวกัน:
1. ระบบกรองข้อมูลตามวันที่ WFH
2. Grid รายงานการทำงานแล้ว
3. Grid ยังไม่ได้รายงาน

**โครงสร้าง**:
- ✅ **Card หลัก**: "รายงานการทำงาน"
  - **ส่วนกรองข้อมูล** (Card ย่อย):
    - มี DateInput สำหรับเลือกวันที่เพื่อกรองข้อมูลในส่วน "รายงานการทำงานแล้ว" และ "ยังไม่ได้รายงาน"
    - ปฏิทินแสดงสีตามจำนวนคนที่ WFH:
      - **สีเหลือง** = มี 1-2 คน WFH
      - **สีแดง** = มี 3 คนขึ้นไป WFH
      - **สีขาว** = ไม่มีคน WFH
    - **วันเสาร์และอาทิตย์ไม่สามารถเลือกได้** (disabled)
    - มีปุ่ม "ล้างตัวกรอง" เพื่อลบตัวกรองและแสดงข้อมูลทั้งหมดในเดือนที่เลือก
    - **เมื่อเลือกวันที่**:
      - ระบบจะกรองข้อมูลในส่วน "รายงานการทำงานแล้ว" ให้แสดงเฉพาะรายการที่ `wfh_date` ตรงกับวันที่เลือก
      - ระบบจะกรองข้อมูลในส่วน "ยังไม่ได้รายงาน" ให้แสดงเฉพาะรายการที่ `wfh_date` ตรงกับวันที่เลือก (รวมทั้ง overdue)
  
  - **Grid รายงานการทำงาน (2 ช่อง)**: แสดงข้อมูลตามวันที่ WFH (wfh_date) ในเดือนที่เลือกเท่านั้น และแสดงเฉพาะการขอ WFH ที่ `status = 'อนุมัติแล้ว'` เท่านั้น

- ✅ **ช่องที่ 1: รายงานการทำงานแล้ว**:
  - แสดงรายชื่อพนักงานที่ส่งรายงานการทำงานแล้ว
  - **เงื่อนไข**: พนักงานต้องได้รับการอนุมัติ WFH (`status = 'อนุมัติแล้ว'`) และมี `work_report` ที่ไม่ว่างเปล่า
  - แสดงข้อมูล: ชื่อพนักงาน (ชื่อเล่น), ตำแหน่ง, รหัสพนักงาน, วันที่ WFH, เวลาที่ส่งรายงาน
  - มี Badge สีเขียวแสดง "ส่งแล้ว"
  - มี ScrollArea สำหรับดูรายการทั้งหมด

- ✅ **ช่องที่ 2: ยังไม่ได้รายงาน**:
  - แสดงรายชื่อพนักงานที่ยังไม่ได้ส่งรายงานการทำงาน
  - **เงื่อนไข**: พนักงานต้องได้รับการอนุมัติ WFH (`status = 'อนุมัติแล้ว'`) แต่ยังไม่มี `work_report` หรือ `work_report` เป็นค่าว่าง
  - แสดงข้อมูล: ชื่อพนักงาน (ชื่อเล่น), ตำแหน่ง, รหัสพนักงาน, วันที่ WFH
  - แยกเป็น 2 ส่วน:
    - **ช่วงที่แจ้งได้**: แสดง Badge สีส้ม "ส่งภายใน X วัน" (ภายใน 0-2 วันหลังจากวันที่ WFH)
      - สามารถส่งรายงานได้ภายใน 2 วันหลังจากวันที่ WFH
    - **เลยกำหนด**: แสดง Badge สีแดง "เลยกำหนด" (เกิน 2 วันหลังจากวันที่ WFH)
      - เกินกำหนดส่งรายงานแล้ว (มากกว่า 2 วันหลังจากวันที่ WFH)
  - มี ScrollArea สำหรับดูรายการทั้งหมด
  - **เมื่อกรองตามวันที่**: จะแสดงเฉพาะรายการที่ `wfh_date` ตรงกับวันที่เลือก (รวมทั้ง overdue)

**ความสัมพันธ์กับกราฟ**:
- กราฟแสดงจำนวนคนที่ WFH ในแต่ละวัน (อนุมัติแล้ว + รออนุมัติ)
- ส่วนรายงานการทำงานแสดงเฉพาะการขอ WFH ที่ **อนุมัติแล้ว** เท่านั้น
- ดังนั้นจำนวนในกราฟอาจมากกว่าจำนวนในส่วนรายงานการทำงาน (เพราะรวม "รออนุมัติ" ด้วย)


**UI Components**:
- Card Layout แสดงรายการรออนุมัติ
- Pagination component สำหรับเปลี่ยนหน้า
- DateInput สำหรับกรองตามวันที่ WFH (2 ตัว: สำหรับ pending requests และสำหรับ filter)
- BarChart สำหรับแสดงกราฟข้อมูล WFH รายเดือน
- Grid 2 ช่องสำหรับแสดงรายงานการทำงาน
- ScrollArea สำหรับดูรายการยาวๆ

**API Endpoints**:
- `GET /api/wfh-requests/pending?page=1&limit=5&wfh_date=YYYY-MM-DD` - ดึงการขอ WFH ที่รออนุมัติ
- `GET /api/wfh-requests/dashboard/daily?month=YYYY-MM` - ดึงข้อมูล WFH รายวันสำหรับกราฟ
- `GET /api/wfh-requests/work-reports?month=YYYY-MM` - ดึงรายงานการทำงาน
- `GET /api/wfh-requests/calendar?month=YYYY-MM` - ดึงข้อมูลสำหรับ Calendar view

---

### 4. Request Forms (ฟอร์มขอลา/WFH)

#### 4.1 ฟอร์มขอลา (Leave Request Form)

**Purpose**: พนักงานสามารถขอลาได้

**Fields**:
- **วันที่ขอลา** (request_date) - Auto-fill: วันนี้
- **วันที่ลา** (leave_start_date - leave_end_date) - Date Range Picker
- **ประเภทการลา** (leave_type) - Select:
  - ลาป่วย
  - ลากิจ
  - ลาพักร้อน
  - ลาไม่รับค่าจ้าง
  - ลาอื่นๆ
- **หมายเหตุ** (reason) - Textarea (บังคับกรอกถ้าเลือก "ลากิจ" หรือ "ลาอื่นๆ")

**Validation**:
- วันที่ลาไม่สามารถเป็นอดีตได้ (ต้องเป็นวันนี้หรืออนาคต)
- `leave_end_date` ต้องมากกว่าหรือเท่ากับ `leave_start_date`
- ระบบคำนวณ `leave_days` อัตโนมัติ

**API Endpoint**: `POST /api/leave-requests`

---

#### 4.2 ฟอร์มขอ WFH (WFH Request Form)

**Purpose**: พนักงานสามารถขอ WFH ได้

**Fields**:
- **วันที่ขอ WFH** (request_date) - Auto-fill: วันนี้
- **วันที่ WFH** (wfh_date) - Date Picker (Calendar view)

**Validation**:
- **สิทธิ์ขอ WFH**: พนักงานต้องทำงานมาแล้วอย่างน้อย 3 เดือน (ตรวจสอบจาก `employees.hire_date`)
- **จำกัดจำนวน WFH ต่อวัน**: สูงสุด 3 คนต่อวัน
  - แสดงสถานะใน Calendar:
    - เหลือ 0/3 → สีเขียว (สามารถขอได้)
    - เหลือ 1/3, 2/3 → สีเหลือง (เตือน)
    - เหลือ 3/3 → สีแดง (ไม่สามารถขอได้)
- **จำกัดจำนวน WFH ต่อเดือน**:
  - พนักงานทั่วไป: สูงสุด 6 วันต่อเดือน
  - ตำแหน่ง IT: สูงสุด 16 วันต่อเดือน
- วันที่ WFH ไม่สามารถเป็นอดีตได้
- ไม่สามารถขอ WFH ในวันเดียวกันซ้ำได้

**Calendar View**:
- แสดงปฏิทินสำหรับเลือกวันที่
- แสดงจำนวนคนที่ขอ WFH ในวันนั้น (0/3, 1/3, 2/3, 3/3)
- สีตามสถานะ (เขียว/เหลือง/แดง)
- ด้านขวาของปฏิทินแสดงข้อมูลเพิ่มเติม:
  - รายชื่อพนักงานที่ขอ WFH ในวันนั้น
  - สถานะการอนุมัติ

**API Endpoint**: `POST /api/wfh-requests`

**API Endpoint (Calendar)**: `GET /api/wfh-requests/calendar?month=YYYY-MM` - ดึงข้อมูลสำหรับ Calendar view

---

### 5. Work Report (รายงานการทำงาน)

**Purpose**: พนักงานกรอกรายงานการทำงานหลังจาก WFH

**Features**:
- ✅ แสดงเฉพาะการ WFH ที่ `status = 'อนุมัติแล้ว'` และ `wfh_date` เป็นอดีต
- ✅ Form สำหรับกรอกรายงานการทำงาน (work_report)
- ✅ บันทึกเวลาที่ส่งรายงาน (work_report_submitted_at)

**UI Components**:
- Textarea สำหรับกรอกรายงานการทำงาน
- Button "ส่งรายงาน"

**API Endpoint**: `PUT /api/wfh-requests/:id/work-report`

---

## 📊 Data Structure

### Leave Request

```typescript
interface LeaveRequest {
  id: string
  employee_id: string
  request_date: string // YYYY-MM-DD
  leave_start_date: string // YYYY-MM-DD
  leave_end_date: string // YYYY-MM-DD
  leave_type: 'ลาป่วย' | 'ลากิจ' | 'ลาพักร้อน' | 'ลาไม่รับค่าจ้าง' | 'ลาอื่นๆ'
  leave_days: number // คำนวณอัตโนมัติ
  reason: string | null
  status: 'รออนุมัติ' | 'อนุมัติแล้ว' | 'ไม่อนุมัติ'
  approved_by: string | null // user.id
  approved_at: string | null // ISO datetime
  approver_note: string | null
  created_at: string
  updated_at: string
  deleted_at: string | null
  // Joined data
  employee?: {
    employee_id: string
    full_name: string
    position: string
  }
  approver?: {
    id: string
    name: string
  }
}
```

### WFH Request

```typescript
interface WFHRequest {
  id: string
  employee_id: string
  request_date: string // YYYY-MM-DD
  wfh_date: string // YYYY-MM-DD
  status: 'รออนุมัติ' | 'อนุมัติแล้ว' | 'ไม่อนุมัติ'
  approved_by: string | null // user.id
  approved_at: string | null // ISO datetime
  approver_note: string | null
  work_report: string | null
  work_report_submitted_at: string | null // ISO datetime
  created_at: string
  updated_at: string
  deleted_at: string | null
  // Joined data
  employee?: {
    employee_id: string
    full_name: string
    position: string
  }
  approver?: {
    id: string
    name: string
  }
}
```

### Calendar Data (for WFH)

```typescript
interface WFHCalendarDay {
  date: string // YYYY-MM-DD
  approved_count: number // จำนวนคนที่อนุมัติแล้ว (0-3)
  status: 'available' | 'warning' | 'full' // สีเขียว/เหลือง/แดง
  requests: Array<{
    id: string
    employee_id: string
    employee_name: string
    status: 'รออนุมัติ' | 'อนุมัติแล้ว' | 'ไม่อนุมัติ'
  }>
}
```

---

## 🎨 UI/UX Guidelines

### Design Style
- **Primary Color**: Orange (#ff6b35, #ff8c42)
- **Status Colors**:
  - รออนุมัติ: Yellow (#ff9800)
  - อนุมัติแล้ว: Green (#4caf50)
  - ไม่อนุมัติ: Red (#f44336)
- **Calendar Colors**:
  - Available (0/3): Green (#4caf50)
  - Warning (1/3, 2/3): Yellow (#ff9800)
  - Full (3/3): Red (#f44336)

### Components
- **Tabs**: ใช้ Mantine Tabs component
- **Tables**: ใช้ Mantine Table component (sortable, filterable)
- **Calendar**: ใช้ Mantine DatePicker หรือ @mantine/dates Calendar component
- **Modals**: ใช้ Mantine Modal สำหรับอนุมัติ/ปฏิเสธ
- **Forms**: ใช้ Mantine Form components

### Responsive Design
- Mobile: แสดงเป็น Card layout
- Tablet: แสดงเป็น Table layout
- Desktop: แสดงเป็น Table layout พร้อม filters

---

## 🔌 API Endpoints

### Leave Requests

| Method | Endpoint | Description | Access |
|--------|----------|-------------|--------|
| GET | `/api/leave-requests` | ดึงรายการการลาทั้งหมด | All |
| GET | `/api/leave-requests/pending` | ดึงการลาที่รออนุมัติ | HR/Admin |
| GET | `/api/leave-requests/:id` | ดึงข้อมูลการลาตาม ID | All (own data) / HR/Admin (all) |
| GET | `/api/leave-requests/dashboard` | ดึงข้อมูล Dashboard | All (own data) / HR/Admin (all) |
| POST | `/api/leave-requests` | สร้างการขอลาใหม่ | All |
| PUT | `/api/leave-requests/:id/approve` | อนุมัติการลา | HR/Admin |
| PUT | `/api/leave-requests/:id/reject` | ปฏิเสธการลา | HR/Admin |
| PUT | `/api/leave-requests/:id` | แก้ไขข้อมูลการลา | All (own data, pending only) |
| DELETE | `/api/leave-requests/:id` | ยกเลิกการลา | All (own data, pending only) |

### WFH Requests

| Method | Endpoint | Description | Access |
|--------|----------|-------------|--------|
| GET | `/api/wfh-requests` | ดึงรายการการขอ WFH ทั้งหมด | All |
| GET | `/api/wfh-requests/pending` | ดึงการขอ WFH ที่รออนุมัติ | HR/Admin |
| GET | `/api/wfh-requests/calendar` | ดึงข้อมูลสำหรับ Calendar view | All |
| GET | `/api/wfh-requests/:id` | ดึงข้อมูลการขอ WFH ตาม ID | All (own data) / HR/Admin (all) |
| GET | `/api/wfh-requests/dashboard` | ดึงข้อมูล Dashboard | All (own data) / HR/Admin (all) |
| POST | `/api/wfh-requests` | สร้างการขอ WFH ใหม่ | All |
| PUT | `/api/wfh-requests/:id/approve` | อนุมัติการขอ WFH | HR/Admin |
| PUT | `/api/wfh-requests/:id/reject` | ปฏิเสธการขอ WFH | HR/Admin |
| PUT | `/api/wfh-requests/:id/work-report` | ส่งรายงานการทำงาน | All (own data) |
| PUT | `/api/wfh-requests/:id` | แก้ไขข้อมูลการขอ WFH | All (own data, pending only) |
| DELETE | `/api/wfh-requests/:id` | ยกเลิกการขอ WFH | All (own data, pending only) |

**ดูรายละเอียด API**: `Documentation/API/LEAVE_WFH_API.md`

---

## 📁 File Structure

### Frontend Components

```
src/
├── pages/
│   └── LeaveManagement.tsx          # Main page component
├── components/
│   └── Leave/
│       ├── LeaveRequestForm.tsx     # Form สำหรับขอลา
│       ├── LeaveRequestList.tsx     # รายการการลา
│       ├── LeaveRequestTable.tsx    # Table สำหรับแสดงการลา
│       ├── LeaveDashboard.tsx       # Dashboard การลา
│       ├── WFHRequestForm.tsx       # Form สำหรับขอ WFH
│       ├── WFHRequestList.tsx       # รายการการขอ WFH
│       ├── WFHRequestTable.tsx      # Table สำหรับแสดงการขอ WFH
│       ├── WFHDashboard.tsx         # Dashboard WFH
│       ├── WFHCalendar.tsx         # Calendar view สำหรับเลือกวันที่ WFH
│       ├── WorkReportForm.tsx       # Form สำหรับรายงานการทำงาน
│       └── ApprovalModal.tsx        # Modal สำหรับอนุมัติ/ปฏิเสธ
└── services/
    └── leaveService.ts              # API service functions
```

### Backend Routes

```
backend/
└── routes/
    ├── leave-requests.js            # Leave requests routes
    └── wfh-requests.js              # WFH requests routes
```

---

## 🔄 Business Logic

### Leave Request Logic

1. **การคำนวณจำนวนวันลา**:
   - คำนวณจาก `leave_start_date` ถึง `leave_end_date`
   - นับเฉพาะวันทำการ (ไม่นับวันหยุดสุดสัปดาห์)
   - ใช้ฟังก์ชัน `calculateWorkingDays(startDate, endDate)`

2. **การอนุมัติ/ปฏิเสธ**:
   - HR/Admin สามารถอนุมัติ/ปฏิเสธได้
   - Self-approval: HR/Admin สามารถอนุมัติตัวเองได้
   - ถ้าปฏิเสธ ต้องบังคับกรอก `approver_note`

3. **การแสดงพนักงานที่จะลาภายใน 3 วัน**:
   - แสดงเฉพาะการลาที่ `status = 'อนุมัติแล้ว'`
   - `leave_start_date` อยู่ระหว่างวันนี้ถึง 3 วันข้างหน้า

### WFH Request Logic

1. **การตรวจสอบสิทธิ์ขอ WFH**:
   - พนักงานต้องทำงานมาแล้วอย่างน้อย 3 เดือน
   - ตรวจสอบจาก `employees.hire_date`

2. **การจำกัดจำนวน WFH ต่อวัน**:
   - สูงสุด 3 คนต่อวัน
   - นับเฉพาะ `status = 'อนุมัติแล้ว'`
   - แสดงใน Calendar view

3. **การจำกัดจำนวน WFH ต่อเดือน**:
   - พนักงานทั่วไป: สูงสุด 6 วันต่อเดือน
   - ตำแหน่ง IT: สูงสุด 16 วันต่อเดือน
   - ตรวจสอบจาก `employees.position`

4. **การรายงานการทำงาน**:
   - แถบ "รายงานการทำงาน" แยกออกมาให้เห็นชัดเจน
   - แสดงเฉพาะการ WFH ที่ `status = 'อนุมัติแล้ว'` และ `wfh_date` เป็นวันปัจจุบันหรืออดีต
   - พนักงานสามารถกรอก `work_report` ได้ในวันที่ WFH หรือหลังจากนั้น
   - สถานะการรายงาน:
     - **"ต้องรายงาน"** (สีน้ำเงิน): วันที่ WFH เป็นวันนี้
     - **"ส่งภายใน X วัน"** (สีส้ม): ภายใน 1-2 วันหลังจากวันที่ WFH
     - **"เลยกำหนด X วัน"** (สีแดง): เกิน 2 วันหลังจากวันที่ WFH
   - กำหนดส่งรายงานภายใน 1-2 วันหลังจากวันที่ WFH

---

## 📝 Recent Updates

### 2026-01-30 (Latest)
- ✅ ปรับให้หน้า Dashboard - WFH เป็นหน้าแรกเมื่อเข้าแถบ "ข้อมูลพนักงาน WFH" สำหรับ HR/Admin
- ✅ สลับตำแหน่ง Dashboard - WFH มาไว้แรกสุดในแถบย่อย
- ✅ ตั้งค่า default tab เป็น "dashboard" สำหรับ HR/Admin เมื่อเข้าแถบ WFH
- ✅ ปรับปรุง Dashboard - WFH: ลบส่วน Summary Cards (จำกัดต่อเดือน, ใช้ไปแล้ว, เหลือสิทธิ์) และ Work Reports
- ✅ สร้าง Dashboard - WFH แบบ Card Layout คล้ายกับ "การลาที่รออนุมัติ":
  - แสดงรายการการขอ WFH ที่รออนุมัติในรูปแบบ Card แต่ละรายการ
  - **ดูข้อมูลอย่างเดียว**: ลบปุ่มอนุมัติ/ปฏิเสธออก (สำหรับดูข้อมูลเท่านั้น)
  - แสดงข้อมูล: ชื่อพนักงาน (ชื่อเล่น), ตำแหน่ง, รหัสพนักงาน, วันที่ WFH
  - แสดง Badge สถานะ "รออนุมัติ" และจำนวนรายการทั้งหมด
- ✅ เพิ่มระบบ Pagination: แสดง 5 รายการต่อหน้า (ลดภาระ backend)
- ✅ เพิ่มระบบ Date Filter: สามารถกรองตามวันที่ WFH ได้
- ✅ เพิ่ม date filter ใน backend API `/api/wfh-requests/pending` เพื่อรองรับการกรองตามวันที่
- ✅ เพิ่มระบบแสดงสีในปฏิทิน (Calendar Color Coding):
  - สีเหลือง = มี 1-2 คนขอ WFH ที่รออนุมัติ
  - สีแดง = มี 3 คนขึ้นไปขอ WFH ที่รออนุมัติ
  - สีขาว = ไม่มีคนขอ WFH ที่รออนุมัติในวันนี้
  - ใช้ข้อมูลจาก Calendar API เพื่อแสดงสีตามจำนวนคนที่ขอ WFH ที่รออนุมัติ
  - มี Color Legend แสดงความหมายของสีแต่ละสี
- ✅ เพิ่มกราฟแสดงข้อมูลการ WFH ของเดือนปัจจุบัน:
  - Bar Chart แสดงจำนวนคนที่ WFH ในแต่ละวัน
  - แยกเป็น 2 bars: อนุมัติแล้ว (สีส้ม) และ รออนุมัติ (สีเหลือง)
  - มี Select สำหรับเลือกเดือน
  - แสดงข้อมูลตามวันที่ WFH (wfh_date) ในเดือนที่เลือก
- ✅ เพิ่ม Grid 2 ช่องสำหรับแสดงรายงานการทำงาน:
  - ช่องที่ 1: รายงานการทำงานแล้ว (สีเขียว) - แสดงเฉพาะพนักงานที่ส่งรายงานแล้ว
  - ช่องที่ 2: ยังไม่ได้รายงาน (แยกเป็นช่วงที่แจ้งได้ และเลยกำหนด)
  - **สำคัญ**: แสดงเฉพาะการขอ WFH ที่ `status = 'อนุมัติแล้ว'` เท่านั้น
  - แสดงข้อมูลตามวันที่ WFH (wfh_date) ในเดือนที่เลือก
  - มีข้อความแจ้งเตือนว่าแสดงข้อมูลตามวันที่ WFH ในเดือนที่เลือกเท่านั้น
- ✅ เพิ่มระบบกรองข้อมูลด้วยปฏิทิน:
  - เลือกวันที่เพื่อกรองข้อมูลในส่วน "รายงานการทำงานแล้ว" และ "ยังไม่ได้รายงาน"
  - เมื่อเลือกวันที่ ระบบจะกรองข้อมูลให้แสดงเฉพาะรายการที่ `wfh_date` ตรงกับวันที่เลือก
  - แสดง Table รายชื่อพนักงานที่ WFH ในวันนั้นพร้อมสถานะ
  - มีปุ่ม "ล้างตัวกรอง" เพื่อลบตัวกรองและแสดงข้อมูลทั้งหมดในเดือนที่เลือก
- ✅ เพิ่ม API endpoints:
  - `GET /api/wfh-requests/dashboard/daily?month=YYYY-MM` - ดึงข้อมูล WFH รายวันสำหรับกราฟ
  - `GET /api/wfh-requests/work-reports?month=YYYY-MM` - ดึงรายงานการทำงาน (เฉพาะ status = 'อนุมัติแล้ว')
- ✅ ปรับปรุง Calendar API ให้ส่ง employee_nick_name และ employee_position ใน requests array
- ✅ ปรับให้วันเสาร์และอาทิตย์ไม่สามารถเลือกได้ (disabled)
- ✅ ปรับให้แสดงเดือนเป็นภาษาไทยในปฏิทิน
- ✅ แก้ไขการคำนวณวันที่ใน API (แก้ปัญหา firstDayStr และ lastDayStr ที่ผิด)
- ✅ ปรับปรุงการตรวจสอบ work_report ให้ตรวจสอบว่าไม่ใช่ empty string
- ✅ เพิ่ม debug logging เพื่อตรวจสอบข้อมูลที่ query ได้

### 2026-01-29
- ✅ เพิ่มแถบ "รายงานการทำงาน" แยกออกมาให้เห็นชัดเจน
- ✅ เพิ่มการแสดงสถานะการรายงาน (ต้องรายงาน, ใกล้กำหนด, เลยกำหนด)
- ✅ เพิ่มการแสดงชื่อพนักงานที่เลือกวันเดียวกันในปฏิทิน
- ✅ ปรับให้แถบ "ข้อมูลการ WFH" แสดงเฉพาะข้อมูลของตัวเอง (แม้เป็น Admin/HR)
- ✅ แก้ไขบัคทั้งหมด 7 รายการ (ดูรายละเอียดใน BUG_FIXES.md)
- ✅ ปรับปรุง UI/UX ของปฏิทินให้เหมือนกันทั้ง Leave และ WFH

### 2026-01-29 (Initial)
- สร้าง Documentation สำหรับหน้า Leave Management ตาม requirements
- ออกแบบ Database Schema สำหรับ leave_requests และ wfh_requests
- กำหนด API Endpoints และ Business Logic
- 2026-01-30 - **พัฒนา Backend API Routes**:
  - สร้าง `backend/routes/leave-requests.js` พร้อม endpoints ครบถ้วน
  - สร้าง `backend/routes/wfh-requests.js` พร้อม endpoints ครบถ้วน
  - สร้าง `backend/utils/leaveHelpers.js` สำหรับ helper functions
  - อัพเดท `backend/server.js` เพื่อเพิ่ม routes ใหม่
- 2026-01-30 - **พัฒนา Frontend Components**:
  - สร้าง `src/services/leaveService.ts` สำหรับ API service functions
  - พัฒนา `src/pages/LeaveManagement.tsx` พร้อม Tabs structure
  - สร้าง `src/components/Leave/LeaveRequestList.tsx` - รายการการลา
  - สร้าง `src/components/Leave/LeaveRequestForm.tsx` - ฟอร์มขอลา **พร้อม Calendar view สำหรับเลือกช่วงวันที่**
  - สร้าง `src/components/Leave/LeaveDashboard.tsx` - Dashboard การลา
  - สร้าง `src/components/Leave/WFHRequestList.tsx` - รายการการขอ WFH
  - สร้าง `src/components/Leave/WFHRequestForm.tsx` - ฟอร์มขอ WFH พร้อม Calendar view
  - สร้าง `src/components/Leave/WFHDashboard.tsx` - Dashboard WFH
  - สร้าง `src/components/Leave/ApprovalModal.tsx` - Modal สำหรับอนุมัติ/ปฏิเสธ
  - สร้าง `src/components/Leave/WorkReportForm.tsx` - ฟอร์มรายงานการทำงาน
- 2026-01-30 - **ปรับปรุง UI Leave Request Form**:
  - เปลี่ยนจาก DateInput แยก 2 ตัว เป็น Calendar แบบเต็มรูปแบบ
  - เพิ่มความสามารถเลือกช่วงวันที่ในปฏิทินเดียว
  - Highlight วันที่ที่เลือกด้วย border สีส้ม (#ff6b35)
  - แสดงช่วงวันที่ที่เลือกด้วย background สีอ่อน
  - Disable วันที่ในอดีต (แสดงเป็นสีเทา)
  - แสดงจำนวนวันทำการอัตโนมัติเมื่อเลือกช่วงวันที่

---

**Last Updated**: 2026-01-30  
**Status**: ✅ Implementation Complete - Ready for Testing
