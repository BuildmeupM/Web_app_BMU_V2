# ⌨️ 08. Document Entry Page (คีย์เอกสาร)

## 📋 Overview

หน้าคีย์เอกสาร - สำหรับแสดงงานที่ได้รับมอบหมายและจัดการการคีย์เอกสาร

**Route**: `/document-entry`  
**Component**: `src/pages/DocumentEntry.tsx`

## 🔐 Access Control

- ✅ **admin**
- ✅ **data_entry**
- ✅ **data_entry_and_service**

## ✨ Features

### 1. สรุปเอกสารที่รับผิดชอบ
- ✅ แสดงสรุปข้อมูล 3 ตัวเลข:
  - เอกสารรวมทั้งหมด (สีน้ำเงิน)
  - เอกสารคีย์แล้วทั้งหมด (สีเขียว) - เฉพาะที่สถานะ "ดำเนินการเสร็จแล้ว"
  - รอคีย์ทั้งหมด (สีเทา)
- ✅ ปุ่ม "สรุปข้อมูลรายวัน" - แสดงสรุปตามวันที่ส่ง (entry_timestamp)
  - พื้นหลังสีขาว กรอบสีส้ม ฟอนต์สีส้ม
  - แสดงเฉพาะวันที่ที่มีเอกสารที่คีย์เสร็จแล้ว
  - แสดงข้อมูลในรูปแบบ Accordion ที่สามารถขยายดูรายละเอียดได้
- ✅ ปุ่ม "สรุปข้อมูลรายเดือน" - แสดงสรุปตามเดือน (work_month)
  - พื้นหลังสีขาว กรอบสีส้ม ฟอนต์สีส้ม
  - แสดงเฉพาะเดือนที่มีเอกสารที่คีย์เสร็จแล้ว
  - แสดงข้อมูลในรูปแบบ Accordion ที่สามารถขยายดูรายละเอียดได้
- ✅ **รายละเอียดใน Summary** (เมื่อขยาย Accordion):
  - แสดงตารางรายละเอียดแต่ละ Build พร้อม:
    - Build, ชื่อบริษัท
    - เอกสารหัก ณ ที่จ่าย (WHT) - จำนวนและสถานะ
    - เอกสารมีภาษีมูลค่าเพิ่ม (VAT) - จำนวนและสถานะ
    - เอกสารไม่มีภาษีมูลค่าเพิ่ม (Non-VAT) - จำนวนและสถานะ
    - รวม - แสดงเฉพาะจำนวนที่เสร็จสิ้นแล้ว (ไม่ใช่จำนวนทั้งหมด)
- ✅ ปุ่ม "โยกงาน" (disabled - สำหรับอนาคต)
- ✅ **Component**: `DocumentEntrySummary` จาก `src/components/DocumentEntry/DocumentEntrySummary.tsx`

### 2. งานที่ได้รับผิดชอบ
- ✅ แสดงตารางรายการบริษัทที่ได้รับมอบหมาย
- ✅ สรุปจำนวนงาน (entries) แทนการนับบริษัท:
  - งานทั้งหมด (สีส้ม)
  - ดำเนินการเสร็จทั้งหมด (สีเขียว) - ทุกประเภทเอกสารเสร็จแล้ว
  - ดำเนินการเสร็จบางส่วน (สีเหลือง) - บางประเภทเสร็จแล้ว
  - ยังไม่ดำเนินการ (สีเทา) - ยังไม่มีประเภทใดเสร็จ
- ✅ แต่ละแถวแสดง:
  - Build (Badge สีส้ม)
  - ชื่อบริษัท
  - จำนวนครั้งที่ส่ง
  - WHT, VAT, Non-VAT, Bot counts (center-aligned)
  - สถานะ (เสร็จแล้ว/กำลังดำเนินการ/รอดำเนินการ)
- ✅ คลิกที่แถวเพื่อแสดงรายละเอียดแบบ inline ด้านล่าง (ไม่ใช่ Modal)
- ✅ Logic การคำนวณสถานะ:
  - ดำเนินเสร็จแล้ว: ทุกประเภทเอกสาร (WHT, VAT, Non-VAT) ในทุก submission มีสถานะ "ดำเนินการเสร็จแล้ว" หรือ document_count = 0
  - รอดำเนินการ: มีอย่างน้อย 1 ประเภทใน submission ใดๆ ที่ยังไม่เสร็จ
- ✅ **Component**: `CompanyList` จาก `src/components/DocumentEntry/CompanyList.tsx`

### 3. Company Detail Section (Inline Display)
- ✅ แสดงข้อมูลบริษัทแบบ inline ด้านล่างแถวที่เลือก (ไม่ใช่ Modal):
  - Build, ชื่อบริษัท
  - เลขทะเบียนนิติบุคคล
  - ไซต์บริษัท
  - สถานะจดทะเบียนภาษี
  - วันที่จดภาษีมูลค่าเพิ่ม
  - ที่อยู่บริษัท
  - ผู้ทำบัญชี (accounting_responsible) - ดึงจาก `work_assignments` (priority) หรือ `monthly_tax_data` (fallback)
  - ผู้ตรวจภาษี (tax_inspection_responsible) - ดึงจาก `work_assignments` (priority) หรือ `monthly_tax_data` (fallback)
  - ผู้รับผิดชอบในการคีย์ (document_entry_responsible)
  - ผู้ยื่นภาษี WHT (wht_filer_employee_id) - แสดงเฉพาะเมื่อมีค่า
  - ผู้ยื่นภาษี VAT (vat_filer_employee_id) - แสดงเฉพาะเมื่อมีค่า
- ✅ แสดงรายการ submissions ทั้งหมด (ครั้งที่ 1, 2, 3, ...) ในรูปแบบ Accordion
- ✅ แต่ละ submission แสดง:
  - ครั้งที่ X (Badge สีส้ม), วันที่ส่ง, ผู้รับผิดชอบ
  - Badge สถานะรวม:
    - "เสร็จสิ้น" (green outline) - ทุกประเภทเสร็จแล้ว
    - "เสร็จบางส่วน" (yellow outline) - บางประเภทเสร็จแล้ว
    - "กำลังดำเนินการ" (yellow light) - มีประเภทที่กำลังดำเนินการ
    - "รอดำเนินการ" (gray light) - ยังไม่เริ่มดำเนินการ
  - ปุ่ม "เริ่มต้นทั้งหมด" (แสดงเฉพาะเมื่อมีอย่างน้อย 1 ประเภทที่รอดำเนินการ และยังไม่เสร็จสิ้นทั้งหมด)
  - 3 Cards: WHT, VAT, Non-VAT (แต่ละ card แสดงจำนวนเอกสาร, สถานะ, datetime, ปุ่มเริ่มต้น/เสร็จสิ้น)
  - ความคิดเห็นส่งมอบงานคีย์ (read-only)
  - ความคิดเห็นส่งคืนงานคีย์ (editable) พร้อมปุ่ม "บันทึก ความคิดเห็นส่งคืนงานคีย์" สำหรับบันทึกแบบ explicit
- ✅ **การป้องกันการอัพเดท**: เมื่อทุกประเภทเอกสารเสร็จสิ้นแล้ว (สถานะ "เสร็จสิ้น") จะ disable:
  - ปุ่ม "เสร็จสิ้น" ในแต่ละ SubmissionCard
  - Textarea และปุ่มบันทึก "ความคิดเห็นส่งคืนงานคีย์"
  - ปุ่ม "เริ่มต้นทั้งหมด"
  - ปุ่ม "เริ่มต้น" และ "เสร็จสิ้น" ใน SubmissionCard ทั้ง 3 ประเภท
- ✅ **Component**: `CompanyDetailSection` จาก `src/components/DocumentEntry/CompanyDetailSection.tsx`

### 4. Submission Card
- ✅ Card สำหรับแสดงข้อมูลแต่ละประเภทเอกสาร (WHT/VAT/Non-VAT)
- ✅ แสดง: จำนวนเอกสาร, สถานะ (Badge), เวลาเริ่มต้น, เวลาดำเนินการเสร็จ
- ✅ ปุ่ม "เริ่มต้น" (แสดงเมื่อสถานะ = "ยังไม่ดำเนินการ")
- ✅ ปุ่ม "เสร็จสิ้น" (แสดงเมื่อสถานะ = "กำลังดำเนินการ")
- ✅ ใช้ `updateStatus()` API เพื่ออัพเดทสถานะ
- ✅ **Component**: `SubmissionCard` จาก `src/components/DocumentEntry/SubmissionCard.tsx`

### 5. Start All Button
- ✅ ปุ่ม "เริ่มต้นทั้งหมด" สำหรับเริ่มทุกประเภทที่ยังไม่เริ่ม
- ✅ Logic: เรียก `updateStatus()` สำหรับทุกประเภทที่มีสถานะ "ยังไม่ดำเนินการ" และ document_count > 0
- ✅ แสดงจำนวนประเภทเอกสารที่รอดำเนินการ
- ✅ **Component**: `StartAllButton` จาก `src/components/DocumentEntry/StartAllButton.tsx`

### 6. Refresh Button
- ✅ ปุ่มรีเฟรชข้อมูลใน header
- ✅ Invalidate และ refetch queries ทั้งหมดที่เกี่ยวข้อง
- ✅ แสดง loading state เมื่อกำลัง refresh

## 📊 Data Flow

### 1. Initial Load
1. User เข้าหน้า Document Entry
2. `DocumentEntrySummary` ดึงข้อมูลสรุปจาก `GET /api/document-entry-work/summary`
3. `CompanyList` ดึงข้อมูลจาก `GET /api/document-entry-work?document_entry_responsible={employeeId}&year={year}&month={month}`
4. Filter logic ใน backend:
   ```sql
   WHERE (
     (current_responsible_employee_id IS NOT NULL AND current_responsible_employee_id = ?) 
     OR 
     (current_responsible_employee_id IS NULL AND responsible_employee_id = ?)
   )
   ```

### 2. Company Selection
1. User คลิกปุ่ม "ดูรายละเอียด" ในตารางบริษัท
2. เปิด `CompanyDetailModal`
3. Modal ดึงข้อมูล:
   - Submissions จาก `GET /api/document-entry-work?build={build}&year={year}&month={month}`
   - Company info จาก `GET /api/monthly-tax-data/{build}/{year}/{month}`
   - Employee details สำหรับ responsible employees

### 3. Status Update
1. User คลิกปุ่ม "เริ่มต้น" หรือ "เสร็จสิ้น" ใน SubmissionCard
2. เรียก `PATCH /api/document-entry-work/{id}/status` พร้อม `document_type` และ `status`
3. Backend:
   - ถ้า status = "กำลังดำเนินการ" → บันทึก `*_entry_start_datetime`, `*_status_updated_by`
   - ถ้า status = "ดำเนินการเสร็จแล้ว" → บันทึก `*_entry_completed_datetime`, `*_status_updated_by`
   - ถ้า status = "ยังไม่ดำเนินการ" → clear datetime fields
4. Invalidate queries เพื่อ refetch ข้อมูล

### 4. Start All
1. User คลิกปุ่ม "เริ่มต้นทั้งหมด"
2. เรียก `updateStatus()` สำหรับทุกประเภทที่มีสถานะ "ยังไม่ดำเนินการ" และ document_count > 0
3. Invalidate queries เพื่อ refetch ข้อมูล

### 5. Return Comment Update
1. User พิมพ์ความคิดเห็นส่งคืนงานคีย์ใน Textarea
2. User คลิกปุ่ม "บันทึก ความคิดเห็นส่งคืนงานคีย์" → เรียก `PUT /api/document-entry-work/{id}` พร้อม `return_comment`
3. Explicit save และแสดง notification
4. ปุ่มจะ disabled เมื่อ:
   - กำลัง loading
   - ค่า comment ไม่ได้เปลี่ยนแปลง
   - สถานะเป็น "เสร็จสิ้น" แล้ว (ทุกประเภทเอกสารเสร็จแล้ว)

## 🔔 Notification System

### Status Update Notification
เมื่ออัพเดทสถานะสำเร็จ:
- Success notification: "อัพเดทสถานะสำเร็จ"
- Error notification: แสดง error message จาก API
- **Accounting Responsible Notification**: ส่ง notification ไปยัง `accounting_responsible` เมื่อสถานะเปลี่ยนเป็น:
  - "กำลังดำเนินการ" → notification type: `document_entry_pending` (yellow)
  - "ดำเนินการเสร็จแล้ว" → notification type: `document_entry_completed` (green)
- **Notification Source**: ดึง `accounting_responsible` จาก `work_assignments` (priority) หรือ `monthly_tax_data` (fallback)

### Return Comment Notification
เมื่อบันทึกความคิดเห็นส่งคืนงานคีย์สำเร็จ:
- Success notification: "บันทึกความคิดเห็นสำเร็จ"
- Error notification: แสดง error message จาก API
- **Accounting Responsible Notification**: ส่ง notification ไปยัง `accounting_responsible` เมื่อ `return_comment` ถูกอัพเดท
  - Notification type: `document_entry_pending` (orange)
  - Message: แสดง preview ของ comment ที่ถูกอัพเดท

## 📡 API Endpoints

### GET /api/document-entry-work
ดึงรายการงานคีย์เอกสาร (paginated, filter)

**Query Parameters**:
- `document_entry_responsible` - Filter by document entry responsible employee_id
  - Logic: ถ้า `current_responsible_employee_id` มีค่า → ใช้ `current_responsible_employee_id`
  - ถ้า `current_responsible_employee_id` เป็น NULL → ใช้ `responsible_employee_id`

**Used By**: `CompanyList` component

### GET /api/document-entry-work/summary
สรุปข้อมูลการคีย์เอกสาร (รายวัน/รายเดือน)

**Query Parameters**:
- `year` (required)
- `month` (required)
- `document_entry_responsible` (required)
- `group_by` (optional, 'day' | 'month')

**Response Structure**:
```json
{
  "success": true,
  "data": [
    {
      "date": "2026-02-03", // เมื่อ group_by = 'day'
      "month": null,
      "items": [
        {
          "build": "001",
          "company_name": "Company Name",
          "wht_document_count": 1,
          "wht_entry_status": "ดำเนินการเสร็จแล้ว",
          "vat_document_count": 3,
          "vat_entry_status": "ดำเนินการเสร็จแล้ว",
          "non_vat_document_count": 3,
          "non_vat_entry_status": "ดำเนินการเสร็จแล้ว",
          "total_documents": 7,
          "completed_documents": 7,
          "pending_documents": 0
        }
      ],
      "total_documents": 7,
      "completed_documents": 7,
      "pending_documents": 0
    }
  ],
  "overall": {
    "total_documents": 7,
    "completed_documents": 7,
    "pending_documents": 0
  },
  "group_by": "day"
}
```

**Features**:
- แสดงเฉพาะรายการที่มี `completed_documents > 0`
- แต่ละ group (วัน/เดือน) มีรายละเอียดแต่ละ Build พร้อมสถานะของแต่ละประเภทเอกสาร
- `overall` เป็นผลรวมของทุกวัน/เดือน

**Used By**: `DocumentEntrySummary` component

### PATCH /api/document-entry-work/:id/status
อัพเดทสถานะการคีย์เอกสาร (WHT, VAT, Non-VAT)

**Request Body**:
```json
{
  "document_type": "wht" | "vat" | "non_vat",
  "status": "ยังไม่ดำเนินการ" | "กำลังดำเนินการ" | "ดำเนินการเสร็จแล้ว"
}
```

**Used By**: `SubmissionCard`, `StartAllButton` components

### PUT /api/document-entry-work/:id
แก้ไขข้อมูลงานคีย์เอกสาร (รวม `return_comment`)

**Used By**: `CompanyDetailModal` component (for return comment)

### GET /api/monthly-tax-data/:build/:year/:month
ดึงข้อมูล monthly tax data สำหรับแสดงข้อมูลบริษัทและผู้รับผิดชอบ

**Used By**: `CompanyDetailModal` component

**Documentation**: `Documentation/API/DOCUMENT_ENTRY_WORK_API.md`

## 🧩 Components

### Main Page Component
- **File**: `src/pages/DocumentEntry.tsx`
- **Features**:
  - Header Section (Orange Card) พร้อมปุ่มรีเฟรช
  - Summary Section (`DocumentEntrySummary`)
  - Company List Section (`CompanyList`)
  - Company Detail Modal (`CompanyDetailModal`)

### Sub-Components
1. **DocumentEntrySummary** (`src/components/DocumentEntry/DocumentEntrySummary.tsx`)
   - แสดงสรุปข้อมูล 3 ตัวเลข
   - ปุ่มสรุปรายวัน/รายเดือน
   - ใช้ `getSummary()` API

2. **CompanyList** (`src/components/DocumentEntry/CompanyList.tsx`)
   - แสดงตารางรายการบริษัท
   - สรุปจำนวนบริษัททั้งหมด/รอดำเนินการ/ดำเนินเสร็จแล้ว
   - คำนวณสถานะจาก entry statuses
   - ใช้ `getList()` API with `document_entry_responsible` filter

3. **CompanyDetailSection** (`src/components/DocumentEntry/CompanyDetailSection.tsx`)
   - Component สำหรับแสดงรายละเอียดบริษัทแบบ inline (ไม่ใช่ Modal)
   - แสดงข้อมูลบริษัทและผู้รับผิดชอบต่างๆ (ดึงจาก `work_assignments` และ `monthly_tax_data`)
   - แสดงรายการ submissions ในรูปแบบ Accordion
   - ใช้ `SubmissionCard` และ `StartAllButton` components
   - รองรับการแก้ไข return comment (explicit save ด้วยปุ่มบันทึก)
   - ป้องกันการอัพเดทเมื่อสถานะเป็น "เสร็จสิ้น" แล้ว

4. **SubmissionCard** (`src/components/DocumentEntry/SubmissionCard.tsx`)
   - Card สำหรับแสดงข้อมูลแต่ละประเภทเอกสาร
   - ปุ่มเริ่มต้น/เสร็จสิ้น
   - ใช้ `updateStatus()` API

5. **StartAllButton** (`src/components/DocumentEntry/StartAllButton.tsx`)
   - ปุ่มสำหรับเริ่มต้นทุกประเภทเอกสารพร้อมกัน
   - ใช้ `updateStatus()` API สำหรับทุกประเภทที่รอดำเนินการ

## 📦 Service Layer

### documentEntryWorkService
**File**: `src/services/documentEntryWorkService.ts`

**Functions**:
- `getList(params)` - ดึงรายการงานคีย์เอกสาร (รองรับ `document_entry_responsible` parameter)
- `getSummary(params)` - ดึงข้อมูลสรุป (รายวัน/รายเดือน)
- `updateStatus(id, data)` - อัพเดทสถานะการคีย์เอกสาร
- `update(id, data)` - แก้ไขข้อมูลงานคีย์เอกสาร (รวม `return_comment`)

## 🗄️ Database Schema

### document_entry_work
ตารางหลักสำหรับเก็บข้อมูลงานคีย์เอกสาร

**Key Fields**:
- `responsible_employee_id` - พนักงานที่รับผิดชอบในการคีย์ (ผู้รับผิดชอบเดิม)
- `current_responsible_employee_id` - พนักงานที่รับผิดชอบในการคีย์ - คนปัจจุบัน (ถ้ามี)
- `wht_entry_status`, `vat_entry_status`, `non_vat_entry_status` - สถานะการคีย์
- `wht_entry_start_datetime`, `vat_entry_start_datetime`, `non_vat_entry_start_datetime` - เวลาเริ่มต้น
- `wht_entry_completed_datetime`, `vat_entry_completed_datetime`, `non_vat_entry_completed_datetime` - เวลาดำเนินการเสร็จ
- `wht_status_updated_by`, `vat_status_updated_by`, `non_vat_status_updated_by` - ผู้อัพเดทสถานะ
- `submission_comment` - ความคิดเห็นส่งมอบงานคีย์
- `return_comment` - ความคิดเห็นส่งคืนงานคีย์

**Migration**: `migrations/015_create_document_entry_work_table.sql`

## 🎨 UI/UX Guidelines

### Layout
- **Container**: Fluid container with padding
- **Header**: Orange card with title and description
- **Summary Cards**: 3 cards (Total, Completed, Pending)
- **Company Table**: Scrollable table with sticky columns
- **Modal**: Large modal (xl size) with scrollable content

### Colors
- **Primary**: Orange (#ff6b35, #ff8c42)
- **Success**: Green (#388e3c)
- **Pending**: Gray (#757575)
- **In Progress**: Yellow

### Status Badges
- "เสร็จสิ้น" - Green badge
- "กำลังดำเนินการ" - Yellow badge
- "รอดำเนินการ" - Red/Gray badge

## 🔄 State Management

### Page State
- `selectedBuild` - Selected company build code
- `selectedCompanyName` - Selected company name
- `isModalOpen` - Modal open state
- `isRefreshing` - Loading state สำหรับปุ่มรีเฟรช

### React Query
- **Query Keys**:
  - `['document-entry-work', 'summary', year, month, employeeId, summaryMode]`
  - `['document-entry-work', 'company-list', year, month, employeeId]`
  - `['document-entry-work', 'submissions', build, year, month]`
  - `['monthly-tax-data', 'company-detail', build, year, month]`
- **Stale Time**: 1-5 minutes (depending on data type)
- **Cache Time**: 5-10 minutes

## ✅ Validation

### Status Update
- `document_type` - Required, must be: 'wht', 'vat', or 'non_vat'
- `status` - Required, must be: 'ยังไม่ดำเนินการ', 'กำลังดำเนินการ', or 'ดำเนินการเสร็จแล้ว'

### Summary
- `year` - Required
- `month` - Required (1-12)
- `document_entry_responsible` - Required (employee_id)

## 🐛 Error Handling

### API Errors
- **400 Bad Request**: Invalid parameters (document_type, status)
- **404 Not Found**: Document entry work not found
- **500 Internal Server Error**: Server error

### User Feedback
- Success notification when status update succeeds
- Success notification when return comment saves
- Error notification when API calls fail
- Loading state during API calls

## 📝 Recent Updates

### 2026-02-04 (Latest)
- ✅ **เปลี่ยนจาก Modal เป็น Inline Display**: แสดงรายละเอียดบริษัทแบบ inline ด้านล่างแถวที่เลือก แทนการเปิด Modal
- ✅ **เพิ่มข้อมูลบริษัทเพิ่มเติม**: แสดงเลขทะเบียนนิติบุคคล, ไซต์บริษัท, สถานะจดทะเบียนภาษี, วันที่จดภาษีมูลค่าเพิ่ม, ที่อยู่บริษัท
- ✅ **ปรับปรุงการดึงข้อมูลผู้รับผิดชอบ**: ดึง `accounting_responsible` และ `tax_inspection_responsible` จาก `work_assignments` (priority) พร้อม fallback ไปยัง `monthly_tax_data`
- ✅ **ปรับปรุง Status Badge และ Styling**:
  - "เสร็จสิ้น" badge มีกรอบสีเขียว (#51cf66)
  - "เสร็จบางส่วน" badge มีกรอบสีเหลือง (#fab005)
  - ซ่อน status badge เมื่อสถานะเป็น "ดำเนินการเสร็จแล้ว" (เพราะมีปุ่ม "เสร็จสิ้น" แสดงอยู่แล้ว)
- ✅ **เพิ่ม Notification System**:
  - ส่ง notification ไปยัง `accounting_responsible` เมื่อสถานะเปลี่ยนเป็น "กำลังดำเนินการ" หรือ "ดำเนินการเสร็จแล้ว"
  - ส่ง notification เมื่อ `return_comment` ถูกอัพเดท
- ✅ **ปรับปรุง Return Comment**: เปลี่ยนจาก auto-save เป็นปุ่ม "บันทึก ความคิดเห็นส่งคืนงานคีย์" สำหรับบันทึกแบบ explicit
- ✅ **ปรับปรุง Summary Statistics**: เปลี่ยนจากการนับบริษัทเป็นการนับงาน (entries) แบ่งเป็น:
  - งานทั้งหมด
  - ดำเนินการเสร็จทั้งหมด
  - ดำเนินการเสร็จบางส่วน
  - ยังไม่ดำเนินการ
- ✅ **ปรับปรุง Summary API**: แสดงรายละเอียดแต่ละ Build ในสรุปรายวัน/รายเดือน พร้อมข้อมูล:
  - Build, ชื่อบริษัท
  - จำนวนและสถานะของเอกสารหัก ณ ที่จ่าย (WHT)
  - จำนวนและสถานะของเอกสารมีภาษีมูลค่าเพิ่ม (VAT)
  - จำนวนและสถานะของเอกสารไม่มีภาษีมูลค่าเพิ่ม (Non-VAT)
  - จำนวนที่เสร็จสิ้นแล้ว (แสดงเฉพาะในคอลัมน์ "รวม")
- ✅ **ปรับปรุง UI/UX**:
  - ปุ่ม "สรุปข้อมูลรายวัน" และ "สรุปข้อมูลรายเดือน" มีพื้นหลังสีขาว กรอบสีส้ม ฟอนต์สีส้ม
  - แสดงข้อมูลสรุปในรูปแบบ Accordion ที่สามารถขยายดูรายละเอียดได้
  - ตารางแสดงรายละเอียดแต่ละ Build พร้อมสถานะการดำเนินงาน
- ✅ **เพิ่มการป้องกันการอัพเดท**: เมื่อสถานะเป็น "เสร็จสิ้น" (ทุกประเภทเอกสารเสร็จแล้ว) จะ disable การอัพเดททั้งหมด:
  - ปุ่ม "เสร็จสิ้น" ในแต่ละ SubmissionCard
  - Textarea และปุ่มบันทึก "ความคิดเห็นส่งคืนงานคีย์"
  - ปุ่ม "เริ่มต้นทั้งหมด"
  - ปุ่ม "เริ่มต้น" และ "เสร็จสิ้น" ใน SubmissionCard ทั้ง 3 ประเภท

### 2026-02-04 (Initial)
- ✅ Created Document Entry page with all components
- ✅ Integrated with backend API
- ✅ Added summary statistics (overall, daily, monthly)
- ✅ Added company list with status calculation
- ✅ Added company detail modal with submissions
- ✅ Added status update functionality (WHT, VAT, Non-VAT)
- ✅ Added start all button for bulk status update
- ✅ Added return comment editing with auto-save
- ✅ Implemented filtering by document_entry_responsible (with current_responsible_employee_id logic)
- ✅ Added refresh button

---

**Last Updated**: 2026-02-04
