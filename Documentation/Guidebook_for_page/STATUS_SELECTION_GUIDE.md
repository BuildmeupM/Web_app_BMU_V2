# 📊 คู่มือการเลือกสถานะของแต่ละหน้า - Tax Status Selection Guide

## 🎯 วัตถุประสงค์

เอกสารนี้อธิบายการเลือกสถานะของแต่ละหน้าในระบบภาษี (หน้าตรวจภาษี, หน้าสถานะยื่นภาษี, หน้ายื่นภาษี) ว่าต่างกันอย่างไรและทำงานอย่างไร

**Component**: `src/components/TaxInspection/TaxInspectionForm.tsx`  
**Function**: `getSelectData(currentStatus, mainStatus)`

---

## 📋 ภาพรวม

ระบบจะแสดงสถานะทั้งหมดที่อาจมีในฐานข้อมูล แต่จะ **disable สถานะที่ไม่ให้เลือก** ตาม role ของผู้ใช้แต่ละหน้า

### หลักการทำงาน

1. **แสดงทุกสถานะ**: แสดงสถานะทั้งหมดที่อาจมีในฐานข้อมูล เพื่อให้ผู้ใช้เห็นสถานะทั้งหมดแม้ว่าจะไม่สามารถเลือกได้
2. **Role-based Selection**: สถานะที่เลือกได้จะแตกต่างกันตาม role ของผู้ใช้แต่ละหน้า
3. **Disabled Status**: สถานะที่ไม่ให้เลือกจะถูก disable แต่ยังแสดงใน dropdown เพื่อให้เห็นค่าจากฐานข้อมูล
4. **เรียงลำดับสถานะ**: สถานะที่เลือกได้จะแสดงอยู่ด้านบน สถานะที่เลือกไม่ได้จะแสดงอยู่ด้านล่าง เพื่อให้ใช้งานง่ายขึ้น
5. **Backward Compatibility**: ระบบจะแปลงสถานะเก่า (`receipt`, `inquiry`, `review`, `edit`) เป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล แต่จะไม่แสดงใน dropdown

---

## 1. 🔍 หน้าตรวจภาษี (Tax Inspection) - ผู้ตรวจภาษี

**Route**: `/tax-inspection`  
**Component**: `src/pages/TaxInspection.tsx`  
**Source Page**: `sourcePage === 'taxInspection'` (default)

### ผู้ใช้
- ✅ **admin**
- ✅ **audit**

### สถานะที่เลือกได้ (7 สถานะ)

| สถานะ (Value) | ข้อความ (Label) | สี | คำอธิบาย |
|--------------|----------------|-----|----------|
| `received_receipt` | รับใบเสร็จ | สีฟ้า (#4facfe) | รับเอกสารใบเสร็จจากลูกค้า |
| `paid` | ชำระแล้ว | สีเหลือง (#ffc107) | ลูกค้าชำระเงินแล้ว |
| `passed` | ผ่าน | สีเขียว (#4caf50) | ตรวจสอบแล้วผ่าน |
| `needs_correction` | แก้ไข | สีแดง (#f44336) | ต้องแก้ไขข้อมูล |
| `inquire_customer` | สอบถามลูกค้าเพิ่มเติม | สีม่วง (#9c27b0) | ต้องสอบถามข้อมูลเพิ่มเติมจากลูกค้า |
| `additional_review` | ตรวจสอบเพิ่มเติม | สีฟ้าอ่อน (#81d4fa) | ต้องตรวจสอบข้อมูลเพิ่มเติม |
| `not_submitted` | ไม่มียื่น | สีดำ (#000000) | ไม่มีการยื่นภาษี |

### สถานะที่แสดงแต่เลือกไม่ได้ (Disabled)

| สถานะ (Value) | ข้อความ (Label) | สี | เหตุผล |
|--------------|----------------|-----|--------|
| `pending_review` | รอตรวจ | สีส้ม (#ff6b35) | สถานะของผู้ทำบัญชี |
| `pending_recheck` | รอตรวจอีกครั้ง | สีแดงอ่อน (#ffcdd2) | สถานะของผู้ทำบัญชี |
| `draft_ready` | ร่างแบบได้ | สีแดงอ่อน (#ffcdd2) | สถานะของผู้ทำบัญชี |
| `sent_to_customer` | ส่งลูกค้าแล้ว | สีฟ้าอ่อน (#81d4fa) | สถานะของผู้ทำบัญชี |
| `draft_completed` | ร่างแบบเสร็จแล้ว | สีส้มอ่อน (#ffb74d) | สถานะของผู้ทำบัญชี |
| `not_started` | สถานะยังไม่ดำเนินการ | สีเทา (#808080) | สถานะของผู้ทำบัญชี |

### Backward Compatibility

ระบบจะแปลงสถานะเก่าเป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล:

| สถานะเก่า | สถานะใหม่ | หมายเหตุ |
|----------|----------|----------|
| `receipt` | `received_receipt` | แปลงอัตโนมัติ |
| `inquiry` | `inquire_customer` | แปลงอัตโนมัติ |
| `review` | `additional_review` | แปลงอัตโนมัติ |
| `edit` | `needs_correction` | แปลงอัตโนมัติ |

**หมายเหตุ**: สถานะเก่าเหล่านี้จะไม่แสดงใน dropdown แต่ระบบจะรองรับข้อมูลเก่าในฐานข้อมูลโดยแปลงเป็นสถานะใหม่อัตโนมัติ

### การทำงาน

ผู้ตรวจภาษีตรวจเอกสารที่ผู้ทำบัญชีส่งมา และเลือกสถานะผลการตรวจ:
- **ผ่าน** (`passed`) - เมื่อข้อมูลถูกต้อง
- **แก้ไข** (`needs_correction`) - เมื่อข้อมูลต้องแก้ไข
- **สอบถามลูกค้าเพิ่มเติม** (`inquire_customer`) - เมื่อต้องสอบถามข้อมูลเพิ่มเติม
- **ตรวจสอบเพิ่มเติม** (`additional_review`) - เมื่อต้องตรวจสอบข้อมูลเพิ่มเติม

---

## 2. 📊 หน้าสถานะยื่นภาษี (Tax Status) - ผู้ทำบัญชี

**Route**: `/tax-status`  
**Component**: `src/pages/TaxStatus.tsx`  
**Source Page**: `sourcePage === 'taxStatus'`

### ผู้ใช้
- ✅ **admin**
- ✅ **data_entry_and_service**
- ✅ **service**

### สถานะที่เลือกได้ (13 สถานะ - ทุกสถานะ)

**⚠️ อัพเดท**: หน้าสถานะยื่นภาษีสามารถเลือกได้ทุกสถานะ (13 สถานะ)

| สถานะ (Value) | ข้อความ (Label) | สี | คำอธิบาย |
|--------------|----------------|-----|----------|
| `received_receipt` | รับใบเสร็จ | สีฟ้า (#4facfe) | รับเอกสารใบเสร็จจากลูกค้า |
| `paid` | ชำระแล้ว | สีเหลือง (#ffc107) | ลูกค้าชำระเงินแล้ว |
| `sent_to_customer` | ส่งลูกค้าแล้ว | สีฟ้าอ่อน (#81d4fa) | ส่งเอกสารให้ลูกค้าแล้ว |
| `draft_completed` | ร่างแบบเสร็จแล้ว | สีส้มอ่อน (#ffb74d) | ร่างแบบภาษีเสร็จแล้ว |
| `passed` | ผ่าน | สีเขียว (#4caf50) | ตรวจสอบแล้วผ่าน |
| `pending_review` | รอตรวจ | สีส้ม (#ff6b35) | รอการตรวจสอบ |
| `pending_recheck` | รอตรวจอีกครั้ง | สีแดงอ่อน (#ffcdd2) | รอการตรวจสอบอีกครั้ง |
| `draft_ready` | ร่างแบบได้ | สีแดงอ่อน (#ffcdd2) | ร่างแบบพร้อมส่งตรวจ |
| `needs_correction` | แก้ไข | สีแดง (#f44336) | ต้องแก้ไขข้อมูล |
| `inquire_customer` | สอบถามลูกค้าเพิ่มเติม | สีม่วง (#9c27b0) | ต้องสอบถามข้อมูลเพิ่มเติมจากลูกค้า |
| `additional_review` | ตรวจสอบเพิ่มเติม | สีฟ้าอ่อน (#81d4fa) | ต้องตรวจสอบข้อมูลเพิ่มเติม |
| `not_started` | สถานะยังไม่ดำเนินการ | สีเทา (#808080) | ยังไม่เริ่มดำเนินการ |
| `not_submitted` | ไม่มียื่น | สีดำ (#000000) | ไม่มีการยื่นภาษี |

### การเรียงลำดับสถานะ

**⚠️ อัพเดท**: สถานะที่เลือกได้จะแสดงอยู่ด้านบนของ dropdown สถานะที่เลือกไม่ได้จะแสดงอยู่ด้านล่าง เพื่อให้ใช้งานง่ายขึ้น

### Logic พิเศษ (เฉพาะหน้าสถานะยื่นภาษี)

#### 1. เมื่อสถานะหลักเป็น "รอตรวจอีกครั้ง"

**เงื่อนไข**: เมื่อสถานะหลัก (`pnd_status` หรือ `pp30_status`) เป็น `'pending_recheck'`

**ผลลัพธ์**: 
- ✅ **ปลดล็อคสถานะเบื้องต้นทั้งหมด** (enable ทุกสถานะ)
- ✅ ผู้ทำบัญชีสามารถแก้ไขสถานะเบื้องต้นได้ (เช่น `pnd1_40_1`, `pnd3`, `pp36`, etc.)

**เหตุผล**: เมื่อเลือกสถานะ "รอตรวจอีกครั้ง" แล้ว ต้องให้ผู้ทำบัญชีสามารถแก้ไขสถานะเบื้องต้นได้ก่อนส่งตรวจอีกรอบ

#### 2. เมื่อสถานะปัจจุบันเป็น "แก้ไข"

**เงื่อนไข**: เมื่อสถานะปัจจุบัน (`currentStatus`) เป็น `'edit'` หรือ `'needs_correction'`

**ผลลัพธ์**: 
- ✅ Enable: `pending_recheck` (รอตรวจอีกครั้ง)
- ✅ Enable: `passed` (ผ่าน)

**Flow การทำงาน**:
- ผู้ทำบัญชีได้รับสถานะ "แก้ไข" จากผู้ตรวจภาษี
- สามารถเลือก:
  - **"ผ่าน"** (`passed`) - ไม่ต้องส่งตรวจใหม่ (แก้ไขเสร็จแล้ว)
  - **"รอตรวจอีกครั้ง"** (`pending_recheck`) - ต้องส่งตรวจอีกรอบ (ยังต้องแก้ไขเพิ่มเติม)

#### 3. เมื่อสถานะปัจจุบันเป็น "สอบถามลูกค้าเพิ่มเติม" หรือ "ตรวจสอบเพิ่มเติม"

**เงื่อนไข**: เมื่อสถานะปัจจุบัน (`currentStatus`) เป็น `'inquire_customer'` หรือ `'additional_review'`

**ผลลัพธ์**: 
- ✅ Enable: `pending_recheck` (รอตรวจอีกครั้ง)

**Flow การทำงาน**:
- ผู้ทำบัญชีได้รับสถานะ "สอบถามลูกค้าเพิ่มเติม" หรือ "ตรวจสอบเพิ่มเติม" จากผู้ตรวจภาษี
- สามารถเลือก:
  - **"รอตรวจอีกครั้ง"** (`pending_recheck`) - ต้องส่งตรวจอีกรอบ (หลังจากสอบถามหรือตรวจสอบเพิ่มเติมแล้ว)

### Flow การทำงานทั้งหมด

```
1. ผู้ทำบัญชีส่ง "รอตรวจ" (pending_review)
   ↓
2. ผู้ตรวจภาษีตรวจและส่งสถานะกลับมา:
   - แก้ไข (needs_correction)
   - ผ่าน (passed)
   - สอบถามลูกค้าเพิ่มเติม (inquire_customer)
   - ตรวจสอบเพิ่มเติม (additional_review)
   ↓
3. ผู้ทำบัญชีได้รับสถานะจากผู้ตรวจภาษี:
   
   กรณีที่ 1: แก้ไข (needs_correction)
   ├─ เลือก "ผ่าน" (passed) → ไม่ต้องส่งตรวจใหม่
   └─ เลือก "รอตรวจอีกครั้ง" (pending_recheck) → ต้องส่งตรวจอีกรอบ
       └─ เมื่อเลือก "รอตรวจอีกครั้ง" → ปลดล็อคสถานะเบื้องต้นทั้งหมด (enable ทุกสถานะ)
           └─ แก้ไขสถานะเบื้องต้นได้ (เช่น pnd1_40_1, pnd3, etc.)
   
   กรณีที่ 2: สอบถามลูกค้าเพิ่มเติม (inquire_customer)
   └─ เลือก "รอตรวจอีกครั้ง" (pending_recheck) → ต้องส่งตรวจอีกรอบ
       └─ เมื่อเลือก "รอตรวจอีกครั้ง" → ปลดล็อคสถานะเบื้องต้นทั้งหมด (enable ทุกสถานะ)
           └─ แก้ไขสถานะเบื้องต้นได้
   
   กรณีที่ 3: ตรวจสอบเพิ่มเติม (additional_review)
   └─ เลือก "รอตรวจอีกครั้ง" (pending_recheck) → ต้องส่งตรวจอีกรอบ
       └─ เมื่อเลือก "รอตรวจอีกครั้ง" → ปลดล็อคสถานะเบื้องต้นทั้งหมด (enable ทุกสถานะ)
           └─ แก้ไขสถานะเบื้องต้นได้
```

---

## 3. 📝 หน้ายื่นภาษี (Tax Filing) - พนักงานยื่นภาษี

**Route**: `/tax-filing`  
**Component**: `src/pages/TaxFiling.tsx`  
**Source Page**: `sourcePage === 'taxFiling'`

### ผู้ใช้
- ✅ **admin**
- ✅ **data_entry_and_service**

### สถานะที่เลือกได้ (4 สถานะ)

| สถานะ (Value) | ข้อความ (Label) | สี | คำอธิบาย |
|--------------|----------------|-----|----------|
| `received_receipt` | รับใบเสร็จ | สีฟ้า (#4facfe) | รับเอกสารใบเสร็จจากลูกค้า |
| `paid` | ชำระแล้ว | สีเหลือง (#ffc107) | ลูกค้าชำระเงินแล้ว |
| `sent_to_customer` | ส่งลูกค้าแล้ว | สีฟ้าอ่อน (#81d4fa) | ส่งเอกสารให้ลูกค้าแล้ว |
| `draft_completed` | ร่างแบบเสร็จแล้ว | สีส้มอ่อน (#ffb74d) | ร่างแบบภาษีเสร็จแล้ว |

### สถานะที่แสดงแต่เลือกไม่ได้ (Disabled)

| สถานะ (Value) | ข้อความ (Label) | สี | เหตุผล |
|--------------|----------------|-----|--------|
| `needs_correction` | แก้ไข | สีแดง (#f44336) | สถานะของผู้ตรวจภาษี |
| `inquire_customer` | สอบถามลูกค้าเพิ่มเติม | สีม่วง (#9c27b0) | สถานะของผู้ตรวจภาษี |
| `additional_review` | ตรวจสอบเพิ่มเติม | สีฟ้าอ่อน (#81d4fa) | สถานะของผู้ตรวจภาษี |
| `not_started` | สถานะยังไม่ดำเนินการ | สีเทา (#808080) | สถานะของผู้ทำบัญชี |
| `not_submitted` | ไม่มียื่น | สีดำ (#000000) | สถานะของผู้ทำบัญชี |
| `pending_review` | รอตรวจ | สีส้ม (#ff6b35) | สถานะของผู้ทำบัญชี |
| `pending_recheck` | รอตรวจอีกครั้ง | สีแดงอ่อน (#ffcdd2) | สถานะของผู้ทำบัญชี |
| `draft_ready` | ร่างแบบได้ | สีแดงอ่อน (#ffcdd2) | สถานะของผู้ทำบัญชี |
| `passed` | ผ่าน | สีเขียว (#4caf50) | สถานะของผู้ตรวจภาษี |

### การทำงาน

พนักงานยื่นภาษีเลือกสถานะการยื่นภาษี:
- **รับใบเสร็จ** (`received_receipt`) - เมื่อรับเอกสารจากลูกค้า
- **ชำระแล้ว** (`paid`) - เมื่อลูกค้าชำระเงินแล้ว
- **ส่งลูกค้าแล้ว** (`sent_to_customer`) - เมื่อส่งเอกสารให้ลูกค้าแล้ว
- **ร่างแบบเสร็จแล้ว** (`draft_completed`) - เมื่อร่างแบบภาษีเสร็จแล้ว

---

## 🔧 Implementation Details

### Function: `getSelectData(currentStatus, mainStatus)`

**Location**: `src/components/TaxInspection/TaxInspectionForm.tsx` (lines 340-397)

**Parameters**:
- `currentStatus` (optional): สถานะปัจจุบันของ field ที่กำลัง render (เช่น `pnd1_40_1`, `pnd3`, etc.)
- `mainStatus` (optional): สถานะหลัก (`pnd_status` หรือ `pp30_status`)

**Logic**:

```typescript
const getSelectData = (currentStatus?: string | null, mainStatus?: string | null) => {
  // ตรวจสอบ sourcePage
  const validSourcePages: Array<'taxInspection' | 'taxStatus' | 'taxFiling'> = ['taxInspection', 'taxStatus', 'taxFiling']
  const currentSourcePage = validSourcePages.includes(sourcePage) ? sourcePage : 'taxInspection'
  
  const isTaxStatusPage = currentSourcePage === 'taxStatus'
  const allowedStatuses = allowedStatusesByPage[currentSourcePage] || []
  
  // ตรวจสอบว่าสถานะหลักเป็น "รอตรวจอีกครั้ง" หรือไม่
  const isMainStatusPendingRecheck = mainStatus === 'pending_recheck'
  
  // ตรวจสอบว่าสถานะปัจจุบันเป็นสถานะที่ผู้ตรวจภาษีส่งกลับมาหรือไม่
  const isEditStatus = currentStatus === 'edit' || currentStatus === 'needs_correction'
  const isInquireCustomerStatus = currentStatus === 'inquire_customer'
  const isAdditionalReviewStatus = currentStatus === 'additional_review'
  
  // สถานะที่ต้อง enable เมื่อสถานะปัจจุบันเป็น "แก้ไข"
  const allowedStatusesWhenEdit = ['pending_recheck', 'passed']
  
  // สถานะที่ต้อง enable เมื่อสถานะปัจจุบันเป็น "สอบถามลูกค้าเพิ่มเติม" หรือ "ตรวจสอบเพิ่มเติม"
  const allowedStatusesWhenInquireOrReview = ['pending_recheck']
  
  // แยกสถานะที่เลือกได้และเลือกไม่ได้ แล้วเรียงให้สถานะที่เลือกได้อยู่ด้านบน
  const enabledOptions: Array<{ value: string; label: string; disabled: boolean }> = []
  const disabledOptions: Array<{ value: string; label: string; disabled: boolean }> = []
  
  allStatusOptions.forEach((s) => {
    // เริ่มต้นด้วยการตรวจสอบว่าสถานะนี้เลือกได้ตาม sourcePage หรือไม่
    let isDisabled = !allowedStatuses.includes(s.value)
    
    // Logic พิเศษสำหรับหน้าสถานะยื่นภาษี (taxStatus)
    if (isTaxStatusPage) {
      // กรณีที่ 1: ถ้าสถานะหลักเป็น "รอตรวจอีกครั้ง" → ปลดล็อคสถานะเบื้องต้นทั้งหมด
      if (isMainStatusPendingRecheck) {
        isDisabled = false
      }
      // กรณีที่ 2: ถ้าสถานะปัจจุบันเป็น "แก้ไข" → enable "รอตรวจอีกครั้ง" และ "ผ่าน"
      else if (isEditStatus && allowedStatusesWhenEdit.includes(s.value)) {
        isDisabled = false
      }
      // กรณีที่ 3: ถ้าสถานะปัจจุบันเป็น "สอบถามลูกค้าเพิ่มเติม" หรือ "ตรวจสอบเพิ่มเติม" → enable "รอตรวจอีกครั้ง"
      else if (
        (isInquireCustomerStatus || isAdditionalReviewStatus) &&
        allowedStatusesWhenInquireOrReview.includes(s.value)
      ) {
        isDisabled = false
      }
    }
    
    const option = {
      value: s.value,
      label: s.label,
      disabled: isDisabled,
    }
    
    // แยกสถานะที่เลือกได้และเลือกไม่ได้
    if (isDisabled) {
      disabledOptions.push(option)
    } else {
      enabledOptions.push(option)
    }
  })
  
  // เรียงให้สถานะที่เลือกได้อยู่ด้านบน สถานะที่เลือกไม่ได้อยู่ด้านล่าง
  return [...enabledOptions, ...disabledOptions]
}
```

### All Status Options

**Location**: `src/components/TaxInspection/TaxInspectionForm.tsx` (lines 209-224)

**สถานะทั้งหมด (13 สถานะ)**:
- `received_receipt` - รับใบเสร็จ
- `paid` - ชำระแล้ว
- `sent_to_customer` - ส่งลูกค้าแล้ว
- `draft_completed` - ร่างแบบเสร็จแล้ว
- `passed` - ผ่าน
- `pending_review` - รอตรวจ
- `pending_recheck` - รอตรวจอีกครั้ง
- `draft_ready` - ร่างแบบได้
- `needs_correction` - แก้ไข
- `inquire_customer` - สอบถามลูกค้าเพิ่มเติม
- `additional_review` - ตรวจสอบเพิ่มเติม
- `not_started` - สถานะยังไม่ดำเนินการ
- `not_submitted` - ไม่มียื่น

**หมายเหตุ**: สถานะเก่า (`receipt`, `inquiry`, `review`, `edit`) จะไม่แสดงใน dropdown แต่ระบบจะแปลงเป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล

### Allowed Statuses by Page

**Location**: `src/components/TaxInspection/TaxInspectionForm.tsx` (lines 235-264)

```typescript
const allowedStatusesByPage: Record<'taxInspection' | 'taxStatus' | 'taxFiling', string[]> = {
  taxInspection: [
    'received_receipt', 'paid', 'passed', 'needs_correction',
    'inquire_customer', 'additional_review', 'not_submitted',
  ],
  taxStatus: [
    // ทุกสถานะ (13 สถานะ)
    'received_receipt', 'paid', 'sent_to_customer', 'draft_completed',
    'passed', 'pending_review', 'pending_recheck', 'draft_ready',
    'needs_correction', 'inquire_customer', 'additional_review',
    'not_started', 'not_submitted',
  ],
  taxFiling: [
    'received_receipt', 'paid', 'draft_completed',
  ],
}
```

### Backward Compatibility

**Location**: `src/components/TaxInspection/TaxInspectionForm.tsx` (lines 494-506)

ระบบจะแปลงสถานะเก่าเป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล:

```typescript
const statusMapping: Record<string, string> = {
  'receipt': 'received_receipt',
  'inquiry': 'inquire_customer',
  'review': 'additional_review',
  'edit': 'needs_correction',
}
```

### การเรียกใช้

**สถานะหลัก** (`pnd_status`, `pp30_status`):
```typescript
data={getSelectData(formValues['pnd_status'], formValues['pnd_status'])}
```

**สถานะเบื้องต้น** (เช่น `pnd1_40_1`, `pnd3`, etc.):
```typescript
data={getSelectData(formValues['pnd1_40_1'], formValues['pnd_status'])}
```

---

## 📊 สรุปเปรียบเทียบ

| หัวข้อ | หน้าตรวจภาษี | หน้าสถานะยื่นภาษี | หน้ายื่นภาษี |
|--------|-------------|-----------------|-------------|
| **ผู้ใช้** | ผู้ตรวจภาษี | ผู้ทำบัญชี | พนักงานยื่นภาษี |
| **Roles** | admin, audit | admin, data_entry_and_service, service | admin, data_entry_and_service |
| **สถานะที่เลือกได้** | 7 สถานะ | 13 สถานะ (ทุกสถานะ) | 3 สถานะ |
| **Logic พิเศษ** | ไม่มี | มี (enable สถานะตาม currentStatus และ mainStatus) | ไม่มี |
| **ปลดล็อคสถานะเบื้องต้น** | ไม่มี | มี (เมื่อ mainStatus = 'pending_recheck') | ไม่มี |
| **การเรียงลำดับ** | สถานะที่เลือกได้อยู่ด้านบน | สถานะที่เลือกได้อยู่ด้านบน | สถานะที่เลือกได้อยู่ด้านบน |

---

## 🎯 จุดสำคัญ

1. **แสดงทุกสถานะ**: ทุกหน้าจะแสดงสถานะทั้งหมดที่อาจมีในฐานข้อมูล แต่ disable สถานะที่ไม่ให้เลือก
2. **Role-based Selection**: สถานะที่เลือกได้จะแตกต่างกันตาม role ของผู้ใช้แต่ละหน้า
3. **การเรียงลำดับ**: สถานะที่เลือกได้จะแสดงอยู่ด้านบน สถานะที่เลือกไม่ได้จะแสดงอยู่ด้านล่าง เพื่อให้ใช้งานง่ายขึ้น
4. **Logic พิเศษสำหรับหน้าสถานะยื่นภาษี**: 
   - Enable สถานะบางสถานะตามสถานะปัจจุบัน (`currentStatus`)
   - ปลดล็อคสถานะเบื้องต้นทั้งหมดเมื่อสถานะหลักเป็น "รอตรวจอีกครั้ง"
   - **อัพเดท**: หน้าสถานะยื่นภาษีสามารถเลือกได้ทุกสถานะ (13 สถานะ)
5. **Backward Compatibility**: ระบบจะแปลงสถานะเก่า (`receipt`, `inquiry`, `review`, `edit`) เป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล แต่จะไม่แสดงใน dropdown
6. **Validation**: ระบบจะตรวจสอบ `sourcePage` เมื่อ component mount และแสดง warning ใน development mode ถ้า `sourcePage` ไม่ถูกต้อง

---

## 📝 หมายเหตุ

- ⚠️ **สำคัญ**: `label` เป็นแค่ข้อความที่แสดงใน UI เท่านั้น ค่าที่ส่งไปยังฐานข้อมูลคือ `value` (เช่น `'needs_correction'`, `'received_receipt'`) ไม่ใช่ `label`
- สถานะเก่า (`receipt`, `inquiry`, `review`, `edit`) จะถูกแปลงเป็นสถานะใหม่อัตโนมัติเมื่อโหลดข้อมูลจากฐานข้อมูล แต่จะไม่แสดงใน dropdown
- Logic พิเศษสำหรับหน้าสถานะยื่นภาษีจะทำงานเฉพาะเมื่อ `sourcePage === 'taxStatus'`
- ระบบจะเรียงลำดับสถานะให้สถานะที่เลือกได้อยู่ด้านบนเสมอ

## 🔄 การเปลี่ยนแปลงล่าสุด

### 2026-02-02
- ✅ เพิ่มระบบตรวจสอบ `sourcePage` ที่ชัดเจน
- ✅ แสดงสถานะทั้งหมดที่อาจมีในฐานข้อมูล (13 สถานะ)
- ✅ เรียงลำดับสถานะให้สถานะที่เลือกได้อยู่ด้านบน
- ✅ ลบสถานะเก่า (ผู้ตรวจภาษี) ออกจาก dropdown
- ✅ เพิ่มการแปลงสถานะเก่าเป็นสถานะใหม่อัตโนมัติ (backward compatibility)
- ✅ หน้าสถานะยื่นภาษีสามารถเลือกได้ทุกสถานะ (13 สถานะ)

---

**Last Updated**: 2026-02-02  
**Related Files**:
- `src/components/TaxInspection/TaxInspectionForm.tsx`
- `src/pages/TaxInspection.tsx`
- `src/pages/TaxStatus.tsx`
- `src/pages/TaxFiling.tsx`
