# 🚀 คู่มือการรัน Backend Server

## 📋 Overview

คู่มือนี้ใช้สำหรับการรัน Backend Server สำหรับระบบ BMU Work Management System

## ⚠️ สำคัญ

**Frontend จะไม่สามารถทำงานได้ถ้า Backend Server ไม่ได้รันอยู่!**

ถ้าพบ error `ERR_CONNECTION_REFUSED` หรือ `Network Error` หมายความว่า Backend Server ไม่ได้รันอยู่

## 🔧 ขั้นตอนการรัน Backend Server

### 1. เปิด Terminal/Command Prompt

- **Windows**: กด `Win + R` พิมพ์ `cmd` หรือ `powershell`
- **Mac/Linux**: เปิด Terminal

### 2. เข้าไปที่โฟลเดอร์ backend

```bash
cd backend
```

หรือถ้าอยู่ใน root directory:

```bash
cd "v:\A.โฟร์เดอร์หลัก\Build000 ทดสอบระบบ\ส่วนตัวเอ็ม\Web_app_BMU React\backend"
```

### 3. ตรวจสอบว่าได้ติดตั้ง dependencies แล้ว

```bash
npm install
```

### 4. ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend` (ถ้ายังไม่มี):

```bash
cp .env.example .env
```

แก้ไขไฟล์ `.env`:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=bmu_work_management
JWT_SECRET=your-super-secret-jwt-key
PORT=3001
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development
```

### 5. รัน Backend Server

#### Development Mode (แนะนำ)
```bash
npm run dev
```

หรือ

#### Production Mode
```bash
npm start
```

### 6. ตรวจสอบว่า Server รันอยู่

ถ้า Server รันสำเร็จ จะเห็นข้อความ:

```
🚀 Server is running on http://localhost:3001
📡 API Base URL: http://localhost:3001/api
🌐 CORS Origin: http://localhost:3000
📊 Environment: development
```

## ✅ การตรวจสอบว่า Backend Server รันอยู่

### วิธีที่ 1: ตรวจสอบใน Terminal
- ดูข้อความ "Server is running on http://localhost:3001"
- ไม่มี error messages

### วิธีที่ 2: เปิด Browser
- เปิด `http://localhost:3001/health`
- ควรเห็น JSON response:
```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "2026-01-31T..."
}
```

### วิธีที่ 3: ตรวจสอบใน Frontend
- เปิดหน้า Client Management
- ถ้า Backend รันอยู่ จะสามารถดึงข้อมูลได้
- ถ้า Backend ไม่ได้รัน จะเห็น error `ERR_CONNECTION_REFUSED`

## 🐛 การแก้ไขปัญหา

### ปัญหา: `ERR_CONNECTION_REFUSED`

**สาเหตุ**: Backend Server ไม่ได้รันอยู่

**วิธีแก้ไข**:
1. ตรวจสอบว่าได้รัน Backend Server แล้ว (ดูขั้นตอนด้านบน)
2. ตรวจสอบว่า Server รันที่ port 3001
3. ตรวจสอบว่าไม่มี process อื่นใช้ port 3001 อยู่

### ปัญหา: `Database connection error`

**สาเหตุ**: ไม่สามารถเชื่อมต่อกับ Database ได้

**วิธีแก้ไข**:
1. ตรวจสอบว่า MySQL Server รันอยู่
2. ตรวจสอบ credentials ในไฟล์ `.env`
3. ตรวจสอบว่า Database `bmu_work_management` มีอยู่แล้ว

### ปัญหา: `Port 3001 already in use`

**สาเหตุ**: มี process อื่นใช้ port 3001 อยู่

**วิธีแก้ไข**:
1. หา process ที่ใช้ port 3001:
   ```bash
   # Windows
   netstat -ano | findstr :3001
   
   # Mac/Linux
   lsof -i :3001
   ```
2. Kill process นั้น หรือเปลี่ยน PORT ในไฟล์ `.env`

## 📝 หมายเหตุ

- Backend Server ต้องรันอยู่ตลอดเวลาที่ใช้งาน Frontend
- ถ้า Backend Server หยุดทำงาน Frontend จะไม่สามารถเรียก API ได้
- แนะนำให้รัน Backend Server ใน Terminal แยกต่างหาก เพื่อดู logs และ errors

---

**Last Updated**: 2026-01-31
**Version**: 1.0.0
