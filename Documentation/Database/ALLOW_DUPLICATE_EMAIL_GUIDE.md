# 📧 คู่มือการรองรับ Email Build ซ้ำกัน

## 🎯 ภาพรวม

ระบบได้รับการปรับปรุงให้รองรับการนำเข้าข้อมูลที่มี **Email Build (company_email) ซ้ำกัน** ได้ โดยระบบจะแจ้งเตือนเมื่อพบข้อมูลซ้ำกัน แต่จะไม่ข้ามข้อมูลและจะนำเข้าทั้งหมด

## 🔄 การเปลี่ยนแปลง

### 1. Database Schema
- **ลบ UNIQUE constraint** จาก `company_email` ในตาราง `employees`
- **สร้าง index ธรรมดา** (ไม่ unique) เพื่อเพิ่มประสิทธิภาพการค้นหา

### 2. Import Logic
- **ไม่ข้ามข้อมูล** ที่มี Email Build ซ้ำกัน
- **แจ้งเตือน** เมื่อพบ Email Build ซ้ำกัน (ทั้งในไฟล์และในฐานข้อมูล)
- **นำเข้าข้อมูลทั้งหมด** แม้ว่า Email Build จะซ้ำกัน

### 3. Validation Logic
- **แจ้งเตือน** เมื่อพบ Email Build ซ้ำกัน
- **ไม่บล็อกการนำเข้า** ข้อมูลที่ซ้ำกัน

## 📋 Migration Script

### การรัน Migration

```bash
# รัน migration script
mysql -u [username] -p [database_name] < Documentation/Database/migrations/006_remove_company_email_unique.sql
```

หรือใช้ MySQL client:

```sql
-- รัน migration script
SOURCE Documentation/Database/migrations/006_remove_company_email_unique.sql;
```

### ตรวจสอบผลลัพธ์

```sql
-- ตรวจสอบว่า UNIQUE constraint ถูกลบแล้ว
SHOW INDEX FROM employees WHERE Column_name = 'company_email';

-- ควรเห็น index ธรรมดา (Non_unique = 1) แทน UNIQUE index (Non_unique = 0)
```

## ⚠️ คำเตือน

### ข้อควรระวัง

1. **ข้อมูลซ้ำกัน**: หลังจาก migration แล้ว ระบบจะอนุญาตให้มี `company_email` ซ้ำกันได้
2. **การค้นหา**: การค้นหาด้วย `company_email` อาจได้ผลลัพธ์หลายรายการ
3. **การเชื่อมโยง**: ถ้ามีระบบอื่นที่ใช้ `company_email` เป็น unique identifier อาจต้องปรับปรุง

### การจัดการข้อมูลซ้ำกัน

- **ตรวจสอบข้อมูล**: ใช้หน้าเว็บ "จัดการพนักงาน" เพื่อตรวจสอบข้อมูลที่ซ้ำกัน
- **แก้ไขข้อมูล**: แก้ไข Email Build ที่ซ้ำกันผ่านหน้าเว็บหรือ API
- **รายงาน**: ระบบจะแจ้งเตือนเมื่อพบ Email Build ซ้ำกันในระหว่างการนำเข้า

## 🔍 ตัวอย่างการใช้งาน

### การนำเข้าข้อมูลที่มี Email Build ซ้ำกัน

เมื่อนำเข้าข้อมูลที่มี Email Build ซ้ำกัน ระบบจะ:

1. **แจ้งเตือน** ใน validation results:
   ```
   ⚠️ Email Build ซ้ำในไฟล์ (แถว: 7, 35) - ระบบจะนำเข้าข้อมูลทั้งหมด (Email Build อาจซ้ำกัน)
   ```

2. **นำเข้าข้อมูลทั้งหมด** ไม่ข้ามข้อมูล

3. **แสดง warnings** ในผลลัพธ์การนำเข้า:
   ```json
   {
     "success": true,
     "data": {
       "total": 50,
       "success": 50,
       "warnings": [
         {
           "row": 7,
           "employee_id": "AC00020",
           "warning": "Email Build ซ้ำในไฟล์ - ระบบจะนำเข้าข้อมูลทั้งหมด (Email Build อาจซ้ำกัน)"
         }
       ]
     }
   }
   ```

## 📝 API Changes

### POST /api/employees/import

**Response (เมื่อมี Email Build ซ้ำกัน):**

```json
{
  "success": true,
  "message": "Import completed",
  "data": {
    "total": 50,
    "success": 50,
    "failed": 0,
    "updated": 0,
    "warnings": [
      {
        "row": 7,
        "employee_id": "AC00020",
        "warning": "Email Build ซ้ำในไฟล์ - ระบบจะนำเข้าข้อมูลทั้งหมด (Email Build อาจซ้ำกัน)"
      },
      {
        "row": 35,
        "employee_id": "AC00039",
        "warning": "Email Build ซ้ำในไฟล์ - ระบบจะนำเข้าข้อมูลทั้งหมด (Email Build อาจซ้ำกัน)"
      }
    ]
  }
}
```

## 🔄 Rollback (ถ้าต้องการย้อนกลับ)

ถ้าต้องการย้อนกลับไปใช้ UNIQUE constraint:

```sql
-- ลบ index ธรรมดา
DROP INDEX idx_employees_company_email ON employees;

-- สร้าง UNIQUE constraint ใหม่
ALTER TABLE employees ADD UNIQUE KEY uk_employees_company_email (company_email);
```

**หมายเหตุ**: การ rollback จะล้มเหลวถ้ามีข้อมูล `company_email` ซ้ำกันอยู่แล้ว ต้องแก้ไขข้อมูลให้ไม่ซ้ำกันก่อน

## 📚 Related Documentation

- [Employee Import Guide](./Employee/EXCEL_TEMPLATE_GUIDE.md)
- [Database Schema](./schema.md)
- [Employee API Design](../API/EMPLOYEE_API_DESIGN.md)

---

**Last Updated**: 2026-01-29
