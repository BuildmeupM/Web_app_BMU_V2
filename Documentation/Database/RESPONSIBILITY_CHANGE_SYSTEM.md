# ระบบการเปลี่ยนผู้รับผิดชอบ (Responsibility Change System)

## 📋 ภาพรวม

ระบบนี้ถูกออกแบบมาเพื่อรองรับการเปลี่ยนผู้รับผิดชอบและการซื้อขายงาน โดยแยกแยะระหว่าง:
- **ผู้รับผิดชอบเดือน** (Original Responsible) - ค่าเดิมเมื่อมีการจัดงาน
- **ผู้รับผิดชอบปัจจุบัน** (Current Responsible) - สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ
- **ผู้รับผิดชอบที่ซื้อ** (Purchased By Responsible) - สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง `monthly_tax_data`

#### ฟิลด์ที่เกี่ยวข้องกับผู้รับผิดชอบทำบัญชี (Accounting Responsible)
- `accounting_responsible` - ผู้รับผิดชอบทำบัญชี (ใช้สำหรับการ query และแสดงผล)
- `original_accounting_responsible` - ผู้รับผิดชอบทำบัญชีเดือน (ค่าเดิมเมื่อมีการจัดงาน)
- `current_accounting_responsible` - ผู้รับผิดชอบทำบัญชีปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ)
- `purchased_by_accounting_responsible` - ผู้รับผิดชอบที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ)

#### ฟิลด์ที่เกี่ยวข้องกับผู้ยื่น WHT (WHT Filer)
- `wht_filer_employee_id` - ผู้ยื่น WHT เดิม (ใช้สำหรับการ query และแสดงผล)
- `wht_filer_current_employee_id` - ผู้ยื่น WHT ปัจจุบัน (มีอยู่แล้ว)
- `original_wht_filer_employee_id` - ผู้ยื่น WHT เดิม (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `purchased_by_wht_filer_employee_id` - ผู้ยื่น WHT ที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้ยื่น VAT (VAT Filer)
- `vat_filer_employee_id` - ผู้ยื่น VAT เดิม (ใช้สำหรับการ query และแสดงผล)
- `vat_filer_current_employee_id` - ผู้ยื่น VAT ปัจจุบัน (มีอยู่แล้ว)
- `original_vat_filer_employee_id` - ผู้ยื่น VAT เดิม (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `purchased_by_vat_filer_employee_id` - ผู้ยื่น VAT ที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้รับผิดชอบในการคีย์ (Document Entry Responsible)
- `document_entry_responsible` - ผู้รับผิดชอบในการคีย์ (ใช้สำหรับการ query และแสดงผล)
- `original_document_entry_responsible` - ผู้รับผิดชอบในการคีย์เดือน (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_document_entry_responsible` - ผู้รับผิดชอบในการคีย์ปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_document_entry_responsible` - ผู้รับผิดชอบในการคีย์ที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้ตรวจภาษี (Tax Inspection Responsible)
- `tax_inspection_responsible` - ผู้ตรวจภาษี (ใช้สำหรับการ query และแสดงผล)
- `original_tax_inspection_responsible` - ผู้ตรวจภาษีเดือน (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_tax_inspection_responsible` - ผู้ตรวจภาษีปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_tax_inspection_responsible` - ผู้ตรวจภาษีที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

### ตาราง `work_assignments`

โครงสร้างเหมือนกับ `monthly_tax_data` แต่ใช้ชื่อฟิลด์ที่แตกต่างกันเล็กน้อย:

#### ฟิลด์ที่เกี่ยวข้องกับผู้รับผิดชอบทำบัญชี (Accounting Responsible)
- `accounting_responsible` - ผู้รับผิดชอบทำบัญชี (ใช้สำหรับการ query และแสดงผล)
- `original_accounting_responsible` - ผู้รับผิดชอบทำบัญชีเดือน (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_accounting_responsible` - ผู้รับผิดชอบทำบัญชีปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_accounting_responsible` - ผู้รับผิดชอบที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้ยื่น WHT (WHT Filer)
- `wht_filer_responsible` - ผู้รับผิดชอบยื่น WHT (ใช้สำหรับการ query และแสดงผล)
- `original_wht_filer_responsible` - ผู้รับผิดชอบยื่น WHT เดิม (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_wht_filer_responsible` - ผู้รับผิดชอบยื่น WHT ปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_wht_filer_responsible` - ผู้รับผิดชอบยื่น WHT ที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้ยื่น VAT (VAT Filer)
- `vat_filer_responsible` - ผู้รับผิดชอบยื่น VAT (ใช้สำหรับการ query และแสดงผล)
- `original_vat_filer_responsible` - ผู้รับผิดชอบยื่น VAT เดิม (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_vat_filer_responsible` - ผู้รับผิดชอบยื่น VAT ปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_vat_filer_responsible` - ผู้รับผิดชอบยื่น VAT ที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้รับผิดชอบในการคีย์ (Document Entry Responsible)
- `document_entry_responsible` - ผู้รับผิดชอบในการคีย์ (ใช้สำหรับการ query และแสดงผล)
- `original_document_entry_responsible` - ผู้รับผิดชอบในการคีย์เดือน (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_document_entry_responsible` - ผู้รับผิดชอบในการคีย์ปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_document_entry_responsible` - ผู้รับผิดชอบในการคีย์ที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

#### ฟิลด์ที่เกี่ยวข้องกับผู้ตรวจภาษี (Tax Inspection Responsible)
- `tax_inspection_responsible` - ผู้ตรวจภาษี (ใช้สำหรับการ query และแสดงผล)
- `original_tax_inspection_responsible` - ผู้ตรวจภาษีเดือน (ค่าเดิมเมื่อมีการจัดงาน) **[ใหม่]**
- `current_tax_inspection_responsible` - ผู้ตรวจภาษีปัจจุบัน (สำหรับฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ) **[ใหม่]**
- `purchased_by_tax_inspection_responsible` - ผู้ตรวจภาษีที่ซื้อ (สำหรับเก็บข้อมูลของพนักงานที่ซื้องานไปทำต่อ) **[ใหม่]**

## 🔄 Logic การใช้งาน

### 1. การจัดงานใหม่ (Work Assignment)

เมื่อมีการจัดงานใหม่ (สร้าง `work_assignments` ใหม่):

```sql
-- ใน work_assignments
SET original_accounting_responsible = accounting_responsible
SET current_accounting_responsible = accounting_responsible
SET original_wht_filer_responsible = wht_filer_responsible
SET current_wht_filer_responsible = wht_filer_responsible
SET original_vat_filer_responsible = vat_filer_responsible
SET current_vat_filer_responsible = vat_filer_responsible
SET original_document_entry_responsible = document_entry_responsible
SET current_document_entry_responsible = document_entry_responsible
SET original_tax_inspection_responsible = tax_inspection_responsible
SET current_tax_inspection_responsible = tax_inspection_responsible

-- ใน monthly_tax_data (เมื่อมีการรีเซ็ตข้อมูล)
SET original_accounting_responsible = accounting_responsible
SET current_accounting_responsible = accounting_responsible
SET original_wht_filer_employee_id = wht_filer_employee_id
SET original_vat_filer_employee_id = vat_filer_employee_id
SET original_document_entry_responsible = document_entry_responsible
SET original_tax_inspection_responsible = tax_inspection_responsible
```

### 2. การซื้อขายงาน (Accounting Marketplace Purchase)

เมื่อมีการซื้องานผ่านระบบตลาดกลาง:

```sql
-- อัปเดต accounting_responsible (เหมือนเดิม)
UPDATE monthly_tax_data 
SET accounting_responsible = buyer_employee_id
WHERE build = ? AND tax_year = ? AND tax_month = ?

-- เพิ่ม: บันทึกข้อมูลการซื้อ
UPDATE monthly_tax_data 
SET 
  purchased_by_accounting_responsible = buyer_employee_id,
  current_accounting_responsible = buyer_employee_id
WHERE build = ? AND tax_year = ? AND tax_month = ?

-- ถ้ายังไม่มี original_accounting_responsible → ตั้งค่าจาก seller
UPDATE monthly_tax_data 
SET original_accounting_responsible = seller_employee_id
WHERE build = ? AND tax_year = ? AND tax_month = ? 
  AND original_accounting_responsible IS NULL

-- ทำเหมือนกันสำหรับ work_assignments
UPDATE work_assignments 
SET 
  accounting_responsible = buyer_employee_id,
  purchased_by_accounting_responsible = buyer_employee_id,
  current_accounting_responsible = buyer_employee_id
WHERE build = ? AND assignment_year = ? AND assignment_month = ?

UPDATE work_assignments 
SET original_accounting_responsible = seller_employee_id
WHERE build = ? AND assignment_year = ? AND assignment_month = ? 
  AND original_accounting_responsible IS NULL
```

### 3. การเปลี่ยนผู้รับผิดชอบ (Responsibility Change) - อนาคต

เมื่อมีการเปลี่ยนผู้รับผิดชอบ (ฟังก์ชั่นที่จะพัฒนาต่อไป):

```sql
-- อัปเดต current_accounting_responsible และ accounting_responsible
UPDATE monthly_tax_data 
SET 
  accounting_responsible = new_employee_id,
  current_accounting_responsible = new_employee_id
WHERE build = ? AND tax_year = ? AND tax_month = ?

-- ทำเหมือนกันสำหรับ work_assignments และฟิลด์อื่นๆ (WHT, VAT, Document Entry, Tax Inspection)
```

**หมายเหตุ**: การเปลี่ยนผู้รับผิดชอบจะไม่บันทึกใน `purchased_by_*` เพราะไม่ใช่การซื้อขายงาน

## 📊 การ Query และแสดงผล

### สำหรับการแสดงผลในหน้าต่างๆ

#### หน้าตรวจภาษี (Tax Inspection)
- ใช้ `tax_inspection_responsible` หรือ `current_tax_inspection_responsible` (ถ้ามี)

#### หน้าสถานะยื่นภาษี (Tax Filing Status)
- ใช้ `wht_filer_current_employee_id` หรือ `current_wht_filer_responsible` (ถ้ามี)
- ใช้ `vat_filer_current_employee_id` หรือ `current_vat_filer_responsible` (ถ้ามี)

#### หน้ายื่นภาษี (Tax Filing)
- ใช้ `wht_filer_current_employee_id` หรือ `current_wht_filer_responsible` (ถ้ามี)
- ใช้ `vat_filer_current_employee_id` หรือ `current_vat_filer_responsible` (ถ้ามี)

#### หน้าคีย์เอกสาร (Document Entry)
- ใช้ `document_entry_responsible` หรือ `current_document_entry_responsible` (ถ้ามี)

### Priority การ Query

สำหรับการแสดงผลผู้รับผิดชอบปัจจุบัน ใช้ลำดับความสำคัญดังนี้:

1. `current_*_responsible` หรือ `*_current_employee_id` (ถ้ามี)
2. `purchased_by_*_responsible` (ถ้ามี)
3. `*_responsible` หรือ `*_employee_id` (ค่าเดิม)

**ตัวอย่าง**:
```sql
SELECT 
  COALESCE(
    current_accounting_responsible,
    purchased_by_accounting_responsible,
    accounting_responsible
  ) as effective_accounting_responsible
FROM monthly_tax_data
WHERE build = ? AND tax_year = ? AND tax_month = ?
```

## 🔍 การ Query ประวัติ

### ดูประวัติการซื้อขายงาน
```sql
SELECT 
  original_accounting_responsible,
  current_accounting_responsible,
  purchased_by_accounting_responsible,
  accounting_responsible
FROM monthly_tax_data
WHERE build = ? AND tax_year = ? AND tax_month = ?
```

### ดูผู้รับผิดชอบเดิมเมื่อมีการจัดงาน
```sql
SELECT 
  original_accounting_responsible,
  original_wht_filer_responsible,
  original_vat_filer_responsible,
  original_document_entry_responsible,
  original_tax_inspection_responsible
FROM work_assignments
WHERE build = ? AND assignment_year = ? AND assignment_month = ?
```

## ⚠️ หมายเหตุสำคัญ

1. **การจัดงานใหม่**: เมื่อมีการจัดงานใหม่ ต้องตั้งค่า `original_*` และ `current_*` ให้เท่ากับค่าที่จัดงาน
2. **การซื้อขายงาน**: ต้องบันทึก `purchased_by_*` และอัปเดต `current_*` และ `*_responsible` ให้เป็นผู้ซื้อ
3. **การเปลี่ยนผู้รับผิดชอบ**: จะอัปเดตเฉพาะ `current_*` และ `*_responsible` ไม่ได้บันทึกใน `purchased_by_*`
4. **Backward Compatibility**: ฟิลด์เดิม (`accounting_responsible`, `wht_filer_employee_id`, etc.) ยังคงใช้ได้ตามปกติ แต่ควรใช้ `current_*` หรือ `purchased_by_*` เมื่อมีข้อมูล

## 📝 Migration

- **Migration 034**: เพิ่มฟิลด์ใหม่ทั้งหมดใน `monthly_tax_data` และ `work_assignments`
- **Migration 035**: อัปเดต backend logic สำหรับการซื้อขายงานให้บันทึก `purchased_by_*` fields

## 🚀 การพัฒนาต่อ

1. **หน้าตรวจภาษี**: ต้องรองรับการแสดงผล `current_tax_inspection_responsible`
2. **หน้าสถานะยื่นภาษี**: ต้องรองรับการแสดงผล `current_wht_filer_*` และ `current_vat_filer_*`
3. **หน้ายื่นภาษี**: ต้องรองรับการแสดงผล `current_wht_filer_*` และ `current_vat_filer_*`
4. **ฟังก์ชั่นการเปลี่ยนผู้รับผิดชอบ**: พัฒนา UI และ backend สำหรับการเปลี่ยนผู้รับผิดชอบ
