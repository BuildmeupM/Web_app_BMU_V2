# 🔄 คู่มือการรีเซ็ตข้อมูลพนักงาน

## 📋 Overview

คู่มือนี้จะอธิบายวิธีการรีเซ็ตข้อมูลพนักงานทั้งหมดในฐานข้อมูลเพื่อนำเข้าข้อมูลใหม่

## ⚠️ คำเตือน

- **การรีเซ็ตข้อมูลจะลบข้อมูลพนักงานทั้งหมดถาวร (hard delete)**
- **ไม่สามารถกู้คืนข้อมูลได้** หลังจากรีเซ็ตแล้ว
- **ข้อมูลใน `users` table จะไม่ถูกลบ** แต่ `user_id` ใน `employees` จะถูก set เป็น NULL
- **ควร backup ข้อมูลก่อนรีเซ็ต** (ถ้าต้องการเก็บข้อมูลเดิม)

---

## 🚀 วิธีใช้งาน

### วิธีที่ 1: ใช้ Script (แนะนำ)

1. **เปิด Terminal/Command Prompt** ในโฟลเดอร์ `backend`

2. **รันคำสั่ง**:
   ```bash
   npm run reset-employees
   ```

3. **ยืนยันการรีเซ็ต**:
   - พิมพ์ `YES` เพื่อยืนยันครั้งแรก
   - พิมพ์ `CONFIRM` เพื่อยืนยันครั้งที่สอง

4. **รอให้ script ทำงานเสร็จ**

5. **นำเข้าข้อมูลใหม่**:
   - ใช้หน้าเว็บ "นำเข้าจาก Excel" เพื่อนำเข้าข้อมูลใหม่
   - หรือใช้ API `POST /api/employees` เพื่อเพิ่มข้อมูลทีละรายการ

### วิธีที่ 2: ใช้ SQL โดยตรง

1. **เปิด phpMyAdmin หรือ MySQL Client**

2. **รัน SQL Commands**:
   ```sql
   -- Step 1: ลบ foreign key references
   UPDATE employees SET user_id = NULL;

   -- Step 2: ลบข้อมูลทั้งหมด
   DELETE FROM employees;

   -- Step 3: รีเซ็ต AUTO_INCREMENT (ถ้าใช้)
   ALTER TABLE employees AUTO_INCREMENT = 1;
   ```

3. **ตรวจสอบผลลัพธ์**:
   ```sql
   SELECT COUNT(*) as total FROM employees;
   -- ควรได้ 0
   ```

---

## 📊 สิ่งที่ Script จะทำ

1. **แสดงสถิติข้อมูลปัจจุบัน**:
   - จำนวนข้อมูลทั้งหมด
   - จำนวนข้อมูลที่ยังไม่ถูกลบ
   - จำนวนข้อมูลที่ถูกลบ (soft delete)

2. **ขอการยืนยัน 2 ครั้ง**:
   - ครั้งแรก: พิมพ์ `YES`
   - ครั้งที่สอง: พิมพ์ `CONFIRM`

3. **ลบ Foreign Key References**:
   - Set `user_id` เป็น `NULL` สำหรับทุก employee
   - เพื่อหลีกเลี่ยง foreign key constraint errors

4. **ลบข้อมูลทั้งหมด**:
   - ใช้ `DELETE FROM employees` (hard delete)
   - ลบทั้งข้อมูลที่ยังไม่ถูกลบและที่ถูก soft delete

5. **รีเซ็ต AUTO_INCREMENT**:
   - รีเซ็ต counter (ถ้าใช้ AUTO_INCREMENT)
   - ถ้าใช้ UUID จะข้ามขั้นตอนนี้

---

## 🔍 ตรวจสอบผลลัพธ์

หลังจากรีเซ็ตแล้ว สามารถตรวจสอบได้ด้วย:

```sql
-- ตรวจสอบจำนวนข้อมูล
SELECT COUNT(*) as total FROM employees;
-- ควรได้ 0

-- ตรวจสอบว่า users table ยังมีข้อมูลอยู่
SELECT COUNT(*) as total FROM users;
-- ควรยังมีข้อมูลอยู่ (ถ้ามี)

-- ตรวจสอบ foreign keys
SELECT COUNT(*) as total FROM employees WHERE user_id IS NOT NULL;
-- ควรได้ 0
```

---

## 📝 ขั้นตอนถัดไป

หลังจากรีเซ็ตข้อมูลแล้ว:

1. **นำเข้าข้อมูลใหม่**:
   - ใช้หน้าเว็บ "นำเข้าจาก Excel"
   - หรือใช้ API `POST /api/employees`

2. **ตรวจสอบข้อมูล**:
   - ตรวจสอบว่าข้อมูลถูกนำเข้าถูกต้อง
   - ตรวจสอบ required fields

3. **เชื่อมโยงกับ Users** (ถ้าจำเป็น):
   - อัพเดท `user_id` ใน `employees` table
   - เชื่อมโยงกับ `users` table

---

## ⚙️ Script Location

- **Script File**: `backend/scripts/reset-employees.js`
- **Package Script**: `npm run reset-employees`

---

## 🛡️ Security Notes

- Script นี้ควรใช้เฉพาะใน **development environment**
- สำหรับ **production** ควรใช้ migration scripts แทน
- ควร backup ข้อมูลก่อนรีเซ็ตเสมอ

---

## 📚 Related Documentation

- [Excel Import Guide](./Employee/EXCEL_TEMPLATE_GUIDE.md)
- [Employee API Documentation](../API/EMPLOYEE_API_IMPLEMENTATION.md)
- [Database Schema](./schema.md)

---

**Last Updated**: 2026-01-29
