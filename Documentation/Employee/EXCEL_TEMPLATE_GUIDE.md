# 📊 คู่มือการใช้งาน Excel Template สำหรับนำเข้าข้อมูลพนักงาน

## 📋 Overview

เอกสารนี้อธิบายวิธีการใช้ Excel Template สำหรับการนำเข้าข้อมูลพนักงานเข้าสู่ระบบ BMU Work Management System

---

## 📥 ดาวน์โหลด Template

**ไฟล์ Template**: `templates/employee_import_template.xlsx`

**วิธีดาวน์โหลด**:
1. ไปที่หน้า "ข้อมูลพนักงาน"
2. คลิกปุ่ม "นำเข้าข้อมูล"
3. คลิก "ดาวน์โหลด Template" เพื่อดาวน์โหลดไฟล์ Excel template

---

## 📐 โครงสร้าง Excel Template

### Header Row (แถวที่ 1)

Template จะมี **32 columns** ตามลำดับดังนี้:

| # | Column Name (Thai) | Column Name (English) | Required | Type | Description |
|---|-------------------|----------------------|----------|------|-------------|
| 1 | รหัสพนักงาน | employee_id | ✅ | Text | รหัสพนักงาน (เชื่อมกับ users.employee_id) |
| 2 | ตำแหน่ง | position | ✅ | Text | ตำแหน่งการทำงาน |
| 3 | รหัสบัตรประชาชน | id_card | ✅ | Text | รหัสบัตรประชาชน 13 หลัก |
| 4 | เพศ | gender | ✅ | Text | เพศ (ชาย, หญิง, อื่นๆ) |
| 5 | ชื่อจริง | first_name | ✅ | Text | ชื่อจริง |
| 6 | นามสกุล | last_name | ✅ | Text | นามสกุล |
| 7 | ชื่อภาษาอังกฤษ | english_name | ❌ | Text | ชื่อภาษาอังกฤษ |
| 8 | ชื่อเล่น | nick_name | ❌ | Text | ชื่อเล่น |
| 9 | วันเกิด | birth_date | ❌ | Date | วันเกิด (รูปแบบ: YYYY-MM-DD) |
| 10 | เบอร์โทร | phone | ❌ | Text | เบอร์โทร |
| 11 | Email | personal_email | ❌ | Email | อีเมลส่วนตัว |
| 12 | Email Build | company_email | ❌ | Email | อีเมลบริษัท (สามารถซ้ำกันได้ - ระบบจะแจ้งเตือน) |
| 13 | PassWord E-mail Buildme | company_email_password | ❌ | Text | รหัสผ่านอีเมลบริษัท |
| 14 | วันเริ่มงาน | hire_date | ✅ | Date | วันเริ่มงาน (รูปแบบ: YYYY-MM-DD) |
| 15 | วันผ่านงาน | probation_end_date | ❌ | Date | วันผ่านงาน (รูปแบบ: YYYY-MM-DD) |
| 16 | วันสิ้นสุด | resignation_date | ❌ | Date | วันลาออก (รูปแบบ: YYYY-MM-DD) |
| 17 | สถานะงาน | status | ✅ | Text | ทำงานอยู่ หรือ ลาออก |
| 18 | ที่อยู่ | address_full | ❌ | Text | ที่อยู่รวม |
| 19 | หมู่บ้าน | village | ❌ | Text | หมู่บ้าน |
| 20 | อาคาร | building | ❌ | Text | อาคาร |
| 21 | ห้องเลขที่ | room_number | ❌ | Text | ห้องเลขที่ |
| 22 | ชั้นที่ | floor_number | ❌ | Text | ชั้นที่ |
| 23 | เลขที่ | house_number | ❌ | Text | เลขที่ |
| 24 | ซอย/ตรอก | soi_alley | ❌ | Text | ซอย/ตรอก |
| 25 | หมู่ที่ | moo | ❌ | Text | หมู่ที่ |
| 26 | ถนน | road | ❌ | Text | ถนน |
| 27 | แขวง/ตำบล | sub_district | ❌ | Text | แขวง/ตำบล |
| 28 | อำเภอ/เขต | district | ❌ | Text | อำเภอ/เขต |
| 29 | จังหวัด | province | ❌ | Text | จังหวัด |
| 30 | รหัสไปรษณีย์ | postal_code | ❌ | Text | รหัสไปรษณีย์ |
| 31 | รูปภาพ | profile_image | ❌ | Text | Path/URL ของรูปภาพ |

**หมายเหตุ**: 
- Column "ชื่อ - นามสกุล" จะถูกสร้างอัตโนมัติจาก "ชื่อจริง" + "นามสกุล" ไม่ต้องกรอก
- Columns ที่มี ✅ Required ต้องกรอกข้อมูล
- Columns ที่มี ❌ Optional สามารถเว้นว่างได้

---

## 📝 วิธีการกรอกข้อมูล

### 1. ข้อมูลพื้นฐาน (Required Fields)

#### รหัสพนักงาน (employee_id)
- **รูปแบบ**: Text (เช่น: AC00010, EMP001)
- **ข้อกำหนด**: 
  - ต้องไม่ซ้ำกับพนักงานที่มีอยู่แล้ว
  - ควรใช้รูปแบบที่สอดคล้องกัน (เช่น: AC00010, AC00011)
- **ตัวอย่าง**: `AC00010`

#### ตำแหน่ง (position)
- **รูปแบบ**: Text
- **ตัวอย่าง**: `ผู้จัดการ`, `นักบัญชี`, `โปรแกรมเมอร์`

#### รหัสบัตรประชาชน (id_card)
- **รูปแบบ**: ตัวเลข 13 หลัก
- **ข้อกำหนด**: 
  - ต้องเป็นตัวเลข 13 หลักเท่านั้น
  - สามารถใส่เครื่องหมาย `-` ได้ (ระบบจะลบออกอัตโนมัติ)
  - สามารถซ้ำกันได้ (ระบบจะแจ้งเตือนแต่ไม่ข้ามข้อมูล)
- **ตัวอย่าง**: `1234567890123` หรือ `1-2345-67890-12-3`

#### เพศ (gender)
- **รูปแบบ**: Text
- **ค่าที่ใช้ได้**: 
  - `ชาย` → จะถูกแปลงเป็น `male`
  - `หญิง` → จะถูกแปลงเป็น `female`
  - `อื่นๆ` → จะถูกแปลงเป็น `other`
- **ตัวอย่าง**: `ชาย`, `หญิง`, `อื่นๆ`

#### ชื่อจริง (first_name) และ นามสกุล (last_name)
- **รูปแบบ**: Text
- **ข้อกำหนด**: ต้องกรอกทั้งสองฟิลด์
- **ตัวอย่าง**: 
  - ชื่อจริง: `ยุทธนา`
  - นามสกุล: `(เอ็ม)`
  - ผลลัพธ์: `ยุทธนา (เอ็ม)` (สร้างอัตโนมัติ)

#### ชื่อเล่น (nick_name)
- **รูปแบบ**: Text
- **ข้อกำหนด**: ต้องกรอก (บังคับ)
- **ตัวอย่าง**: `เอ็ม`, `จอห์น`, `แมรี่`

#### วันเริ่มงาน (hire_date)
- **รูปแบบ**: Date (YYYY-MM-DD)
- **ตัวอย่าง**: `2024-01-15`, `2024-12-31`

#### สถานะงาน (status)
- **รูปแบบ**: Text
- **ค่าที่ใช้ได้**: 
  - `ทำงานอยู่` → จะถูกแปลงเป็น `active`
  - `ลาออก` → จะถูกแปลงเป็น `resigned`
- **ตัวอย่าง**: `ทำงานอยู่`, `ลาออก`

---

### 2. ข้อมูลส่วนตัว (Optional Fields)

#### วันเกิด (birth_date)
- **รูปแบบ**: Date (YYYY-MM-DD)
- **ตัวอย่าง**: `1990-05-20`

#### เบอร์โทร (phone)
- **รูปแบบ**: Text
- **ตัวอย่าง**: `0812345678`, `02-123-4567`

#### Email (personal_email)
- **รูปแบบ**: Email address
- **ตัวอย่าง**: `john.doe@example.com`

#### Email Build (company_email)
- **รูปแบบ**: Email address
- **ข้อกำหนด**: ต้องไม่ซ้ำกับพนักงานอื่น
- **ตัวอย่าง**: `john.doe@buildmeup.com`

---

### 3. ข้อมูลที่อยู่ (Optional Fields)

สามารถกรอกได้ 2 แบบ:

#### แบบที่ 1: กรอกที่อยู่รวม (address_full)
- กรอกที่อยู่ทั้งหมดใน column "ที่อยู่"
- ตัวอย่าง: `123 ถนนสุขุมวิท แขวงคลองตัน เขตคลองตัน กรุงเทพมหานคร 10110`

#### แบบที่ 2: กรอกแยกส่วน
- กรอกแต่ละส่วนแยกกัน:
  - เลขที่: `123`
  - ซอย/ตรอก: `สุขุมวิท 39`
  - ถนน: `สุขุมวิท`
  - แขวง/ตำบล: `คลองตัน`
  - อำเภอ/เขต: `คลองตัน`
  - จังหวัด: `กรุงเทพมหานคร`
  - รหัสไปรษณีย์: `10110`

**หมายเหตุ**: สามารถกรอกทั้งสองแบบพร้อมกันได้ ระบบจะใช้ข้อมูลที่กรอกไว้

---

## ✅ ตัวอย่างการกรอกข้อมูล

### ตัวอย่างแถวข้อมูล (Row 2)

| รหัสพนักงาน | ตำแหน่ง | รหัสบัตรประชาชน | เพศ | ชื่อจริง | นามสกุล | ... | วันเริ่มงาน | สถานะงาน |
|------------|---------|----------------|-----|---------|---------|-----|------------|----------|
| AC00010 | ผู้จัดการ | 1234567890123 | ชาย | ยุทธนา | (เอ็ม) | ... | 2024-01-15 | ทำงานอยู่ |

### ตัวอย่างแถวข้อมูล (Row 3)

| รหัสพนักงาน | ตำแหน่ง | รหัสบัตรประชาชน | เพศ | ชื่อจริง | นามสกุล | ... | วันเริ่มงาน | สถานะงาน |
|------------|---------|----------------|-----|---------|---------|-----|------------|----------|
| AC00011 | นักบัญชี | 9876543210987 | หญิง | สมหญิง | ใจดี | ... | 2024-02-01 | ทำงานอยู่ |

---

## ⚠️ ข้อควรระวัง

### 1. รูปแบบวันที่
- **ต้องใช้รูปแบบ**: `YYYY-MM-DD` (เช่น: `2024-01-15`)
- **ไม่ใช้รูปแบบ**: `15/01/2024`, `15-01-2024`, `Jan 15, 2024`
- **วิธีแก้ไขใน Excel**:
  1. เลือก column ที่เป็นวันที่
  2. คลิกขวา → Format Cells
  3. เลือก Custom → พิมพ์ `yyyy-mm-dd`

### 2. รหัสบัตรประชาชน
- ต้องเป็นตัวเลข 13 หลักเท่านั้น
- สามารถใส่เครื่องหมาย `-` ได้ (ระบบจะลบออกอัตโนมัติ)
- ตรวจสอบให้แน่ใจว่าไม่ซ้ำกับพนักงานอื่น

### 3. Email
- Email Build (company_email) สามารถซ้ำกันได้ (ระบบจะแจ้งเตือนแต่ไม่ข้ามข้อมูล)
- ตรวจสอบรูปแบบ email ให้ถูกต้อง (มี @ และ domain)

### 4. รหัสพนักงาน
- ต้องไม่ซ้ำกับพนักงานที่มีอยู่แล้ว
- ควรใช้รูปแบบที่สอดคล้องกัน

### 5. ข้อมูล Required Fields
- ต้องกรอกข้อมูลใน Required Fields ทั้งหมด:
  - รหัสพนักงาน
  - ตำแหน่ง
  - รหัสบัตรประชาชน
  - เพศ
  - ชื่อจริง
  - นามสกุล
  - ชื่อเล่น (บังคับกรอก)

---

## 🚀 ขั้นตอนการนำเข้าข้อมูล

### Step 1: ดาวน์โหลด Template
1. ไปที่หน้า "ข้อมูลพนักงาน"
2. คลิกปุ่ม "นำเข้าข้อมูล"
3. คลิก "ดาวน์โหลด Template"

### Step 2: กรอกข้อมูลใน Excel
1. เปิดไฟล์ template ที่ดาวน์โหลดมา
2. กรอกข้อมูลพนักงานในแถวที่ 2 เป็นต้นไป
3. ตรวจสอบข้อมูลให้ถูกต้อง:
   - Required fields ครบถ้วน
   - รูปแบบวันที่ถูกต้อง (YYYY-MM-DD)
   - รหัสบัตรประชาชน 13 หลัก
   - Email Build สามารถซ้ำกันได้ (ระบบจะแจ้งเตือน)
   - รหัสพนักงานไม่ซ้ำกัน

### Step 3: บันทึกไฟล์
1. บันทึกไฟล์เป็น `.xlsx` format
2. ตรวจสอบว่าไฟล์ไม่เกิน 10MB

### Step 4: อัพโหลดไฟล์
1. กลับไปที่หน้า "นำเข้าข้อมูล"
2. คลิก "เลือกไฟล์" และเลือกไฟล์ Excel ที่กรอกข้อมูลแล้ว
3. ตรวจสอบข้อมูลใน Preview (ถ้ามี)
4. คลิก "นำเข้าข้อมูล"

### Step 5: ตรวจสอบผลลัพธ์
1. ระบบจะแสดงผลการนำเข้า:
   - จำนวนรายการทั้งหมด
   - จำนวนรายการที่สำเร็จ
   - จำนวนรายการที่ล้มเหลว
   - รายละเอียดข้อผิดพลาด (ถ้ามี)

---

## 🔍 การตรวจสอบข้อผิดพลาด

### ข้อผิดพลาดที่พบบ่อย

#### 1. Missing required fields
**ข้อความ**: `Missing required fields: employee_id, position, or id_card`  
**สาเหตุ**: ไม่ได้กรอกข้อมูลใน Required Fields  
**วิธีแก้**: กรอกข้อมูลใน Required Fields ทั้งหมด

#### 2. Duplicate employee_id
**ข้อความ**: `Duplicate employee_id: AC00010`  
**สาเหตุ**: รหัสพนักงานซ้ำกับพนักงานที่มีอยู่แล้ว  
**วิธีแก้**: เปลี่ยนรหัสพนักงานเป็นค่าที่ไม่ซ้ำ

#### 3. Invalid date format
**ข้อความ**: `Invalid date format`  
**สาเหตุ**: รูปแบบวันที่ไม่ถูกต้อง  
**วิธีแก้**: ใช้รูปแบบ `YYYY-MM-DD` (เช่น: `2024-01-15`)

#### 4. Invalid id_card format
**ข้อความ**: `Invalid id_card format`  
**สาเหตุ**: รหัสบัตรประชาชนไม่ใช่ 13 หลัก  
**วิธีแก้**: ตรวจสอบให้แน่ใจว่าเป็นตัวเลข 13 หลัก

#### 5. Duplicate company_email (แก้ไขแล้ว - ระบบรองรับข้อมูลซ้ำกัน)
**สถานะ**: ✅ **แก้ไขแล้ว** - ระบบรองรับข้อมูลซ้ำกันแล้ว (2026-01-29)

**พฤติกรรมปัจจุบัน**:
- ระบบจะแจ้งเตือนเมื่อพบ Email Build ซ้ำกัน (ทั้งในไฟล์และในฐานข้อมูล)
- ข้อมูลทั้งหมดจะถูกนำเข้าแม้ว่า Email Build จะซ้ำกัน
- ไม่มี error เมื่อพบ Email Build ซ้ำกัน

**ดูเพิ่มเติม**: [คู่มือการรองรับ Email Build ซ้ำกัน](../Database/ALLOW_DUPLICATE_EMAIL_GUIDE.md)

---

## 📊 ตัวอย่างไฟล์ Excel Template

### Sheet 1: Employee Data

| A | B | C | D | E | F | G | ... |
|---|---||---||---||---||---|-----|
| รหัสพนักงาน | ตำแหน่ง | รหัสบัตรประชาชน | เพศ | ชื่อจริง | นามสกุล | ... | ... |
| AC00010 | ผู้จัดการ | 1234567890123 | ชาย | ยุทธนา | (เอ็ม) | ... | ... |
| AC00011 | นักบัญชี | 9876543210987 | หญิง | สมหญิง | ใจดี | ... | ... |

---

## 💡 Tips & Best Practices

### 1. ใช้ Data Validation
- สร้าง Dropdown list สำหรับ "เพศ" และ "สถานะงาน"
- ใช้ Data Validation เพื่อป้องกันการกรอกข้อมูลผิดรูปแบบ

### 2. ตรวจสอบข้อมูลก่อนอัพโหลด
- ใช้ Excel Formula เพื่อตรวจสอบ:
  - รหัสบัตรประชาชน = 13 หลัก
  - Email format ถูกต้อง
  - วันที่อยู่ในรูปแบบที่ถูกต้อง

### 3. Backup ข้อมูล
- สำรองไฟล์ Excel ก่อนอัพโหลด
- เก็บไฟล์เดิมไว้เพื่ออ้างอิง

### 4. กรอกข้อมูลทีละน้อย
- ถ้ามีข้อมูลจำนวนมาก แนะนำให้แบ่งเป็นหลายไฟล์
- อัพโหลดทีละ 100-500 รายการ

---

## 📞 ติดต่อสอบถาม

ถ้ามีปัญหาหรือข้อสงสัยเกี่ยวกับการใช้งาน Excel Template:
- ตรวจสอบ Documentation: `Documentation/Database/EMPLOYEE_COLUMN_MAPPING.md`
- ติดต่อผู้ดูแลระบบ

---

**Last Updated**: 2026-01-29  
**Version**: 1.1.0

## 📝 Changelog

### Version 1.1.0 (2026-01-29)
- ✅ ระบบรองรับ Email Build ซ้ำกันได้แล้ว
- ✅ แจ้งเตือนเมื่อพบ Email Build ซ้ำกัน แต่ไม่ข้ามข้อมูล
- ✅ อัพเดทเอกสารเกี่ยวกับ duplicate email handling

### Version 1.0.0 (2026-01-29)
- Initial release
