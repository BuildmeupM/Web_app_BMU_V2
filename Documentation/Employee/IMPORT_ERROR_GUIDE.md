# 🔍 คู่มือแก้ไขข้อผิดพลาดการนำเข้าข้อมูล - Employee Import

## 📋 Overview

เอกสารนี้อธิบายข้อผิดพลาดที่พบบ่อยในการนำเข้าข้อมูลพนักงานจาก Excel และวิธีแก้ไข

---

## ❌ ข้อผิดพลาดที่พบบ่อย

### 1. Duplicate Entry for Key 'company_email' (แก้ไขแล้ว - ระบบรองรับข้อมูลซ้ำกัน)

**⚠️ หมายเหตุ**: ปัญหานี้ได้รับการแก้ไขแล้ว ระบบรองรับการนำเข้าข้อมูลที่มี Email Build ซ้ำกันได้

**ข้อความ Error (เดิม)**:
```
Duplicate entry 'buildmeupacc025@gmail.com' for key 'company_email'
```

**สถานะ**: ✅ **แก้ไขแล้ว** - ระบบรองรับข้อมูลซ้ำกันแล้ว (2026-01-29)

**การเปลี่ยนแปลง**:
- ลบ UNIQUE constraint จาก `company_email` ในฐานข้อมูล
- ระบบจะแจ้งเตือนเมื่อพบ Email Build ซ้ำกัน แต่จะไม่ข้ามข้อมูล
- ข้อมูลทั้งหมดจะถูกนำเข้าแม้ว่า Email Build จะซ้ำกัน

**พฤติกรรมปัจจุบัน**:
- **แจ้งเตือน**: ระบบจะแสดง warning เมื่อพบ Email Build ซ้ำกัน (ทั้งในไฟล์และในฐานข้อมูล)
- **ไม่ข้ามข้อมูล**: ข้อมูลทั้งหมดจะถูกนำเข้าแม้ว่า Email Build จะซ้ำกัน
- **ไม่ error**: ไม่มี database error เมื่อพบ Email Build ซ้ำกัน

**วิธีใช้งาน**:
1. นำเข้าข้อมูลที่มี Email Build ซ้ำกันได้ตามปกติ
2. ระบบจะแสดง warnings ในผลลัพธ์การนำเข้า
3. ตรวจสอบ warnings เพื่อทราบว่ามี Email Build ซ้ำกันหรือไม่

**ดูเพิ่มเติม**: [คู่มือการรองรับ Email Build ซ้ำกัน](../Database/ALLOW_DUPLICATE_EMAIL_GUIDE.md)

**ตัวอย่าง**:
```
แถว 34: AC00038 → buildmeupacc025@gmail.com
แถว 40: AC00040 → buildmeupacc025@gmail.com  ← ซ้ำ!
แถว 41: AC00041 → buildmeupacc025@gmail.com  ← ซ้ำ!
```

**วิธีแก้**: เปลี่ยน email ในแถว 40 และ 41 เป็นค่าอื่น หรือลบออก

---

### 2. Duplicate Entry for Key 'employee_id'

**ข้อความ Error**:
```
Duplicate entry 'AC00010' for key 'employee_id'
```

**สาเหตุ**:
- รหัสพนักงานซ้ำกับข้อมูลที่มีอยู่แล้วในระบบ
- หรือรหัสพนักงานซ้ำกันภายในไฟล์ Excel

**วิธีแก้ไข**:
1. ตรวจสอบว่ารหัสพนักงานนี้มีอยู่ในระบบแล้วหรือไม่
2. ถ้ามีอยู่แล้ว:
   - เปลี่ยนรหัสพนักงานเป็นค่าอื่นที่ไม่ซ้ำ
   - หรืออัพเดทข้อมูลพนักงานที่มีรหัสนี้อยู่แล้วแทนการสร้างใหม่
3. ถ้าซ้ำกันภายในไฟล์: เปลี่ยนรหัสพนักงานให้ไม่ซ้ำกัน

---

### 3. Duplicate Entry for Key 'id_card'

**ข้อความ Error**:
```
Duplicate entry '1234567890123' for key 'id_card'
```

**สาเหตุ**:
- รหัสบัตรประชาชนซ้ำกับข้อมูลที่มีอยู่แล้วในระบบ
- หรือรหัสบัตรประชาชนซ้ำกันภายในไฟล์ Excel

**วิธีแก้ไข**:
1. ตรวจสอบว่ารหัสบัตรประชาชนนี้มีอยู่ในระบบแล้วหรือไม่
2. ถ้ามีอยู่แล้ว: ไม่สามารถแก้ไขได้ (รหัสบัตรประชาชนต้องไม่ซ้ำ)
3. ถ้าซ้ำกันภายในไฟล์: ตรวจสอบว่าพิมพ์ผิดหรือไม่

---

### 4. Missing Required Fields

**ข้อความ Error**:
```
ข้อมูลไม่ครบ: รหัสพนักงาน, ตำแหน่ง, ชื่อเล่น
```

**สาเหตุ**:
- ไม่ได้กรอกข้อมูลใน Required Fields

**Required Fields (บังคับกรอก)**:
1. รหัสพนักงาน
2. ตำแหน่ง
3. รหัสบัตรประชาชน
4. เพศ
5. ชื่อจริง
6. นามสกุล
7. ชื่อเล่น

**วิธีแก้ไข**:
- กรอกข้อมูลใน Required Fields ทั้งหมดให้ครบถ้วน

---

### 5. Invalid ID Card Format

**ข้อความ Error**:
```
รหัสบัตรประชาชนต้องเป็น 13 หลัก
```

**สาเหตุ**:
- รหัสบัตรประชาชนไม่ใช่ 13 หลัก

**วิธีแก้ไข**:
- ตรวจสอบว่ารหัสบัตรประชาชนเป็นตัวเลข 13 หลัก
- ลบเครื่องหมาย `-` ออก (ถ้ามี)

---

## 🔍 วิธีตรวจสอบข้อมูลก่อน Import

### 1. ใช้ Validation Feature

ระบบจะตรวจสอบข้อมูลอัตโนมัติเมื่อเลือกไฟล์ Excel:
- แสดงจำนวนรายการที่ถูกต้อง/ไม่ถูกต้อง
- แสดงรายการข้อมูลที่ไม่ครบ
- แสดงรายการ email/รหัสพนักงานที่ซ้ำ

### 2. ตรวจสอบด้วยตนเองใน Excel

#### ตรวจสอบ Email ซ้ำกัน:
1. เปิดไฟล์ Excel
2. เลือก column "Email Build"
3. ใช้ Conditional Formatting → Highlight Duplicates
4. ตรวจสอบและแก้ไข email ที่ซ้ำกัน

#### ตรวจสอบรหัสพนักงานซ้ำกัน:
1. เลือก column "รหัสพนักงาน"
2. ใช้ Conditional Formatting → Highlight Duplicates
3. ตรวจสอบและแก้ไขรหัสพนักงานที่ซ้ำกัน

---

## 📊 ตัวอย่างการแก้ไข

### ตัวอย่างที่ 1: Email ซ้ำกันในไฟล์

**ปัญหา**:
```
แถว 34: AC00038 → buildmeupacc025@gmail.com
แถว 40: AC00040 → buildmeupacc025@gmail.com  ← ซ้ำ
แถว 41: AC00041 → buildmeupacc025@gmail.com  ← ซ้ำ
```

**วิธีแก้**:
```
แถว 34: AC00038 → buildmeupacc025@gmail.com (คงไว้)
แถว 40: AC00040 → buildmeupacc026@gmail.com (เปลี่ยน)
แถว 41: AC00041 → buildmeupacc027@gmail.com (เปลี่ยน)
```

หรือ

```
แถว 34: AC00038 → buildmeupacc025@gmail.com (คงไว้)
แถว 40: AC00040 → (เว้นว่าง) (ลบ email)
แถว 41: AC00041 → (เว้นว่าง) (ลบ email)
```

---

### ตัวอย่างที่ 2: Email ซ้ำกับข้อมูลในระบบ

**ปัญหา**:
```
Error: Duplicate entry 'buildmeuphr@gmail.com' for key 'company_email'
```

**วิธีแก้**:
1. ตรวจสอบว่ารหัสพนักงาน HR00007 มี email นี้อยู่แล้วหรือไม่
2. ถ้ามีอยู่แล้ว:
   - **Option A**: ลบ email ออกจาก Excel (เว้นว่าง)
   - **Option B**: เปลี่ยน email เป็นค่าอื่น
   - **Option C**: อัพเดทข้อมูลพนักงานที่มี email นี้อยู่แล้วแทนการสร้างใหม่

---

## ✅ Checklist ก่อน Import

- [ ] ตรวจสอบ Required Fields ครบถ้วน
- [ ] ตรวจสอบรหัสพนักงานไม่ซ้ำกันในไฟล์
- [ ] ตรวจสอบรหัสบัตรประชาชนไม่ซ้ำกันในไฟล์
- [ ] ตรวจสอบรหัสพนักงานไม่ซ้ำกับข้อมูลในระบบ (ถ้าเป็นไปได้)
- [ ] ตรวจสอบรูปแบบวันที่ถูกต้อง (YYYY-MM-DD)
- [ ] ตรวจสอบรหัสบัตรประชาชนเป็น 13 หลัก
- [ ] **หมายเหตุ**: Email Build ซ้ำกันได้ (ระบบจะแจ้งเตือนแต่ไม่ข้ามข้อมูล)

---

## 💡 Tips

### 1. ใช้ Excel Functions เพื่อตรวจสอบ

#### ตรวจสอบ Email ซ้ำกัน:
```excel
=COUNTIF($G$2:$G$100, G2)>1
```
- ถ้าได้ `TRUE` แสดงว่า email ซ้ำกัน

#### ตรวจสอบรหัสพนักงานซ้ำกัน:
```excel
=COUNTIF($A$2:$A$100, A2)>1
```
- ถ้าได้ `TRUE` แสดงว่ารหัสพนักงานซ้ำกัน

### 2. ใช้ Data Validation

สร้าง Dropdown list สำหรับ:
- เพศ: ชาย, หญิง, อื่นๆ
- สถานะงาน: ทำงานอยู่, ลาออก

### 3. แบ่ง Import เป็นหลายครั้ง

ถ้ามีข้อมูลจำนวนมาก แนะนำให้:
- แบ่งเป็นหลายไฟล์ (100-200 รายการต่อไฟล์)
- Import ทีละไฟล์
- ตรวจสอบผลลัพธ์ก่อน import ไฟล์ถัดไป

---

## 📞 ติดต่อสอบถาม

ถ้ามีปัญหาหรือข้อสงสัย:
- ตรวจสอบ Documentation: `Documentation/Employee/EXCEL_TEMPLATE_GUIDE.md`
- ติดต่อผู้ดูแลระบบ

---

**Last Updated**: 2026-01-29  
**Version**: 1.0.0
