# =============================================
# ⚠️ SECURITY WARNING ⚠️
# Never commit real credentials to version control!
# Copy this file to .env and fill in your actual values.
# =============================================

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_secure_password_here
DB_NAME=bmu_work_management

# JWT Secret Key
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=generate_a_secure_random_key_at_least_64_chars

# JWT Token Expiration (default: 7 days)
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
HOST=0.0.0.0
# HOST=0.0.0.0 allows access from any IP address on the network
# Use specific IP (e.g., 192.168.1.100) to restrict access to that IP only
# Use localhost or 127.0.0.1 to only allow local access
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
